# cargo's libgit2 does not honor global git url.*.insteadof rewrites (https→git@github.com: SSH),
# so auth fails through an HTTPS proxy. Fetch git dependencies with the git CLI instead.
[net]
git-fetch-with-cli = true
