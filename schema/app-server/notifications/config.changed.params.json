{
  "$id": "urn:bingo:app-server:v1:config.changed.params",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "One notification's params: the event header, then the body's own fields.",
  "properties": {
    "config": {
      "$ref": "../definitions.json#/definitions/ConfigSnapshot"
    },
    "event": {
      "$ref": "../definitions.json#/definitions/EventMeta"
    }
  },
  "required": [
    "config",
    "event"
  ],
  "title": "NotificationParams_for_ConfigChanged",
  "type": "object"
}
