# Feedback States Specification

> Version: v1.88 · Status: in effect (2026-08-19)
> Scope: the unified design conventions for every user-visible feedback state in bingo. The three frontends — TUI, `bingo app-server`, `--print` — share a single source; qa acceptance anchors are at the end.

## General principles

1. **Feedback states must not depend on the environment**: the same operation must produce perceivable feedback in the TUI, in a pipe, in CI, and on a weak-network narrow screen, and feedback information must not live only in the interactive UI (see the CLI-side conventions).
2. **State machine design, not one-shot events**: every async operation goes through `idle → loading → success / error → idle`; it must fully reset, and getting stuck in an intermediate state is forbidden.
3. **Error copy = what happened + what the user can do**: dead-end copy like "operation failed" alone is forbidden.
4. **No motion for motion's sake**: feedback animation is only used to draw attention or bridge state changes; timing and amplitude follow each section's conventions.
5. **Feedback is granular per action, not per control**: duplicate prevention, loading, timeout, and reset all hang off the submit action rather than only off the button click (see the "duplicate prevention" note in the Loading section and 3.1 in the error section).

## State machine

```text
idle ── action ──▶ loading ── success ──▶ success ──┐
                      │                            │
                      ├── timeout ──▶ error ───────┴──▶ idle (must reset)
                      └── failure ──▶ error
```

Four reset actions (qa can assert each one; see "Acceptance anchors"):

1. Clear the `aria-busy` / loading state
2. Remove error styling and `aria-invalid` (no leftover red outline)
3. Remove the error message from the `aria-live` region (by **writing an empty string / updating the copy**, not by deleting the node — most browsers don't announce deleted nodes)
4. Focus transfer: on success → next operation; on failure → focus the error element (field-level) or the retry button (page-level); **the focus must be set asynchronously after the error element finishes rendering** (async focus after render, e.g. requestAnimationFrame or a component effect; implementation-specific), otherwise the focus fails

**Stale-response race**: after a retry/new request is issued, late failure/success responses from the old request must be ignored (abort or sequence-number check); the new success must not be flashed over by an old error. Test this together with the reset.

## 1. Loading (in-progress state)

| Item | Convention |
|---|---|
| Trigger threshold | Appears when an async operation hasn't completed within >200ms |
| Form | Local operations: inline spinner in the button, replacing the button icon slot in place; page-level: skeleton screen in the content area |
| Forbidden | Fullscreen overlay blocking; silent waiting with no feedback |
| Duplicate prevention | De-dup at the **submit action** granularity: a disabled button blocks the mouse but not the Enter key / form onSubmit — the submit action granularity intercepts uniformly; the same action is not submitted twice during loading (idempotency guarantee) |
| Copy | Local buttons don't change copy (keep "Submit" → spinner in place); page-level shows "Loading…" |
| Timeout | Tiered timeouts: **10s for reads, 15s for writes, applied to short synchronous operations** (list_models / count_tokens / complete_text etc.); on timeout, enter the corresponding error level with a retry hint, **primary action = retry**; **long agent turns (streaming + multi-round tools) do not apply 10s/15s** — continuous progress feedback already exists within a turn (status line + activity rows + `chat.busy`), so timeouts are backed by the transport layer (120s/60s) + user interruption; **if a long turn fails (transport timeout/interruption), it escalates to a flow-level error** (retry or return, never a silent local hint); **the timeout timer must be cancelled on success/failure/cancel** — otherwise a late error arriving after success will overwrite the success state. Cancellation mechanism: at the deadline the feedback layer **drops the future** (wrapped in tokio `timeout()`; the reqwest connection is cancelled with it); the sequence-number check is only a defensive fallback |

### 1.1 Motion tokens (the animation contract)

Every moving thing in the TUI is one of **seven tokens**, all issued by a single motion authority
built once from the `motion` setting. Nothing animated reads the frame counter directly, and no
surface carries a private copy of the gate — that split is what let `motion: "off"` cover the
welcome banner while the spinner, the loudest moving thing on screen, kept spinning.

| Token | What moves | Cadence | Under `motion: "off"` |
|---|---|---|---|
| `pulse` | the status-row spinner glyph, and the todo header's | one frame per **120ms** through the unchanged 8-glyph star cycle (`· ✢ * ✻ ✽ ✻ * ✢`) | frozen on `✻` |
| `beam` | an **8-cell** glimmer sweeping the running verb and its ellipsis, its cells painted 55% of the way from the verb's colour to the theme's body text **and bold** | one pass per **2s**, with a dark beat between passes | no window; the verb is one flat colour |
| `stall` | the spinner and verb turn warning-coloured | after **3s** with no event reaching **the conversation on screen**; an event addressed to it resets it (D134: a background instance's stream is not evidence about main's turn) | **still reports** — the colour changes, nothing moves |
| `settle` | the turn's completion row (`✻ Churned for 40.0s`) wears the accent | one **120ms** window at turn end, then rest | **still reports** — one repaint, not a blink |
| `breath` | the update banner's colour | unchanged: 90-frame sine, 270-frame window (see `update-banner.md`) | rests at the base colour, banner kept |
| `title` | the terminal title's marker | `✳ ⠂ ✳ ⠐` at one frame per **960ms** | static `✳` |
| `meter` | a numeric readout that jumped (the status row's `↓ N tokens`) | eases to the new value over **300ms**, ease-out | snaps |

Three rules bind the set:

1. **The gate rests decoration, never information.** `stall` and `settle` keep changing colour with
   `motion: "off"`; `pulse`, `beam`, `breath`, `title` and `meter` stop. A user who asked for
   stillness asked for less movement, not for less to be told — the same boundary §5 draws for
   `prefers-reduced-motion` ("the loading indicator itself must not be removed").
2. **Time is an argument.** Every token is a pure function of a frame number and takes no clock
   reading of its own, so tests drive animation by passing numbers and the demand-gated render loop
   stays deterministic. Cadences are stated in **milliseconds** and converted through the frame
   interval; a token never counts frames.
3. **Animation costs frames, so a token that ends must end.** Anything animating keeps the
   demand-gated loop awake for exactly its own window and hands the session back to zero-write idle
   after it — a still terminal writes no bytes.

**Stall wording.** A stalled turn changes colour and nothing else: the verb, the elapsed seconds and
the Esc hint stay exactly as they were. Nothing new is known at three seconds, so nothing new is
claimed — the colour says "this has been quiet for a while", which is the whole of the truth.

**Settle and write-once.** Printed scrollback rows are final, so the blink happens *before* the row
freezes: the finished turn's last message is held out of scrollback for the 120ms window and enters
it at rest. Freezing it mid-blink would print the accent into the transcript for the rest of the
session.


## 2. Toast (light notification)

| Item | Convention |
|---|---|
| Trigger | When the operation result needs no user decision (saved, copied) |
| Duration | Auto-dismiss after 3s by default; **hovering or keyboard-focusing a toast pauses the timer, and leaving/unfocusing resumes with the remaining time** (not reset); manually closable |
| Stacking | No more than 2 at once; **only when the slots are full is the oldest evicted** (if A has 0.5s left and B arrives while there's room, B queues until A leaves) |
| De-duplication | Repeated triggers of the same kind (5 rapid clicks of "Copied") → **replaced by a single toast with the timer reset**, never stacked |
| Copy | One sentence stating the result + an action entry when needed (e.g. "Copied ✓ / Undo") |
| Accessibility | Container `aria-live="polite"`; readable by screen readers |

### 2.1 Attention channel (feedback for a user who is not looking)

Every state above assumes eyes on the screen. A terminal session routinely runs for minutes while its user is in another window, and general principle 1 ("feedback must not depend on the environment") applies to that environment too: the feedback has to leave the viewport.

| Item | Convention |
|---|---|
| Channel | One setting, `notifications`: `auto` (default) / `bell` / `iterm2` / `kitty` / `ghostty` / `off`. `auto` reads the terminal (`TERM_PROGRAM=iTerm.app` → `OSC 9`; kitty via `TERM_PROGRAM` or `TERM=xterm-kitty` → the three-part `OSC 99`; `TERM_PROGRAM=ghostty` → `OSC 777 ; notify`) and otherwise falls back to the **terminal bell**, which every terminal has. There is no capability probe — a notification protocol has no query, so a wrong guess is silence, and only a terminal that names itself gets its own protocol |
| Triggers (exactly four) | (1) a permission prompt is surfaced — the turn is blocked and only the user can unblock it; (2) a turn whose wall time reached **10s** ended; (3) a turn failed at flow level; (4) **an agent needs looking at** (D98) — either a subagent sent `SendMessage(to: "main", urgent: true)`, or one of its runs failed. Half of trigger 4 is the first thing the model side can pull, and the bell is all it can pull: the words that follow are still the main agent's. Nothing else notifies: a short turn was watched, and a page-level error sits beside a session that carries on |
| Copy | One line, stating the state and nothing more: `Waiting for permission` · `Turn complete` · `Turn failed` · `An agent needs you`. No counts, no summaries, and no agent name — the transcript is where detail belongs, and what there is to read is already on screen |
| Terminal title | `OSC 2`, tracking the same states: busy `✳ bingo — working…`, waiting `✳ bingo — waiting for permission`, idle `bingo — <directory>`. While a turn runs the marker animates on the `title` token — `✳ ⠂ ✳ ⠐`, one frame per 960ms, static `✳` under `motion: "off"` — and a pending permission prompt outranks it: the waiting title is never animated over. Set once per state change (an unchanged title emits nothing, so a busy turn costs about one write per second), and handed back on teardown — including the panic path — but **only if this session ever took it** |
| Multiplexers | Inside tmux the notification OSC travels in the passthrough envelope (tmux does not know 9/99/777 and drops them). The bell and the title stay bare: tmux acts on a bell it can see (`monitor-bell`), and `OSC 2` is a sequence it understands, sets as the pane title and propagates under `set-titles on` — passthrough would set the outer title behind its back and be overwritten on the next redraw |
| Forbidden | Notifying per event (per tool call, per stream chunk); a notification carrying information that exists nowhere on screen; emitting anything at all under `off`, the terminal title included |
| Ownership | The bytes are built by a pure module and written by the terminal driver, which stays the single owner of escape-sequence writes; they are emitted between frames, never inside a viewport diff |

### 2.2 The quiet console (what may write into @main)

@main is the user's conversation with the main agent, not the system's message bus. An agent's lifecycle — spawn, completion, cancellation, ack chases, room traffic, a subagent's notice — does **not** appear there. What the user sees of a dispatch is the dispatch row's own state, and then whatever the main agent says about it. Exactly one thing crosses straight into the flow.

| Item | Convention |
|---|---|
| The one exception | A run reaching `Failed` (a crash, an abnormal termination, an ack watchdog giving up). Bad news must not depend on the main agent's narration: the turn that would have narrated it may never run |
| Form | One line in the flow, in the state-line family (no `❯` bubble — nobody typed it) and in the **error tier**, naming the instance and one line of reason: `⚠ @scout · subagent failed: connection reset`. `Done` and `Cancelled` draw nothing at all — the dispatch row's state already says so |
| Stamp | **Kept**, flush right, per the send-stamp convention — the one state line that carries one. It is news, about someone, at a moment that matters; the other state lines describe *now* and have nothing to stamp |
| Attention | A failure fires the attention channel (§2.1 trigger 4). So does an arriving `SendMessage(to: "main", urgent: true)` — but that one draws **no line**: the bell is the harness's guarantee, the words stay the main agent's |
| The dispatch row | `◉ @scout: fix the parser` while a run is alive, with the last **three** things the instance did hanging under the `⎿` connector (`⏺ Read(src/lexer.rs)`), or one `In progress… · 4 tool uses · 8.3k tokens` when the window is too short to hold them. It settles into `Done (12 tool uses · 8.3k tokens · 1m 4s)`, and **only the settled form reaches scrollback** — a message holding a running row cannot settle, so everything above it is redrawn rather than printed (D106) |
| Several agents at once | A round that dispatched more than one draws **one** block: `⏺ Running 2 agents…` (`⏺ 2 agents finished` once none is left running) over a `├─ @scout: fix the parser · 1 tool use · 2.1k tokens` row per agent with its current activity, or `Done`, under it. Opening any of them takes the group apart into the individual rows (D106) |
| A completion | One dim `● @scout completed · fix the parser` where the task notification reaches the main agent's context, before the main agent says anything about it. Only when the notification is really the main agent's — a run the user started in an agent's own conversation reports to nobody — **and only for a run this turn's own `Agent` call dispatched** (D114): a delivery-triggered run (a room post, a queued message waking a member) still notifies main's context exactly as before, but completes into the conversation rows and the dialog, never the flow. A **failure** keeps its `⚠` alert and gets no second line; a **cancellation** draws nothing, because the user just did it (D106) |
| Deliberate notices | `SendMessage(to: "main")` puts the message in the main agent's inbox and wakes it when idle — and draws **nothing** in the flow (D114): the message is main's mail, not the user's conversation, and the flow's whitelist is the user's own conversation. What the user gets is the status layer's signal on the sender — the per-sender mail count the arrivals mirror now feeds — cleared when that agent's page is visited (v6). The `summary` field still travels for screen-side surfaces and still never enters the envelope: `main_mail` carries exactly what was said. The delivery, the marker and the debounce are untouched — v3 rendered nothing here, v4 printed one `@scout❯` line, and the inbox turn retires it (D106 → D114) |
| Room relays | **Nothing** in the flow, no exceptions (v6 retired D116's `⚑` line on the user's ruling): a post that **names the user** (`@user`, the D99 mention accounting) lights the room's badge with the count in the accent and rings D79 once per mention turn-on — the badge sits in constant view on the conversation rows, so the badge is the message and the bell is the interrupt. Further mentions wait behind the lit badge until the room is read; a user standing in the room gets no ring at all, because the store never sets mention on the active conversation. The room's surfaces are its page and its badge |
| User-DM runs | A run whose triggering batch was entirely the user's own DM messages produces **no** task notification and **no** woken turn for the main agent. That exchange is between the user and that agent (D63). One main-origin item anywhere in the batch and the run is main's business as before |
| Digest debounce | Mail for the main agent (room relays, direct messages) wakes **one** turn per burst: the wake waits for a 2s quiet window, with a 15s ceiling so a chatty room cannot starve it. Never one wake per message. `urgent` skips the wait. **Room relays pass the @-gate first (v6/D118)**: a line naming nobody waits in a per-room pen and enters the mail only on a mention (`@main`/`@all`, released in room order behind the naming line), in bulk at five held lines, or at the 120s age pump — the debounce shapes delivery of what the gate has released. Direct messages are never penned |
| The record is not scoped | Scoping is about attention, not about the record: a run that wakes nobody is still on the agent's row in the background dialog, still in its own transcript, and still on the flow's dispatch row while it lives. The D95 lifecycle feed that used to carry this claim retired with the directory column that read it (D107) |
| How a woken turn ends | Like any other turn: in prose, rendered in `@main` as main speaking, counted on D99's unread. D102 gave it a second, silent ending; D103 removed it on the parity ruling. The noise control is the debounce above and the dispatch row's own state, not a marker that renders as nothing |
| Direct sends | A composer line shaped `@name <message>` or `#room <message>` bypasses the model: it goes to that inbox or that room under the user's own name, leaves a **transient** `Sent to @name` on the info tier, and writes **nothing** into the flow or into the model's history. An unresolved name is prose and opens an ordinary turn — no error, no receipt |
| A send from a page | Same delivery, **no receipt**: the message is drawn on the page it was sent from, the moment it is sent — the console echoes it into that conversation's store exactly as main's own prompt enters main's (D134). A receipt is what a surface prints when it has nowhere to show the thing itself. A *refusal* still speaks, on the warning tier (D105 → v6) |
| A page writes nothing into @main | A page is the screen for a while, not a splice: main's messages and events keep landing in main's own store, untouched, while the page has the terminal — and coming home reprints a recent tail of them (the page turn, D120). Nothing of the page's content enters main's transcript, and nothing of main's enters the page's — since D134 that is a property of the addressing rather than of the projection: every event names its conversation and is written into that store alone |
| Lifecycle states | An instance starting, going idle or being stopped writes **no** flow line, ever. Its surfaces are its conversation row (v6) and its row in the background dialog (D107) |
| The background dialog | `Ctrl+B` with no foreground command opens one modal over everything working in the background — `Agents`, `Shells`, `Rooms` — with `↑/↓ to select · Enter to view · f to foreground · x to stop · ←/Esc to close` under it. It draws nothing into the flow and settles nothing: it is chrome over the composer, rebuilt from the registries every frame (D107) |
| Where the numbers live | The unread and mention counts the accounting store has kept since D99 are rendered **here and nowhere else**: `@scout: reading src/lib.rs… (3 unread)`, the count in the unread colour and in the accent where that conversation said your name. Foregrounding a row reads it, so the badge is gone on the way back (D107) |
| Forbidden | Progress narration into @main; a per-post wake; a flow line for a state change; `urgent` for anything not blocking on the user, or from anyone but a subagent writing to main |

## 3. Error states (3 escalating levels + mixed state)

| Level | Scenario | Form | Focus |
|---|---|---|---|
| Field-level | Input validation failure | Inline below the input: icon + specific reason ("Email format is invalid"); red outline marks only the erroneous field | Focus the corresponding input |
| Page-level | Single request failure, dependency unavailable | Error card/placeholder + retry button | Focus the retry button |
| Flow-level | API down, insufficient permissions, session failure | Full-page state + a way back (never a dead end) | Focus the primary action |
| **Mixed state** | Batch operation partially succeeded | "Succeeded 2/3, 1 failed" + a list of failed items, each individually retryable | Focus the failed-items list |

### 3.1 Error code → user action mapping (mandatory)

Besides "how to report", the error-code contract must provide a **user action**: the UI action for a given error code must be consistent.

| Error code (example) | Meaning | User action | Error level |
|---|---|---|---|
| `AUTH_REQUIRED` / 401 | Login expired / key missing / invalid key | Log in again / configure the key | Flow-level |
| `PERMISSION_DENIED` / 403 | No permission | Go back / request permission | Page/flow-level |
| `SERVER_ERROR` / 500 | Server-side error | Retry later | Page-level |
| `OFFLINE` | No network | Check the network and retry | Page-level |

The mapping table uses the thiserror error types as its single source of truth; a new error code must register its mapping at the same time.

**Error level = typical slot; context can override**: the "error level" column gives the code's **typical level**; the actual presented level is **explicitly carried by the producer based on the trigger context** (not derived by the render layer, not a duplicated mapping in tests) — e.g. `TIMEOUT` short-sync = page-level / long turn = flow-level; when `PERMISSION_DENIED`'s multi-slot codes take the flow-level slot, it renders full-page. The presentation layer and assertions go by the `level` carried on the event.

General:
- Error copy must state "what happened + what the user can do"; the copy may change, but the carried information must not.
- Error codes are presented in the UI as collapsible "advanced details": ordinary users see only the human-readable copy by default; expanding reveals `code=...`.
- After a field-level error is focused, a successful retry/fix must return to idle (see the state-machine reset).

## 4. CLI-side conventions (headless / pipe / CI)

### 4.1 Structured error protocol

- Errors are defined with stable codes/categories (e.g. `CONFIG_INVALID`, `PERMISSION_DENIED`); the thiserror error types in code are the single source of truth; the render layer only consumes, never re-implements.
- UI rendering, CLI output, logs, and qa assertions share the same error definition:
  - Assertions rely on the error code, not string matching of copy; copy changes don't break tests.
- **Stable output contract** (this exact format for all non-TTY output, greppable):

```text
[error] code=CONFIG_INVALID msg=config file could not be parsed: line 3
```

  `key=value` on a single line; `code` is the stable error code, `msg` is the human-readable copy (changeable). qa assertions depend only on `code`.
- **msg escaping conventions** (to protect key=value parsing; stable for qa/log grep):
  - Newlines/tabs normalized to spaces;
  - The primary `msg` is truncated to 200 chars (truncation beyond that is expected);
  - Multi-line stacks go in a separate `detail=` field; the primary `msg` stays single-line.
- **Error code naming convention**: always `SCREAMING_SNAKE` (e.g. `CONFIG_INVALID`); new codes register accordingly.

### 4.2 TTY / non-TTY degradation

- Interactive TTY: spinner / progress bar allowed.
- Pipe / CI / scripted calls: no spinner; progress and errors must land on **greppable single-line logs** (`[error] code=...`, `[progress] ...`), never visible only in the interactive UI.
- The same operation gives equivalent feedback in both environments (no information loss, only different presentation).

### 4.3 Error code implementation path (decided: C, exit mapping; final call by dev)

Current state: bingo's thiserror errors are defined scattered across modules (src/api/client.rs, settings.rs, tasks.rs, mcp.rs — 10+ places), the Display copy is the only error information, and there is no unified error-code layer yet. Three implementation paths were considered (C selected):

- **A. Top-level AppError aggregation**: a unified enum absorbs every module's errors, code table centralized — clearest contract, largest change surface.
- **B. Module-level `code()`**: existing enums expose ErrorCode + a centralized code table — keeps module isolation, small change, requires maintaining a cross-module table.
- **C. Exit mapping (selected)**: low-level errors stay as-is; only the CLI/UI exits do one "error → stable code" mapping — minimal intrusion, aligns with AGENTS.md's "default to subtracting"; can evolve to A once requirements mature.

**C's landing guardrails** (devex gates + qa owns acceptance; the contract drifts if any is missing):

1. **Downcast fragility guard**: downcasts based on **matching by type name / string** are forbidden (renames in refactors silently break). C lands as = **every module error implements a lightweight `ErrorCode` trait (returning a `&'static str` code); exits only call it, never branch on types** — compile-time exhaustive, discoverable, not fragile (B's interface + C's intrusion surface). Boxed exits use **`downcast_ref::<$t>()` compile-time type references** (not name matching; refactors fail at compile time), which is an allowed form.
2. **Registration is the contract**: hard rule — a new error path must **explicitly return a stable code for every variant**; variants not yet assigned a stable code **explicitly return `GENERIC`** (debug builds warn; **`_` implicit fallback is forbidden**). `GENERIC` is a **published stable code** (not a temporary value); qa assertions explicitly cover "explicit `GENERIC` path → lands on `GENERIC`". The code table lives in one file, the single lookup point for dev/qa.
3. **Codes only grow: never modified, never reused (semver-style)**: once an error code is published, its meaning can't change and its number can't be reused, or historical logs and existing assertions all break. Single-file code table + append-only additions make review obvious at a glance; the file header comment states the semver rule.
4. **Single exit function + single code-table file** (structural consistency): the mapping logic's single source = each module's `ErrorCode` impl; **the TUI exit goes through `map_error`, the CLI boxed exit through `error_code_boxed` + a macro registry** — both functions only consume ErrorCode impls and don't implement mapping themselves; consistency is guaranteed this way, with dual-exit comparison assertions kept as a fallback. **The macro registry is "registration is the contract" site #2**: types newly implementing ErrorCode must be registered, otherwise the CLI exit silently lands on `GENERIC` (the TUI exit stays correct) — a test asserting that the macro registry covers every ErrorCode-implementing type is required. The code table is centralized in the same file.
5. **Drift-guard unit tests**: **enumerate every variant of every module's error enum**, asserting a mapping to a **non-`GENERIC`** stable code — upgraded from "prevent missed registration" to "CI blocks temporary GENERIC misses"; copy changes don't break, code deletions do. The unit test itself must be maintained as variants are added; accept this cost (it's the CI-period fallback beyond compile time).
   - **GENERIC allowlist** (qa suggestion + dev finalized): `const GENERIC_ALLOWLIST: &[&str]` in `src/error.rs`, entries using locatable paths (e.g. `"tool::bash::Error::NonZeroExit"`); the unit test asserts "any variant not in the allowlist is non-GENERIC".
   - **Temporary annotation**: every allowlist entry must carry a `TODO(generic-allow): <issue>/<date> <reason>` comment, preventing permanent exemption.
   - **Review convention**: new allowlist entries must have a reason (why no stable code is registered yet); entries without a reason are not allowed.

**Landing decision (dev)**: the contract is centralized in **`src/error.rs`**, a single file containing three things:
1. The `ErrorCode` trait: `fn error_code(&self) -> &'static str` (SCREAMING_SNAKE)
2. Explicit `GENERIC` return + debug warning + the shared `map_error` exit function
3. Drift-guard unit tests (enumerate **every variant** of every module's error enum; non-allowlist variants must be non-`GENERIC`; including "explicit `GENERIC` path assertions" and dual-exit comparison, mirroring the qa AC table)

**Implementation integration points (dev confirmed against the code; nothing new invented)**:
- `src/ui.rs` is the renderer-agnostic contract layer (TUI/GUI/test harness share `UiEvent`) — the natural mount point for the "single `map_error` exit" already exists.
- `UiEvent::Error` is currently a plain string, used in only 3 places project-wide (chat.rs 1439 consumes; 2748/2789 produce `e.to_string()`) — upgrading it to `UiEvent::Error { code, msg }` is a minimal change surface and the ideal entry point for the error-code contract; TUI error rows then naturally carry stable codes, and qa can assert them.
- The TUI state-machine reset already has a prototype: `chat.busy` is de facto a turn state machine, and chat.rs already has many pure-logic busy-state tests (no runtime); qa assertions reuse that pattern.

**ErrorCode trait implementation model (compile-time enforcement + explicit GENERIC, dev's corrected version)**:
- Each module implements the trait on its own enum; the match **exhaustively covers every variant with no `_` arm** and returns a stable code — a new variant left unhandled **fails at compile time**, so "compile-time exhaustive" holds.
- Variants "not yet registered" are given an **explicit `GENERIC` return by the developer** (explicit behavior, not silently falling into `_`); explicit `GENERIC` warns via `eprintln!` in debug builds, reminding to register.
- **In release, explicit `GENERIC` semantics are known**: it means "this path has no stable code assigned yet" (error semantics degraded to generic) — not an accidental loss; a known trade-off, recorded honestly, with no promise that compile time catches it.
- **Current explicit `GENERIC` paths = 0**: the `missing_code` warning mechanism is dormant; when future explicit `GENERIC` returns are added (including the **boxed-exit macro-registry miss that lands on `GENERIC`** branch), **`missing_code` must be called to warn** — preventing silence: a loud `eprintln!` warning in debug, known semantics in release; the dormancy can't be documented only and leave no code-side warning tail.
- Exits only call `error_code()`, never branch on concrete types.

This spec constrains only the **exit contract** (CLI/UI output stable error codes); the internal path is decided as C; exit output is consistent. TTY detection uses `std::io::IsTerminal` (zero new dependencies).

### 4.4 Scenario → error code table

dev implementation and qa assertions both reference this table (**registration is the contract**; after implementation, every variant is explicitly assigned a
stable code and `GENERIC_ALLOWLIST` is empty):

| Scenario | Error code | User action |
|---|---|---|
| Read/write timeout (short sync ops); long-turn transport timeout | `TIMEOUT` | Retry later (short-sync = page-level; long turn = flow-level, AC-53) |
| Login expired / key missing / invalid key / 401 | `AUTH_REQUIRED` | Log in again / configure the key |
| No permission / 403 | `PERMISSION_DENIED` | Go back / request permission |
| Rate limited / 429 | `RATE_LIMITED` | Retry later |
| Context window rejected after one compact retry | `CONTEXT_OVERFLOW` | Reduce or clear context |
| Server error / stream protocol / MCP connection failure | `SERVER_ERROR` | Retry later |
| No network (transport-layer error) | `OFFLINE` | Check the network and retry |
| Invalid config (settings / team.json read, write, or validation) | `CONFIG_INVALID` | Fix the config and retry |
| Local storage read/write failure (tasks / transcript / experience) | `STORAGE_ERROR` | Check disk / permissions and retry |
| Tool execution failure | `TOOL_FAILED` | Check the tool output and retry |
| Hook execution failure | `HOOK_FAILED` | Check the hook config |
| The turn's task ended without reporting an outcome (panic inside the spawn) | `TURN_LOST` | Retry the turn or go back (flow-level) |
| Explicit `GENERIC` path | `GENERIC` (no stable code assigned yet) | Follow the copy's guidance |

Code-value semver: once published, only grow, never modify or reuse; a new code = new variant mapping + an added assertion in
the `src/error.rs` drift-guard unit tests (missing either turns CI red).

**Short-sync operation failure = degrade visibly, never swallow silently (policy after #18 lands)**:
- When a short sync operation (`list_models` / `count_tokens`) fails, the TUI emits a page-level `UiEvent::Error` (`level: Page, context: ShortSync`) → an error-colored error line at the end of the content area (the user perceives "what happened + what they can do").
- **Behavior degradation is preserved**: the /model menu still shows known models (usable when the list is empty), /status still shows budget 0 — the error line is a hint; it doesn't block interaction or escalate to a full-screen state.
- Comparison: silently swallowing errors (`unwrap_or_default()` / `unwrap_or(0)` with no perception) violates general principle 1 "feedback states must not depend on the environment"; corrected to "degrade + stay visible".

**Empty long-turn recovery = retry once, then report the silence — never invent it, never call it a transport failure**:
- A completed upstream response with no committed assistant content and no tool calls is an abnormal long-turn result, including a stream that ends with an unclosed thinking/text/tool block. It must not be treated as an ordinary TurnEnd.
- **A turn the output budget cut off is truncation, not emptiness**: `stop_reason: max_tokens` belongs to the max_tokens recovery (the truncated content enters history and a resume prompt continues it), even when thinking is the only block it produced — thinking is not readable output, but a mid-thought cut is not silence either.
- Because this attempt has no committed assistant output or tool side effects, bingo retries it automatically once. The malformed empty assistant is not persisted or fed back to the model.
- If the retry succeeds, the TUI may show a non-fatal warning that the empty response was retried; headless mode emits equivalent stderr feedback.
- **If the retry is also empty, the turn completes as a silent turn** (D124), not as an error: the warning stands, neither attempt is recorded, and a subagent's result reads `[subagent returned no text]`. Silence is a legal turn in the conversation model — a member draining room lines it owes nothing is instructed to end its turn that way, and any session can be woken by an inbox holding nothing it must answer. Failing there restored the inbox and had the same batch redelivered into the same silence once per chase round, under `SERVER_ERROR` copy that named the transport for a decision the model had made.
- The transport's own failure modes are untouched: a stream that dies mid-response is still the `SERVER_ERROR` retry ladder below.

### 4.5 The three frontends, and where feedback lands in each

The core raises feedback once, and each frontend renders it. What follows is
that mapping; nothing here is a fourth answer to what happened.

| The core's fact | TUI | `bingo app-server` | `--print` |
|---|---|---|---|
| a warning from a run | the warning tier above the composer (`WARNING_TTL`) | `feedback/raised` / `feedback/cleared`, with the stable code | `[bingo] warning: …` on stderr |
| an action's notice | the info / output / error tiers, by notice level | `item/completed` carrying the notice, plus the action's own result | not shown: `--print` runs one turn and prints its prose |
| long work in progress | the running-status row, the pinned panel | `operation/started` · `operation/progress` · `operation/completed` | not shown |
| a turn's failure | the full-screen error state, with the code | `turn/completed` with `status: failed` and `error.code` | `[error] code=… msg=…` on stderr, exit 1 |
| a prompt | the approval dialog | `interaction/opened`, answered with `interaction/respond` | one line on stderr, one letter from stdin |

Two consequences worth stating, because they are easy to get wrong:

- **Which tier a terminal chose is not session state.** The console's four tiers
  (output / info / error / warning) and its pinned panels are frontend-local by
  the parity ledger's classification; what is shared is the notice and its
  level, and the feedback and its code. A GUI is free to have three tiers or
  seven.
- **A code survives the trip out.** `--print` reports the code the core computed
  from the error the run actually hit, not a code for "a print run failed" —
  otherwise general principle 1 would hold only inside the TUI.

## 5. DOM / style / ARIA conventions

> This section is the web-frontend (DOM/ARIA) stance; **bingo's current stack is ratatui TUI + headless CLI — no DOM/aria/CSS animation/prefers-reduced-motion/rAF**. The normative values (states, timing, reset) are unchanged; the TUI side implements them per the mapping below.

| Norm item (Web) | bingo TUI mapping |
|---|---|
| `aria-busy` / loading | `chat.busy` + status line (exists) |
| `aria-invalid` red outline | error-line highlight styling |
| `aria-live` write-empty-string update | status-area content update (not row deletion) |
| Focus transfer (async focus after render) | after the error line renders, scroll it into view + highlight |
| `prefers-reduced-motion` | the `motion` setting (`auto` / `off`, or `BINGO_NO_MOTION=1`) is its exact analogue, and covers **every** animated surface through one gate (§1.1). Decoration rests; the indicator itself is never removed, and the two tokens that carry information (`stall`, `settle`) keep reporting |
| requestAnimationFrame | TUI's frame loop is naturally post-render; not an issue |
| Animation durations (150ms/120ms/100ms) | applicable, and stated in milliseconds: the seven tokens' cadences in §1.1 (120ms / 2s / 3s / 120ms / 300ms / 960ms). Durations are converted to frames at the render layer, never authored in frames |

**TUI-side supplement (landed with #18)**: error **level/context is explicitly carried by the producer** (`UiEvent::Error { code, msg, level, context }`; chat.rs knows the trigger path when emitting), the render layer only consumes and never derives — level is not an inherent property of the code (`TIMEOUT` dual-slot, `PERMISSION_DENIED` dual-slot); the render layer and tests share the same event contract; duplicating a "code → level" mapping in the render layer or test side is forbidden.

Web-side conventions (for a future web frontend to reuse):

- **Loading button**: `aria-busy="true"` + disabled; spinner via `<span role="status">`.
- **Toast**: container `aria-live="polite"`; when it contains an action entry, use `role="status"`, not `role="alert"` (non-blocking notification).
- **Field-level error**: error copy attached via `role="alert"` or `aria-describedby` associated with the input; input `aria-invalid="true"`.
- **Page/flow-level error**: `role="alert"`; the retry button reachable and focused.
- **Error code advanced details**: collapsible region hidden by default, expand button semantic (`aria-expanded`).
- **Focus timing**: reset item 4's focus must happen after the error element finishes rendering (async focus after render); skip rather than block on failure.
- **Motion**: spinner loop animation; toast enters with fade-in + slight upward shift (150ms, cubic-bezier ease-out), exits with fade-out (120ms); error blocks complete within 100ms of appearing, no exaggerated displacement. All durations short and disableable via `prefers-reduced-motion`.
- **Reduced-motion boundary**: `prefers-reduced-motion` only disables motion (skeleton shimmer may be disabled); **the loading indicator itself must not be removed** — slow-load perception is a state, not decoration.

## 6. Testability conventions

- **Timing assertions**: timings like 200ms / 3s use **fake timers / virtual time** (ms-level determinism); E2E keeps only smoke tests, never timing assertions (avoids flakiness). On the Rust side use `tokio::time::pause/advance` (tokio already includes the time feature; zero new dependencies); on the Web side use component-level fake timers.
- **Test hooks**: components/commands must expose injectable hooks — injectable delay (to trigger the loading state), injectable failure responses (to trigger each error level) — so each state is stably reproducible.
- Assertions always go by the error code (`[error] code=...`), never by matching copy.

## 7. Acceptance anchors for qa

- **Loading**: 200ms threshold triggers/hides correctly; local button in-place spinner; fullscreen overlay forbidden; **action-granularity de-dup (including Enter/form onSubmit)**.
- **Toast**: 3s auto-dismiss, closable; hover/keyboard-focus pauses and resumes with the remaining time; max 2, oldest evicted only when full; same-kind de-dup replaces + resets the timer.
- **Attention channel**: the byte sequence of each channel is a golden (bell = `\x07` alone; `OSC 9` / three-part `OSC 99` sharing one id per notification / `OSC 777 ; notify`); `auto` resolves over the full `TERM_PROGRAM`/`TERM` matrix and an explicit channel is never second-guessed; under tmux the notification OSC is wrapped and the bell and title are not; `off` emits zero bytes across every trigger, title included. Trigger coverage asserts the state machine, not the terminal: an accepted permission ask notifies (a queued one does not), a turn ≥10s notifies and a fresh one does not, a flow-level error notifies and a page-level one does not. Control characters never survive into a sequence.
- **Motion tokens**: each of the seven is asserted as a pure function of a frame number — `pulse` advances exactly once per 120ms and preserves the glyph order; `beam` sweeps monotonically, stays inside the text and wraps once per 2s; `stall` never flips under 3s, flips on crossing it, and resets on progress; `settle` reports accent for exactly the first 120ms window; `meter` reaches its target within 300ms, monotonically, and re-aims from the displayed value; `title` alternates on 960ms boundaries. The gate is asserted twice over: `motion: "off"` rests every decoration (spinner on `✻`, no sweep, no breathing, static title, numbers snap) **and** leaves `stall`/`settle` reporting. Wiring is asserted at the surfaces: a stalled status row is warning-coloured with the glimmer out and its copy untouched, the busy title writes once per frame change and never over a pending prompt, and the completion row blinks accent while still live and enters scrollback at rest.
- **Error states**: three levels + mixed state each trigger correctly; 401/403/500/offline map to the correct actions; field-level focuses the corresponding input; copy contains "what happened + what the user can do"; state resets after retry.
- **Timeout**: read 10s / write 15s (short sync ops) time out into the corresponding error level; long turns go through the transport layer (120s/60s) + user interruption, escalating to flow-level on failure; **a late error arriving after success is cancelled**; the timeout timer is cancelled on success/failure.
- **Interrupt**: a user interrupt is a persistent record, never a toast. The transcript keeps whatever the model already said and appends the marker the model reads — `[Request interrupted by user]` mid-stream, `[Request interrupted by user for tool use]` when tools were running; a turn that produced nothing records the marker alone. The transcript view renders that marker as a single error-coloured state line (no `❯` bubble, no send time), and a tool row stopped by the interrupt renders amber with the result line `Interrupted` — never the green completion glyph. Assert the marker in history and on screen, and that no transient "interrupted" warning is emitted alongside it.
- **Empty long turn**: an upstream completion with no committed assistant content and no tool calls is never accepted *unreported*; an unclosed in-flight block marks the attempt malformed; bingo retries once without recording that empty attempt, then either completes normally or completes silent — warning shown, nothing recorded, no error code, the drained inbox not restored (D124). A `max_tokens` stop never enters this path at all: thinking-only or not, it is truncation and goes to the resume-prompt recovery.
- **Transient stream error**: a long-turn SSE/API error classified as `429`, `5xx`, overloaded, or `server_error` restarts the whole uncommitted response up to 10 times with server-provided delay first (clamped at 60s), otherwise jittered exponential backoff capped at 32s; the first reconnect notice is suppressed and later notices show `Reconnecting... N/10`. TUI and subagent live views clear failed-attempt output before the replacement stream; headless stdout cannot retract already-written bytes, but persisted/result content contains only the successful attempt. Quota, usage-not-included, invalid-prompt, and context-overflow errors fail immediately. The 10s/15s short-operation tier does not enter this retry loop.
- **State reset**: all four reset actions asserted one by one (aria-busy, aria-invalid, aria-live content, focus transfer); focus happens after rendering completes; stale-response races are ignored.
- **Structured errors**: non-TTY output follows the single-line `[error] code=... msg=...` contract; assertions use error codes, not copy.
- **Accessibility**: toast `aria-live` / error `role="alert"` readable by screen readers; under `prefers-reduced-motion`, motion is disabled but the loading indicator remains.

## Changelog

- v1.88 (2026-08-19): three frontends, one source of feedback (D155). `--print` stopped being a host of its own and became a client of the same core, so a headless warning is the same `feedback/raised` a GUI reads and a headless failure carries the same stable code the console shows — general principle 1 now holds *between* frontends and not only between environments of one. §4.5 is that mapping. Two things it makes explicit: which tier a terminal picked is frontend-local (the shared facts are the notice's level and the feedback's code), and `--print`'s exit code line reports the code the run's own error produced rather than one meaning "a print run failed".
- v1.87 (2026-08-17): one event path, N conversations (D134). **An instance's turn streams onto the console's own channel**, addressed to its conversation, and is written into its store by the handler that writes main's — so its page has result rows, folds, status and stamps because it is the same code, not because a second renderer was taught to agree. Three visible consequences: a **send from a page is echoed at once** rather than showing up as the domain's pending record on the next rebuild; a **reconnect notice from a subagent takes the warning tier** instead of being spliced into that instance's own prose, where it read as something the agent had said; and an **instance's page reports its own context window** in the footer, the surface D89's stranded `on_context_usage` hook had been waiting for. The `stall` baseline is now per-screen: a background instance's deltas no longer reset the clock on main's status row. A room is the one conversation still filled by projection, deliberately — it is a log, not a turn loop — and its page appends the tail past what it has rather than rebuilding.
- v1.86 (2026-08-17): silence is a turn, not a failure (D124, from a live session: four crew turns died reporting `stream protocol error: the model returned no response after the stream ended`). **Two different silences wore one error.** A member woken by room lines that name nobody is *instructed* to end its turn without speaking, and a turn with neither text nor a tool call is exactly what that instruction produces — which the query loop classified as a malformed response, retried once (invisibly: the retry warning is gated on `!quiet` and every subagent is quiet), and then failed under a message naming the transport. The failure restored the drained inbox, the chase redelivered the same batch, and the same silence failed again, once per round. **A second silence now ends the turn**: warning shown, neither attempt recorded, the inbox left drained, the subagent's result reading `[subagent returned no text]` — the sentence the downstream already had for it. Sibling members that happened to write "No reply needed." always passed, which is what made it look transient: it was a coin flip on phrasing, not a network. **`max_tokens` left the empty path entirely** (D73's leftover, standing since August 13): a turn the budget cut off mid-thought has thinking as its only block, read as empty, and was thrown away *before* the recovery that exists for it — it now records its truncated content and continues on the resume prompt, thinking-only included. The transport's own ladder is untouched: a stream that dies mid-response is still `SERVER_ERROR` with reconnects. The channel note carries the matching half — silence belongs in the room, never in the turn text.
- v1.85 (2026-08-17): pages and the roster (D120/D121, the v6 view). **An agent's page is main's page**: entering a conversation turns the screen to it — same renderer, same folds, same gutter, write-once into the terminal's own scrollback; switching is a page turn and coming home reprints a recent tail. The room page is speech only. Esc stops the page's run then comes home (main's turn is out of its reach; ctrl+c overrides); shift+tab cycles the viewed agent's mode and the footer badge follows. **The status layer is the conversation rows** (the user's CC screenshot, verbatim shape): `● main` first, then agents and rooms, three-row window, badges as before — entered by `↓` at the end of prompt history, no chord; `Enter` opens, `k` stops, typing returns to the draft. Retired: the alt-screen zoom, the agent tree, the pills, ctrl+shift+o's preview, and D116's `⚑` flow line (user ruling: the badge in constant view plus one ring is the mention's whole surface).
- v1.84 (2026-08-17): main joins its own team (D117/D118, the v6 wake gate). **Waking is earned, delivery is not**: every room line still lands in every member's inbox in total order, but only a mention (`@name`, `@all`) wakes its holder now — idle members on the spot, running members at their next tool boundary — and unnamed traffic is read in batches: five unread lines, or the oldest sitting 120s (one 15s sweep enforces the age; an empty inbox never wakes, so a quiet room costs no model calls — the user's ruling, verbatim: no new messages, no read). **Main is a member under the same rules** (user ruling): the unconditional room→main relay retired for a per-room pen with the same three releases — mention (the pen drains in room order ahead of the naming line), bulk at five, age on the frame clock and at main's own turn boundary — and the 2s/15s digest debounce now shapes only what the gate has released. The frozen-budget `⚠` was never a relay and still lands at once; DM mail was never penned and is untouched. **Misfired mentions speak**: a token resolving to nobody, or naming a stopped member, comes back named in the sender's tool result — a needs-you-now promise silently unkept would be worse than a bounce. (Header desync repaired: v1.81–v1.83 shipped without bumping the header from v1.80.)
- v1.83 (2026-08-16): the needs-you tier (D116). **A room post naming the user is the one line a room gets**: `⚑ #dev-team @qa: <excerpt>` — flag and room in the accent, author in their colour — stamped like the alert (news at a moment), ringing the D79 attention channel, pushed when the store's mention bit turns on for a member room (the slow poll's badge fingerprint is the edge detector). One turn-on, one line: further mentions land behind the same lit badge until the room is read, reading re-arms it, and the active conversation never flags — the store cannot set mention on what is being looked at. **Waiting on the user is a visible state**: a subagent's pending permission ask (its reason opens `{instance} · `, the roster is the parser) turns its tree row to `waiting on you (permission)` in the accent and its pill to the flag tier, so the one state that cannot wait for the user to come looking is the one the chrome shouts; the ask dialog itself already named its source. Main's own asks mark nobody. This closes the D114–D116 inbox program: the flow is a whitelist, the containers badge, and the whitelist's three tiers — question (`⚑`), failure (`⚠`), approval (the dialog) — are the only interrupts left.
- v1.82 (2026-08-16): the status layer is the summons (D115, user ruling on ctrl+t). **`ctrl+t` narrows to the task panel alone** — "ctrl+t 只和 task 展示有关" — a toggle, with the agent tree keeping its own door (`shift+↑/↓`, opening in selection as before) and the two panels exclusive in both directions. **The member rooms join the tree and the pills**: a room the user is in is a row (`#dev-team: 3 members`) after the instances and a pill after the agent pills; enter on a room row zooms the room; a room the user is not in stays in the ctrl+b dialog, which is the D95 membership rule worn by the switcher. **Every conversation wears a two-tier badge** — the dialog's `Tone::Unread` grammar spread to the rows and the pills: unread is a bare dot in the text colour (activity is a style change, no number), a mention — a room post naming `@user`, per the D99 accounting — is the count in the accent, bold. An agent's unread folds in the mail it sent main (`Chat::agent_mail`, D114's mirror), because the sender's dot is all that remains on screen of an arrival; a pill brightens for a running agent or an unread conversation and keeps bold for the zoomed one. **Entering reads**: the zoom's `set_active` clears the store's badge and the agent's mail dot alike. The slow poll keeps a badge fingerprint and dirties the frame when one moves, so chrome repaints without an event. This is the pull model's bell: D114 closed the flow, and the badge is how a container calls the user in.
- v1.81 (2026-08-16): the inbox turn — the flow's whitelist closes (D114, after the three-survey research pass; conversation-model-v5.md, since superseded and deleted by conversation-model-v6.md). **The transcript renders four things and no more**: the user's messages, main's prose, runs this turn's own `Agent` call dispatched, and things that need the user. Three push-mode leaks closed at once. **Arrival lines retired**: a `SendMessage(to: "main")` draws nothing — the message is main's mail; the arrivals mirror now feeds a per-sender mail count for the status layer instead of a flow line, and the `@name❯` renderer, its parser and its 50-column budget are deleted with their producer. **The `●` notice and the stapled row are dispatch-only**: watch registrations carry a `dispatch` bit, true only for the run an `Agent` call itself asked for (continuations and deliveries are false), and both the streaming turn's tree and the completion notice gate on it — a member woken by a room post mid-turn no longer appears under whatever main happened to be saying. **Perception is not presentation**: main's inbox, its wake, the debounce, `wakes_owner` and the notification queue are byte-identical — every cut is in the view. The `⚠` alert stays unconditional, and `●` streaks still coalesce (D111's batching survives on the rows that remain).

- v1.80 (2026-08-16): the room learns manners, and speakers get names (D112/D113, user rulings). **Speech discipline moved into the prompt**: the channel note's broadcast rule now says a broadcast is owed one *covered* answer, not one answer each — a member who wakes to a greeting a colleague already answered adds a line only if it carries something new — and the serial room's stale bounce flipped its default to **drop**: when what landed while you drafted already covers your message, dropping is the answer, and resend is the exception. The bounce is the coordination point (a racing member cannot see its peers until the room tells it), so the judgment now lives where the information does. **A speaker's run opens with their name**: `@dev` in the identity colour, bold, over the first message of each run — with avatars on the portrait's first cell rides this row (D97's original geometry), with them off the name is the identity, which is the ruling (avatar + name, or name alone). The user's own bubble keeps its `❯` and no label; furniture names nobody; one run, one name.
- v1.79 (2026-08-16): the orchestration verbs fold, and arrivals queue up (D111, user ruling). **`SendMessage` and `Channel` calls join the collapse groups** on the ruling AgentControl already had: check three agents, message one, reseat a room — one streak of coordination, one `⏺` summary row (`Checked 1 subagent, messaged @scout, created 1 room`), expanded rows on click/ctrl+o exactly as before. A lone send names its target, a burst to one target counts itself (`Messaged @scout 3 times`), a spray falls back to the count; `Channel` counts creates, roster changes and list-looks apart, so a created room is never reported as a glance. A target-less or action-less call stays standalone (malformed calls are not folded into anything). **Consecutive arrivals coalesce into one block**: a run of `●` notices and `@name❯` lines renders with no blank row inside it — three agents reporting in are one event to the reader — while the `⚠` alert always opens its own block, because bad news does not queue politely. The streak decision reads only settled text, so blocks render identically on every frame (write-once). The zoomed view folds through the same classifier and inherits all of it.
- v1.78 (2026-08-16): every avatar follows the one switch (D110, user ruling). **`experimental.chatAvatars` now governs all of it**: the conversation gutter (console, zoomed view, room view), the chip fallback, the watch row's portrait, and the face transmissions behind them. Off — the default — is the plain transcript: the message column opens at the left edge, no placeholder cells are emitted, no portrait is claimed for transmission, and both the inline flow and the zoom's alt-screen skip the image sweep. On restores everything D97/D99 built, unchanged, including the D108a pad row under a message shorter than the portrait. Identity **colours** are exempt by definition — the tree, the pills, `@name❯` lines and task owners keep their colours in both states, because a colour is an identity, not an avatar. The ruling reverses D99's "the gutter is not experimental any more", and the pin test that carried that sentence now carries this one.
- v1.77 (2026-08-16): the sender writes its own preview, and the record page closes (D108). **`SendMessage` gains an optional `summary`** — CC's field, 5-10 words, with CC's own description — and the `@name❯` line prefers it, falling back to the message's first fifty columns exactly as before where none was written; the tree's `Ctrl+Shift+O` preview reads it too, so a `SendMessage` row says what was sent rather than `Using SendMessage…`. Both sources go through the same fifty-column cut, so a summary that is not one cannot overrun the row. **The summary never reaches the recipient**: CC carries it in its *teammate* envelope and drops it in its *subagent* one, and v4 replicates the subagent, so `main_mail` is byte-identical and the model reads only what was said to it. The field is left off main's own schema, where nothing would draw it, while the parser still accepts it at any depth. **The D96/D100 observation page retires as a surface**, which is v4's table finally emptied: `tab` in the background dialog's agent detail is gone and its footer no longer names it, the two-level page and its lane grouping are deleted, and what survives is the attribution walk underneath — the zoomed view is the live record and there is no read-only second copy of it. No other behaviour changed.
- v1.76 (2026-08-16): the background dialog (D107). **One modal now answers everything that is not the conversation on screen.** `Ctrl+B` still backgrounds a shell command running in the foreground; with none running it opens the **background dialog** — `Background tasks`, the running counts under it (`2 agents · 1 active shell`), then `Agents`, `Shells` and `Rooms`, each heading appearing only where there is another kind to tell it apart from, and `No tasks currently running` when there is nothing at all. A row is `@scout: reading src/lib.rs…` in the instance's identity colour, `$ cargo build (running)` for a shell — the chip green on `done`, red on `error`, amber on `stopped` — and `#build: 12 messages · main, scout` for a room, with `you're not in` where the user is not a member. The bottom line is `↑/↓ to select · Enter to view · f to foreground · x to stop · ←/Esc to close`, and `f` and `x` appear only where the row can be asked to do them. **The cursor is on a row, not on a position**: the list orders running work first and then whatever moved most recently, and the cursor follows its row when the order changes under it, so `x` can never stop what slid into the cursor's place. **This is where the numbers finally are.** The unread counts and mention flags the accounting store has kept since D99 render here — `(3 unread)`, in the accent where the conversation said your name — and foregrounding a row reads it through the same door the zoom uses, so the badge clears itself. **`f` is the room zoom's first door**: the view D105 built with no way in is reached from the Rooms section, by the same key and the same verb an agent uses. **`Enter` opens a detail** that replaces the list — the instance's activity, cost, progress and prompt; a shell's status, runtime, command and output tail; a room's members, count and last words — with `← to go back · Esc/Enter/Space to close` under it, plus `x to stop`, `f to foreground` and `tab to open the record` where they apply. **Two surfaces retire into it**: the D95 team directory (its roster is the Agents section, its rooms are the Rooms section, its `j` gives way to `/join`) and the D80 agent manager. **The lifecycle feed retires outright** — nothing renders it any more, `/team` has answered on the info tier since D104, and a run's start and end are the flow's own rows. **Named limits**: a background shell cannot be stopped (bingo keeps no handle on a promoted command's child), so `x` there says so on the warning tier instead of pretending; a running shell's detail shows no output, because only the completion payload is kept; `@main` has no row, since it is neither stoppable nor foregroundable and the tree already leads with it; each section is windowed at eight rows; and the observation page is still reachable by `tab`, which v4 retires and D108 sweeps.
- v1.75 (2026-08-16): the transcript's tiers (D106). **The flow shows agent life at four levels and no more.** A dispatch is `◉ @scout: fix the parser` — the instance's name and its task, which is the shape the agent tree already speaks — with the last **three** things it did hanging under the connector while it works (`⏺ Read(src/lexer.rs)`), condensing to one `In progress… · 4 tool uses · 8.3k tokens` line when the window is too short to hold them, and settling into `Done (12 tool uses · 8.3k tokens · 1m 4s)`. **Only the settled form is ever printed into scrollback**: a message holding a running dispatch cannot settle, so the progress rows live in the redrawn tail by construction rather than by discipline. A round that dispatched several agents draws **one** block — `⏺ Running 2 agents…` over a stem of `├─ @scout: … · 1 tool use · 2.1k tokens` rows, each with its current activity or the single word `Done` — and opening any of them takes the group apart. **A completion leaves one dim `● @scout completed · fix the parser`** where the task notification reaches the main agent's context, and only when the notification is really the main agent's; a failure keeps its `⚠` alert and gets no second line, and a cancellation still draws nothing. **A message an agent sends the main agent is one visible line again**: `@scout❯ <first fifty columns>` in the sender's identity colour, with a send stamp, no avatar face, and its whole body reachable only through the `ctrl+o` transcript — v3 rendered nothing here and let the woken turn speak for it. The delivery underneath is byte-identical: same inbox, same marker, same 2s/15s debounce. **Room relays still render nothing**, deliberately. **State changes never write a flow line** — that is what the tree and the pills are for. **Named limits**: both new lines are recognised by their text, so a user who types `● …` or `@name❯ …` gets the rendering; the teammate line's summary is the message's first fifty columns, because bingo's `SendMessage` has no summary field to prefer.
- v1.74 (2026-08-16): resume-if-stopped lands (D105 review fixup). **A direct message to a stopped instance resumes it** — `AgentRegistry::deliver` flips the instance back to idle and the delivery flush respawns its run from the kept session and history, which closes the gap v1.73 named as a limit. The resume goes through the one delivery door only: a follow-up chase pushes without flipping state and a room broadcast still skips stopped members, so nothing automatic undoes a stop the user asked for. The zoomed view's send to a stopped agent therefore succeeds silently (the echo is the receipt); the warning tier is reserved for what still refuses — a deleted name, a room that will not have you.
- v1.73 (2026-08-16): the zoomed view (D105). **The screen can be one agent's conversation for a while, and go back to being the transcript with nothing changed.** `Enter` on a selected tree row — or in the `Ctrl+B` manager's detail — swaps to an alternate screen headed `Viewing @scout · esc to return` with the task the instance was dispatched with under it, and `Esc` swaps back. The body is the agent's **whole record**: the task, main's instructions, the user's own messages, room relays, reminders, its work folded the way `@main`'s is (`⏺ Searched for 1 pattern, read 2 files`), its answers, and the run streaming right now whoever started it — one projection (`perspective::walk` with the protagonist's attribution), one row renderer and one avatar gutter, so a message looks like itself here and on the observation page alike. Intake takes the furniture tier: a spawn prompt nobody wrote does not arrive wearing somebody's face. **The composer stays live and addresses the agent**, in its identity colour, through the same delivery the transcript's `@name` line uses; the echo is the receipt, so nothing is printed to say a message was sent, and a *refusal* — a stopped instance, a room that will not have you — speaks on the warning tier. A `/` line is a message and a `!` line is a message: the console's parser never sees this draft, which is CC's own construction. **`Esc` has three meanings in one ladder** — the tree's cursor, then the running turn (aborted; history kept, view stays), then the view — and the hint row says which one is armed. `Shift+Tab` cycles the **viewed agent's** permission mode and leaves the console's alone; `Shift+↑/↓` walks the roster with the view following, or the tree when one is open. A view closes itself when its agent leaves the roster and stays open when the agent merely finishes. **The transcript underneath is untouched**: the loop writes only the draft, the tree cursor, the zoom pointer, the accounting store's active conversation and the frame counter — never a message, never the flush cursor, never a drained channel — so a round trip rebuilds the document byte for byte and a message that landed during the zoom prints once, in order, afterwards. **Named limits**: a stopped instance refuses messages (`AgentRegistry::deliver`), so v4's resume-if-stopped semantics are not implemented and the view says so instead of swallowing it; `Ctrl+O`/`Ctrl+B`/`Ctrl+T`/`Ctrl+G`/`Ctrl+R` are inert while a view is open, because the surfaces they open cannot be shown over it; a **room** has the same view and no door until the background dialog opens one.
- v1.72 (2026-08-16): the status layer (D104). **The screen answers "who is working" without being asked a second question.** `ctrl+t` is one key with three stops — the task area, the **agent tree**, then closed — collapsing to a two-stop toggle when nobody is on the roster; the team directory left the cycle and waits for the background dialog. The tree is chrome, rebuilt from the agent registry every frame and never written into scrollback: `@main` first (its state is the host turn — `: Idle`, or `: <verb>…` and its token count while a turn runs), then one row per instance in name order, `┌─ ├─ └─` drawing the stem. **A row says what an instance is doing and what that has cost**: `@scout: reading src/lib.rs… · 12 tool uses · 8.3k tokens` running, `Idle for 14s` waiting, `[stopped]` once stopped — and the stats segment is empty while a run has produced nothing, because bingo's progress is per-run and `0 tool uses · 0 tokens` on every idle row is noise. Names wear their identity colour, main's reserved slot included. **Selection is explicit state and plain typing is never stolen.** `shift+↑/↓` with the tree closed opens it and parks on `@main`; with it open the cursor walks `@main → instances → hide` and wraps at both ends, and the `hide` row appears only while the cursor is in the tree. Only a *selected instance row* makes `k` mean stop — through the `ctrl+b` manager's own path, so there is one warning and one watch transition, and no confirmation, which is the upstream ruling. `@main` has no `k`; a stopped instance's `k` does nothing. `esc` peels twice: the cursor first, the panel second. **`enter` is unbound**, so no row promises it — the upstream ` · enter to view` and ` · enter to collapse` are the two strings deliberately left off until the zoomed view lands. `ctrl+shift+o` hangs three condensed lines of an instance's *own conversation* off its row, under the stem; terminals without the kitty keyboard protocol cannot tell the chord from `ctrl+o` and get the transcript instead. **With the tree closed and anybody on the roster, the last row of the window is the pills**: `@main @scout @writer · shift + ↓ to expand` — main first and never sorted, running instances ahead of resting ones, identity colours, dim where an instance is idle or stopped, `→` where the window is too narrow to name them all. Pills and tree are exclusive: the tree says everything a pill would. **No badge on either**, and that is deliberate — the accounting store keeps counting unread and mentions for the surface that wants numbers, which is the background dialog, not this one. **The task panel gained two display-only marks**: ` (@scout)` in the owner's colour when the owner is still on the roster (an owner who has been stopped or never existed is not named), and ` › blocked by #3` listing only the blockers that are not done, with the blocked row dimmed. Neither assigns, claims nor unblocks anything. **Named interim cost:** the team directory has no door at all until the background dialog opens one, so `/team` now prints its answer on the info tier instead of pointing at a key.

- v1.71 (2026-08-16): one transcript, and a composer line that can leave it (D103). **The terminal shows exactly one conversation: the user's with main.** D89 made every conversation a place the terminal could be pointed at and D90 gave the places a bar; both retire, with them the `/open` command, the `ctrl+k` switcher, the dividers a switch printed, the empty-pair note, the `tab` door out of a conversation and the delivery receipt that used to settle into the flow. Nothing splices, nothing replays, and the flow is `Chat::messages` in order — so a reader who scrolls back sees one thread instead of a braid of visits. **`ctrl+k` is readline's kill again** (`alt+k` stays as its alias). **The one grammar that still reaches somebody else is the composer's.** `@scout fix the lexer too` delivers to scout's inbox as the user and `#build tests are green` posts to the room — joining first, with the room's own membership line, if the user is not a member — while the model sees neither. Success is a **transient** `Sent to @scout` on the info tier: never a flow line, never in main's history, gone at the next keystroke. A name that resolves to nothing is **prose**, submitted verbatim as an ordinary turn, so `@utils explain this` is a question rather than a failed delivery. **The typeahead says what Enter will do.** At the start of a line `@` lists every instance the send can reach — stopped ones included, because a message resumes one — as `@scout · send message · running`, with project files still under them; `#` lists the rooms as `#build · post to room`, and `· joins you` where the user is not a member. Mid-line the sigils are unchanged: `@` is D85's file-and-agent reference, `#` is a hash in a sentence. **The silence contract is gone** (see §2.2): a woken turn renders its prose like any other turn. **Named interim cost:** between this batch and the zoomed view there is no surface that shows an agent's conversation — the `ctrl+b` detail's `tab` and the directory's `o` still open its record, and `Enter` in both is unbound rather than given a meaning that would have to be taken back.

- v1.70 (2026-08-16): the silence contract (D102). **A turn the main agent was *woken* into has two endings and no third.** Since D98 a dispatch finishing, an agent's message or a room going quiet wakes main into a turn nobody typed into — a *digest* — and until now every one of those turns ended in prose, so a completion the screen had already reported bought a paragraph restating it. The turn now ends either by **saying the thing the user needs to know or decide** — a question, a blocker, a result that changes what they should do next, anything they asked to be told about — which renders in `@main` as main speaking and counts on D99's unread exactly as before; or by replying with **exactly `[[quiet]]`**, which renders as **nothing at all**: no message block in the flow, no unread bump, no mention accent, no completion bell, and no touch of the conversation's activity clock. The floor under it is unchanged and always visible — the dispatch row's own state and its one-line result, the `@main` badge, the failure alert, the chase machinery. **The record keeps what the flow drops.** The marker is written to the transcript verbatim like any other reply, and on main's observation page it reads as a dim timeline note, in nobody's lane — scaffolding, filed where every other piece of scaffolding is filed. Only the printed flow is quiet. **In a turn the user typed into, the marker renders literally.** There it is the model misfiring, and a misfire the renderer swallows is a bug nobody can see; this is also why the marker is bracketed rather than tag-shaped — a bare `<quiet/>` on its own line parses as an HTML block and renders to zero rows, which is the silent data loss the rule exists to prevent. **The two facts are told apart by a stamp, not by a coincidence.** A digest is marked at `submit_auto`, the one door a woken turn comes through, and the reply carries the mark; an ordinary turn submitted with an empty prompt is not a digest, and the marker in it stays on screen. **Nothing of a quiet turn reaches scrollback, not even for a frame** — including the work it did before going quiet, which renders as nothing along with the text, because the calls are already in the record and the dispatch row already said the run was done. **The contract is main's alone**: it is a system block of the main session, dropped from what a subagent inherits, because nothing wakes a subagent with a notification and an instance taught to disappear could disappear its own report.

- v1.69 (2026-08-16): the console is called `@main` (D101). **The bar, the rules and the grammar say `@main`.** The floor of the terminal was labelled `hub` — a word for a thing that no longer exists, since D94 took the bus out and D98–D100 made main a participant like any other. It is now spelled the way every participant is spelled: the bar's first entry reads `@main`, the rule that hands the flow back reads `── @main ──`, `Ctrl+K` pins `@main` on top, the `?` panel says `back to @main` and `open a conversation: @agent · #room · @main`, and the composer's standing hint inside a conversation reads `esc to @main`. Nothing about the behaviour moved: home is still first in the bar, still unclosable, still where turns run and where Esc goes — "home" survives as a *property* of `@main` rather than as a second name for it. **`/open` accepts `@main` and a bare `main`, and no longer accepts `hub`.** The sigil grammar is unchanged (accepted, not required, `#main` still reads as a room), and the retired word is gone from the completion dropdown as well as from the parser — a spelling kept alive only for old muscle memory would have been the second name the rename existed to remove. `/open hub` now answers `no conversation called hub`. **The word goes out of what the model reads too**: the subagent note, the room note, the crew and hire notes, the ack-chase follow-up line and every tool description say `main`, so the address language in the prompt is the address language in the bar. `hub` survives in exactly two places, both on purpose: **`hub-and-spoke`**, which names a *topology* and not a participant (the design doc itself still uses it), and the internal `BufferId::Hub` variant, whose doc comment now reads "the home conversation (`@main`)" — the design doc's explicit ruling, because that buffer's mechanics genuinely differ from every other conversation's and a name that says so is worth more than a name that matches its label. **Nothing on the wire changed**, because nothing on the wire ever said it: the JSON-events protocol and the share document carry participant names, and main's name has been `"main"` since channels.rs was written.

- v1.68 (2026-08-16): the record's doors (D100). **The observation page belongs to every participant, main included.** Main's page is built from the **session transcript** — the same store every turn loads its context from — and the attribution default *flips* with the protagonist: in a subagent's record unmarked user-role prose is main speaking (it is the one sender the delivery path leaves unmarked), in main's own record it is the human at the keyboard, and there is no spawn prompt, so the first-message-is-intake rule does not apply. Every already-recognised shape keeps its home: the `<messages>` and legacy `<channel-messages>` envelopes unwrap, `[message from @X]` mail lands in its sender's lane, `[#room msg #N]` relays stay timeline-only because the room's log is authoritative, task notifications and the task reminder are intake, and runtime scaffolding reaches the timeline and nowhere else. So main's page reads: its `@user` lane (the console conversation), one lane per agent that wrote to it or that it answered, its rooms, its intake, its timeline. **The clock is the turn marker.** A subagent's history carries a stamp per message; a transcript carries only the D91 turn marker, so that marker's wall clock is carried across the messages recorded inside its turn, and a transcript written before those markers reads zero throughout — the documented "no clock" a compacted agent's page already degrades under (lanes sort by a zero, the index prints no time). **Three doors, one page.** `tab` on an **empty composer** inside a conversation opens that conversation's record — the agent's page in `@agent`, main's in the console, nothing in a `#room` because a room has no single protagonist; `o` on a member row in the `Ctrl+T` directory opens that member's; and `tab` in the `Ctrl+B` detail is unchanged. With text in the composer `tab` is still completion (the slash and `@` dropdowns are judged well above the door), and the door is inert behind a permission ask, like every other full-screen surface. **Main joins the directory roster**, first: presence is the host turn (`●` running / `○` idle), the label is `main · console · idle`, its rooms list like anyone's, and Enter on it goes to the console rather than to a DM with an instance that does not exist. It gains no manage verbs — the directory has none, and main is not stoppable. **The empty DM gets a door.** D99's honest consequence was that an agent main spawned and the user never wrote to has an empty `@agent` view; it now opens with its rule plus one dim `· no conversation yet · tab opens @scout's record ·`. The note is furniture rather than replay — it is not counted in the poll's cursor, so the first real message still prints — and it is not repeated while the flow already shows it as the last thing that conversation printed.

- v1.67 (2026-08-16): the pure pair, and avatars everywhere (D99). **`@agent` renders the user's lane and nothing else.** The DM used to show an agent's whole context flat — the task it was spawned with, main's instructions, room relays, mail from other agents, chases and the task reminder, interleaved with the user's own conversation as dim scaffolding notes. All of it leaves: what renders is what the user said, what the agent answered *them*, and the work it did for those turns, and the rest is read on the observation page, where it always belonged. The lane is derived by the same attribution walk the page uses — one parser (`buffer::line_source`), one walk (`perspective::walk`), two readers — so the pair view and the page cannot disagree about who said what. Reply attribution follows the page's rule: a turn attaches to the counterpart it last heard from, so a reply main drew stays out and a reply the user drew stays in. **An agent's work renders the way main's does.** Its tool calls and reasoning come back as **activities on the agent's own message**, folded by the console's own collapse machinery into `⏺ Searched for 4 patterns, read 2 files (ctrl+o to expand)`, instead of N flat `⏺ Tool(…)` lines. A run's prose and work are one message, and a run ends wherever anything at all stood between two of the agent's rows in the full record — which is also what keeps the replay append-only, since every continuation is triggered by something and that something is a break. The tool *output* is not in reach: the record kept the call, the result went to the model, so expanding a replayed group shows the calls it folded and no output rows. **The live tail belongs to your run.** A run triggered by main, by a room or by a chase shows no stream and no `is replying…` row in the DM — no reply is owed to the person reading it — and a message main sent that is in flight or queued is no longer drawn as the user's own bubble. **The avatar gutter comes to @main.** The console was the one conversation without one, on the argument that its grammar is Claude Code's; the better reading is that main is a participant like any other. A row that nobody said — the failure alert, a route or ask receipt, the interrupt marker, a rewind line — takes the gutter's *indentation* and no face, and leaves the run, so main speaking after an alert re-leads with its own face; a steered `↪` line is the user's words and keeps the user's. The user's rows wear exactly what they wear in a DM, main wears a **reserved portrait** — taken out of the pinnable vocabulary and out of the name hash, so no teammate can be dealt it or pin it and the console looks the same every session — and the two skins still differ in the gutter and nowhere else. The `experimental.chatAvatars` **sender band retires with it**: it existed because the console had no gutter, and a band over a gutter drew the same speaker twice. The switch now governs one thing, whether a subagent's watch row wears that agent's portrait instead of `◉`. **Honest accounting.** A DM's badge counts the pair lane's *messages* rather than the history's length, so a turn that made forty tool calls counts as one reply; it wears the mention accent only when the agent actually answered you, instead of on every change, which was badge blindness by construction. **@main gets the real unread D98 promised**: main's own prose counts while the reader is elsewhere, a `⚠ @name · …` failure alert counts and wants you, and entering the console clears both. A room mention is matched case-insensitively with word boundaries, so `@User` and `@USER,` reach the person and `@username` does not. **The replay budget drops 30 → 8**: the record keeps the whole conversation and the flow keeps the scrollback of every visit, so a switch owes the reader the last exchange rather than a screenful of history above the composer. **Named limits**: an agent spawned by main and never spoken to has an empty `@agent` view until the user says something, which is the model's own answer (its report is main's news); a collapse group cannot span two of the agent's runs, because a run boundary is a message boundary here; and a replayed group has no output to expand.

- v1.66 (2026-08-16): the console goes quiet, and there is one verb for speaking (D98). **`SendMessage` is now the only speech tool.** `Post` and `notify_user` retired into it, and `to` is the conversation namespace the interface already uses — an instance name or `@name` for an agent, `#name` for a room — so the tool layer and the bar share one address language. Hub-and-spoke is kept by **addressing** rather than by withholding the tool: the main agent may write to any instance and any room it is in, a subagent to `main` and the rooms it is a member of, and anything else is refused in words that name what it *may* address. Room delivery is the retired `Post`'s machinery, byte-identical; the ack watchdog stays on agent targets; `urgent: true` is legal only on a subagent→main send and is refused elsewhere, because there is nobody on the other end of a bell rung at a room. **A subagent reaching main renders nothing.** The message lands in the main agent's inbox — the same store room relays already used, so there is one drain and one injection — under a marker that names the sender (`[message from @scout]`, the `[DM from user]` shape with the one thing that marker never needed to carry), and wakes an idle main. What the user sees is what main then says: it is an async command finishing. `urgent` rings the attention channel on arrival and nothing more. **Bad news is the one direct line.** A run reaching `Failed` draws `⚠ @scout · subagent failed: …` in the error tier and rings; `Done` and `Cancelled` draw nothing, because the dispatch row's own state already says so. The rule is asymmetric on purpose: good news can wait for the main agent to narrate it, a crash cannot, since the turn that would have narrated it may never run. **A run the user started stops waking main.** Every terminal state used to fire `submit_auto` trigger-blind, so the user's own DM exchange with an agent woke the main agent to digest it — the D63 privacy line drawn everywhere except on the wake path. A run is now registered with the question already answered: a batch that is *entirely* user-origin enqueues no notification for its owner, so nothing wakes and nothing is injected; a dispatch, a continuation, a room relay, or any batch mixing one of those in behaves exactly as before. The lifecycle feed still takes every run — the directory is a record, and records do not get scoped. **Room digestion is debounced.** One post used to buy one woken turn, so three agents talking for a minute bought three digests of a conversation that had not finished happening. The wake now waits for a 2s quiet window, restarted by every arrival, under a 15s ceiling so a room that never goes quiet cannot starve it; both former wake sites (the channel watch event and TurnEnd) route through this one, so they cannot disagree about when. **Named limits**: @main loses the unread count D94 gave it, because that counter existed only to count relays — D99 gives @main a real one; an alert line therefore raises no badge this batch. The observation page can now attribute a direct message to its sender, but main's own page does not exist until D100.

- v1.65 (2026-08-15): three presentation debts of the conversation model, paid together (D97). **Avatars come back as a gutter.** D89 retired the workspace skin and its portrait column with it; the face returns where it always belonged — beside the message, in DM, room and perspective views, drawn by the row builders rather than by a second renderer. The gutter is `avatar::gutter_width()` wide (five cells where the terminal places kitty images, four for the initial-on-colour chip that tmux and the other placement-less terminals get, per the D42 gap) and it is taken out of the width **before** anything wraps, so a CJK body still fits the terminal instead of overrunning it by exactly the gutter. The portrait is two cells tall and rides the sender's name row plus the first body row under it — rows the layout was already spending. **One face per run** (Slack's convention): consecutive messages from the same sender show the portrait once, and a work step or a runtime note does not break the run, because a tool call sits inside its own turn. Continuation rows, process rows and system/membership lines take the indentation and no face: the message column stays one straight edge, and only somebody who spoke gets a picture. **The hub keeps no gutter at all** — its grammar is Claude Code's two speakers and `⏺` markers, and a portrait column beside it would be a second convention in the same window. The indentation is an `El::Gutter` wrapper, so the row count is unchanged and every click range and the caret keep the offsets the render walk computed. **The conversation bar moves to the bottom row.** It used to sit directly on the composer on the argument that it is *about* the composer; it reads better as this window's status area — where you are, what is unread — and a status area belongs at the bottom edge, which is where the eye already goes for it. Order inside the tail region is now flow tail → hints → composer box → footer → **bar**. Content and overflow are unchanged (D90/D95: only the conversations the user is in, and no row at all when there is only one). **Content images can be opened.** A session-level registry records every picture that *renders* — pasted from the clipboard, attached by path, produced by a tool, loaded from a markdown URL an agent or the model wrote — newest first, each with its source label, its clock, its byte size and its format. Avatars are chrome and never register: that rule holds at the tee, not in a reader's head. The registry reads the session's own data and is therefore untouched by the D93 vision projection, which is a view taken at the send seam. Three doors, all reaching one action: **click** an image row (or the `#[image N]` marker in a bubble) in the fullscreen host; **`/images`** opens a picker listing `source · stamp · size`, ↑↓/1-9 to browse, Enter to open, Esc to close — it reuses the existing `Menu` slot, so it costs no new `EscLayer`; and **`o`** in the `Ctrl+O` transcript opens the picture in view (a pager has no cursor, so "the image row" is the first one on screen). The open writes the file only if the image existed solely in memory — an image already on disk is opened where it lives, never copied — and spawns the desktop's handler **detached** (`open` / `xdg-open` / `cmd /c start`), so the TUI never waits behind somebody's viewer. A failure is one line on the info tier: `could not open image: <err>`. Temp files live in a pid-tagged directory, bounded at 100 entries and 50 MB, evicted oldest-first, and eviction removes a file by the exact path it wrote and never a directory.

- v1.64 (2026-08-14): an agent gets a page of its own, and it is the audit layer (D96). **The perspective page** is a read-only, two-level dossier over one agent's communications, opened with `tab` on that agent in the `Ctrl+B` manager's detail. It is a self-driving alt-screen modal on the D82 transcript's pattern — it owns every key while it is up and therefore takes **no `EscLayer` slot**, the same call the transcript makes — and it reuses the transcript's pager outright, so `j`/`k`, `g`/`G`, `/` search and `n`/`N` mean there what they mean under `Ctrl+O`. **Two levels, one loop, one snapshot.** The **index** (`── @scout · perspective · read-only ──`) groups the agent's threads under headings — a merged `timeline`, `direct messages` one row per counterpart, `rooms`, and `intake` — each row carrying a message count and the clock of its last activity, flush right in the muted tier and dropped rather than wrapped where the row is too narrow (D93: content wins). The cursor walks **lanes, not rows**, so it can never land on a group heading (the D95 directory's rule). Enter opens a thread, Esc returns to the index, Esc again closes the page, and `q` closes it from either depth — except while the search input is open, which owns every key, because there `q` is a letter and Esc cancels the search. Empty lanes are never listed: a row reading `#parser (0)` is furniture. **The protagonist rule is the display contract**: in every thread on X's page, X's own process — its collapsed `✻ Thinking` header and its `⏺ Tool(…)` call lines — is shown, whoever the counterpart is, through the same `Post` renderer the DM tail uses (split out as `settled_post_rows`, so there is still exactly one renderer). Process rows are *work*, not messages, so the index's counts do not include them. A **room** thread is the room's whole log with the agent's own rows marked, not its monologue — a thread that showed one voice would not be a thread. `ctrl+e` is deliberately **unbound**: it toggles fold state that lives on the hub's messages, and a thread's rows carry a tool call as one collapsed line with no output behind it, so there is no second state to show. **Snapshot, not a feed**: the page is built when it opens and does not move while it is read; reopening is the refresh. **The privacy stance lands here.** This is the one surface on which an agent's conversations with somebody other than the user are visible — the user's own `@X` pair view is untouched and still mixes nothing in, which is asserted by leaving every existing `dm_posts` test alone. **What attribution can and cannot recover** is the batch's real finding: `absorb_inbox` renders an inbox item into one flat prompt string and the sender's name does not survive it, so a counterpart is recovered from literal markers alone — `[DM from user]` and the steer block are the user, `[follow-up instruction] …` and unmarked prose are the hub (it is the one sender left unmarked), `[#room msg #N] who: …` is a relay recorded in the timeline only because the room's own log is the authoritative copy, and `[follow-up N/M]`, the task reminder, `<task-notifications>` and the first message (the task that created the agent) are intake. Scaffolding nobody wrote — interrupt markers, the compaction summary, the stop-hook re-inject, the max-tokens resume — is recognised **so that it cannot fall through to the unmarked-prose rule and be misfiled as the hub speaking**, and lands in the timeline and nowhere else. The markers are parsed in exactly one place (`buffer::line_source`), which the DM view and the page both read, so the shapes can never be recognised two ways. **Named limits**: agent↔agent *direct* messages do not exist in the domain today — every production caller of `deliver` passes `main` or `user` and `SendMessage` is depth-0 only — so a page today has at most two counterpart lanes, and agents reach each other through rooms; where interleaving makes exact reply-attribution impossible the agent's turns attach to the counterpart it last heard from, which is best-effort by construction and the reason the timeline is the lane that is complete; a room lane is the room's log and is therefore *not* contained in the timeline, which is complete about the agent rather than about rooms; and a compaction clears an instance's stamps outright, so a compacted agent's lanes sort by a clock that reads zero.
- v1.63 (2026-08-14): rooms become first-class and the team stops pretending to be a conversation (D95). **A room is the only group primitive, and its roster is an arbitrary subset of the team.** The domain used to seat `main` and `user` in every room it created, which made every room the user's room by construction — the one thing the model says a room is not. Nobody is seated behind the caller's back now: the tool layer stamps the *creator* into the roster it creates, everyone else has to be named (`user` and `main` included), and direct sub-agents get the `Channel` tool too, so two members who need to work something out can form a room without the top of the tree convening it. Blueprint rooms (`.bingo/team.json`) name `main` and `user` explicitly, so declared rooms behave exactly as before. **Joining and leaving are events, not settings**: they land in the room's own log with a sequence number, render as dim `· coder joined · 14:32` lines every member sees, and are deliberately *not* delivered into anybody's context — waking N agents because a roster changed is the flooding D94 removed — nor counted by the serial commit check, so a join can never bounce a post that was already being drafted. The share document gained a defaulted `kind` field, so a pre-D95 document still reads as all speech. **The bar means "conversations I am in".** A room the user is a member of is listed with its unread; a room they are not in is absent from the bar, from `Ctrl+K` and from `/open` — and, unlike a DM, is *removed* from the registry when they leave, because a stopped agent's DM is still your conversation while somebody else's room never was. **Observing is the new state.** From the directory, a room the user is not in opens read-only: the same flow, the same renderer, framed by its own rule — `── #parser · observer · read-only ──` — with a standing `read-only · /join to speak in this room` under the composer and the identical sentence as the refusal if they submit anyway (one wording, so the answer to "why can't I type here" does not depend on whether you tried). The composer stays *live* rather than being disabled, so `/help`, `Ctrl+K` and Esc mean what they mean everywhere; only Enter is refused. `/join` — or `j` on the room in the directory — is the way in, and it is announced, which is what makes free reading defensible: there is no lurk-then-speak. `/leave` is the counterpart. **The `#team` board retires.** `BufferId::Team` is gone: the team is the organization, you cannot say anything to it, and a read-only buffer with an unread badge was a conversation-shaped hole where a roster belonged. Its bounded 200-event log survives untouched as the **team directory's feed** — second stop of the `Ctrl+T` cycle (tasks → team → closed, Esc closes one stop), showing the roster with presence and each member's rooms, every room with its members and a `you're not in` mark, and the last ten lifecycle entries newest-first. The D93 crew gate died with it: a gate existed because a badge asked to be read, and a column in a panel the user opens asks for nothing, so a solo hire's lifecycle is simply in the feed. `/team` output still goes to the feed and the hub keeps one pointer line, now `→ team (ctrl+t)` — naming the key rather than a buffer to switch to. **Scoped out on purpose**: the directory navigates and informs only. Enter opens a DM or a room, `j` joins; stopping an agent stays in the `Ctrl+B` manager, which owns that verb and its warning, and a second surface that could stop an agent would be a second place for "stop" to mean something slightly different. **Named limits**: the observer frame is the rule and the composer hint rather than a drawn border, because scrollback is written once and a box around a growing flow cannot be; while observing, the bar shows no active entry, since the room is by definition not one of the user's conversations; and the join key is `j` *in the directory* plus `/join` anywhere — not a bare `J` in a live composer, which would have to be either a letter or an action, and not `ctrl+j`, which is Enter on many terminals.
- v1.62 (2026-08-14): the hub stops being the message bus, and agents get one deliberate way in (D94). Every agent lifecycle event in the session used to write into the user's conversation with the main agent: when the watch handler could not find an existing row for a label it built one and hung it off **the last assistant message it could find**, so a background hire finishing forty seconds after its turn ended, a continuation run opening under a new label, and an ack watchdog reporting a chase all appended `◉ name · task` + `⎿ done` under a reply that had nothing to do with them. **A lifecycle event now writes into the hub only while a turn is running.** The discriminator is *when*, not *what*, and deliberately so: `Agent` renders no tool row of its own, so that watch row **is** the row for the Task call the user just watched the model make — deleting it outright would delete the hub turn's only evidence of its own tool use. With no turn running the same event draws nothing, and the signal it carried is carried instead by the conversation bar's presence and unread, the bounded lifecycle log (which D95 renders as the team directory), and the agent's own DM — whose badge already followed the instance's history, verified rather than assumed, and now pinned end to end by a test. Background shell commands and channel rows keep the walk-back they always had: a command is the hub's own tool. **What the model sees is untouched** — the auto turn that carries `<task-notifications>` into the main agent's context still fires on every terminal state, and the D64 markers, D63 privacy and ack-retry contracts are byte-identical; this is a change to what the *user* sees. **`notify_user` is the road that replaces the flood**: one tool, subagents only, because the main agent already holds the hub and reaches the user by answering. It renders one dim, stamped line — `🔔 @scout → you: the migration finished` — and is rate limited to one line per agent per minute, with extras counted and reported once as `🔔 @scout: 3 more — see the DM` when the window rolls. Nothing is lost by counting: the text was written as a tool call, so it is in that agent's transcript and reaches the user through its DM either way — the window protects the hub's scarcity, not the information, and the tool says so to the caller (`delivered` / `queued`, the latter naming where the text still is and telling it not to resend). `level: "urgent"` additionally fires the attention channel, which becomes its **fourth trigger and the first the model side can pull** — hence the ceiling at the source, one ring per agent per window, rather than a gate on the notification itself; its copy is `An agent needs you`, with no name and no count, because the line naming the agent is already in the hub. The relay is **the one state line that keeps its send stamp**: it is a real message, sent by someone, at a moment that matters, where the interrupt marker and the dialog receipts describe *now*. The hub also gains an unread count for the first time — nothing could previously arrive there that the user had not asked for. Headless prints the notice to stderr; the JSON protocol host gets no new wire event this batch (inventing a shape before a client asks would freeze it) but still runs the rate limiting, so the model's contract does not vary by host. **Named limits**: a completion's DM badge can wait for the next registry sweep because the domain broadcasts `Done` before it stores the history; `release_hires` can still freeze a DM's badge in a project with a live crew; the DM's sequence counts raw history entries, so one report reads as `(2)`; and the coalesce flush rides the tick, so a fully idle loop pays what it owes when it next wakes.

- v1.61 (2026-08-14): six things the first real-device run of D76–D92 found (D93). **A model that cannot see is no longer sent images.** The vision flag reached the system prompt and nothing else, so pasting a screenshot while a text-only model was active shipped the whole base64 block and burned a large slice of the context window on bytes the model could not read — and a screenshot a *tool* read bypassed even the endpoint-wide send gate. Every outgoing request is now projected at the one send seam: each image becomes `[image omitted: <model-id> has no vision]`, in user messages and inside tool results alike, and the token count is measured on the same projected payload so the footer's context band cannot run ahead of what is actually sent. It is a projection, not a rewrite — the transcript on disk and the history in memory keep the real blocks, so switching back to a model with vision shows the image again. **A buffer switch lands at the tail.** The rule and replay a switch prints go to the end of the document, but the scroll offset was reconciled against the document as it stood *before* the rebuild, so the new rows landed below the fold even for a reader sitting at the bottom; the rebuild now builds first and reconciles second, and a switch re-arms the stick so a reader who had scrolled up is brought along. **Three surfaces for the same agent become one.** The pre-blueprint presence strip above the composer — running agents and channels, one summary row — is deleted; the conversation bar is its successor and says more in the same row. And the `#team` board is listed only once a team actually exists: a crew started from `.bingo/team.json`, never a subagent the model hired for one task. The lifecycle log fills either way, so a crew formed later opens onto the history it missed. **Send stamps move beside the message.** A stamp was a row of its own under every body — a terminal line spent on five characters, and down a transcript a column of clocks louder than the words between them. It now sits on the message's first row, flush right, in the muted tier, at least two columns clear of the text; where the row is too narrow to hold both, the stamp is dropped rather than the message wrapped or truncated. Content wins. **The glimmer becomes visible.** The sweep was six cells painted one step along the accent ramp (`claude → claude_strong`, ~18/255 per channel), which the dark theme swallowed whole; it is now eight cells, bold, and painted 55% of the way toward the theme's body text — brighter on a dark ground, darker on a light one, derived from the palette rather than named, so a retuned theme keeps the property. **`$EDITOR` stops swallowing a lost edit.** The reported cause — that a rename-style save (vim's default `backupcopy`) was read back through a stale inode — was tested and is not real: the read-back goes through the path and rename-style saves have always round-tripped, which a new test now pins. The way this round trip *does* lose work is an editor that opens its own window and returns immediately (`code`, `zed`, `subl` without their wait flag): it exits zero before the user has typed, the file is read back unchanged and removed, and the edit they go on to save has nowhere to land. That was reported as a successful edit, which is to say as silence. An unchanged file is now its own outcome and says so — `the editor saved no changes · an editor that opens a window needs its wait flag (e.g. "code -w")` — naming the cure instead of leaving the user to conclude bingo ate the prompt.

- v1.60 (2026-08-14): the palette stops being the terminal's and the code stops being grey (D92). The dark preset was thirteen named ANSI colours while the light one was fully RGB — so on the theme most terminal users run, every markdown heading, list bullet, quote bar, link, table rule, thinking block, tool-output gutter and diff line was whatever colour the user's own scheme had assigned to `Cyan` or `DarkGray`, and shifted when they changed schemes. **Both presets are now spelled entirely in RGB** and a test asserts it over the struct, so no slot can quietly opt out again; terminals without truecolor still get the same colours through the existing 256-colour downgrade, which now walks one enumeration of the palette rather than a hand-copied list. The dark scheme is built around the brand orange `#D77757` on the terminal's own near-black: a warm off-white body (`#EBE7E2`, was pure white), a warm neutral grey ladder, and desaturated teal / sky / mauve for the structural tokens — every slot at 3.1:1 or better against a `#0D0D0D` ground, the text tiers at 15.8 / 6.5 / 3.4:1. **The dim hierarchy becomes three named tiers with jobs**: `text` for content, `text_secondary` for text *about* content (result lines, tool output, diff context, quotes, list markers), `text_muted` for furniture. `text_muted` is the slot formerly called `subtle`, which had no accessor and zero readers in the entire codebase; it now carries the expand hints (`(ctrl+o to expand)`, `… +N lines`), the send-time stamps under every message, the approval dialog's key hints, the transcript pager's footer, the conversation bar's separators and ellipses, the `manager_box` and welcome-card borders, the `(url)` trailing each link, the footer's `·`, and the new diff gutter — and it was retuned in both presets, because a tier that carries text may not sit at 2.3:1. **Fenced code blocks are syntax-highlighted.** A fence that names a language it knows — `rust`, `python`, `javascript`/`typescript`, `json`, `bash`/`sh`, `toml`, `yaml`, `markdown`, `diff` and a dozen more, info strings like ` ```rust,ignore ` included — renders in eight classes: comments recede to the muted tier, keywords take the accent, strings and numbers and calls and types get one hue each. All eight are theme tokens rather than a bundled editor theme, so the dark and light highlight palettes are not two more tables to maintain and `/theme` switches them for free; a test asserts no class collides with the code background or with another class in either preset. **An unknown or missing tag stays monochrome**, exactly as before — guessing a language from a tag we do not recognise is worse than not colouring it. A highlighted line truncates at the same column a monochrome one does, so knowing the language never moves the fence's right edge. Cost is bounded by a memo keyed on `(language, source)`: a block re-rendered without changing — every frame of a streaming tail — pays nothing, and blocks past 96 KB or 4,000 lines are handed back unhighlighted rather than allowed to drop a frame. **Diffs gain an old/new line-number gutter**, right-aligned in the muted tier: a context line shows both numbers, an addition only the new, a removal only the old, and the width is taken from the largest number in the whole diff so a hunk crossing 99 → 100 does not shift the code column. Long lines now **wrap instead of being clipped**, and a continuation carries a blank gutter and a blank marker, so the code column stays a straight edge; the `+`/`-` markers keep their colours. Because there is one diff builder, the gutter appears on the approval preview, the completed-edit rows and the transcript view at once — asserted by a test that renders both surfaces and compares. `/theme` also re-renders the diff rows still in the live region, which are baked when the edit lands and would otherwise be left on the old palette; rows already committed to scrollback keep their old colours, which is the standing write-once contract rather than a gap here. **Deliberate limits**: the diff gutter's wrap width is fixed when the edit lands, so a resize does not re-wrap rows already built (a narrower terminal clips them, exactly as it did before D92); a full-repo audit of the remaining `dim()` sites was not attempted — the menu-gated surfaces (`/model`, `/provider` and `/resume` pickers, the `@` completion dropdown, the ctrl+b agent manager, the rewind list) still use tier 2 for their hint rows and are the obvious next sweep.

- v1.59 (2026-08-14): a wrong turn becomes recoverable (D91). bingo had no checkpoint and no undo of any kind: a turn that edited the wrong files left the user hand-reverting them, and the conversation that produced it stayed in history forever, priming every turn after it. Two presses of **Esc on an empty composer** — the slot D80 explicitly left empty — now open **Rewind**. Stage one lists the turns the user opened, newest first, each with its wall clock, its first line elided to 60 characters, and how many files that turn *and everything after it* changed. Stage two asks what "back" means, in Claude Code's five words: `Restore code and conversation` · `Restore conversation` · `Restore code` · `Summarize from here` · `Never mind`. ↑↓ move, `1`-`5` jump, Enter confirms, and Esc peels one level — action list back to turn list, then closed — the two-stage peel `/model` already established. `EscLayer::Rewind` joins D80's stack in the picker stratum directly under the switcher, and needs no hub check and no busy check of its own: `BackToHub` and `Interrupt` are both layers above it, so in a DM or under a running turn the stack answers first and rewind is never reached. **What each option means**: *conversation* truncates the session file to end at that user message and puts its text back in the composer, because rewinding to a turn is almost always about asking it differently; *code* rewrites the files `Edit` and `Write` changed at or after that turn — a file the turn created is removed **by name**, a file it edited is reverted — and touches the conversation not at all; *both* does code first and reports one combined line, because a conversation restored over files that failed to come back would describe a state the disk is not in; *summarize* replaces that turn and everything after it with a model-written summary. Each leaves a state line in the flow (`⏪ restored 3 files` with the paths, `⏪ conversation restored to 14:32`, `⏪ code and conversation restored to 14:32`) — the v1.45 family: one dim line, no `❯` bubble, no send stamp, nothing in the model's history. **An unavailable half is dimmed and says why** rather than failing on Enter: `Restore code (no files recorded for this turn)`, `Summarize from here (nothing before this turn to summarize into)`. Confirming a dimmed row is swallowed, not fallen through. **Refusals**, all info-tier: a running turn (`finish or interrupt the turn first`), a compaction in flight (`wait for compaction to finish first`), a session with no turns yet (`no turns to rewind to yet`), and — checked again at the moment of confirming, not only when the list was built — a turn that has since been folded into a compaction summary (`that turn is no longer in this conversation; nothing was changed`). **The store**: pre-images are taken by the two tools that write files, once per file per turn, under `~/.local/share/bingo/rewind/<session>/`, independent of git because a repository may not exist and the working tree is not bingo's to commit. It is bounded — 50 MB or 200 turns per session, evicted oldest-first — and a file over 8 MB is recorded as *unsnapshotted* rather than stored. A snapshot that fails for any reason **never fails the edit**: it is written down as a miss, and the selector discloses it (`3 files (+1 unsnapshotted)`) at the moment the user is deciding, which is where that fact is worth something and a ten-second toast twenty turns earlier was not. **Deliberate limits, stated rather than papered over**: (a) anything a `Bash` command wrote is not covered — a shell can change any file in any way and there is no pre-image to take before it does; (b) directories `Write` created on the way to a file are not removed, because deleting a directory bingo may not have made is a larger wrong than leaving an empty one; (c) only turns carrying a turn marker are offered, so sessions started before this version have no rewind points at all — the alternative was inferring turn boundaries from message text, which the harness's own injections (task reminders, notifications, resume prompts, interrupt markers) are indistinguishable from; (d) **the ctrl+o transcript view does not shrink after a conversation restore**. It is built from what this terminal session printed, not from the session file, and the rows above are already the terminal's property under write-once scrollback — so the flow keeps its history and the state line marks where the cut was. What the model sees and what a reload replays are truncated exactly; what the screen remembers is what happened, which is the same divergence Claude Code's own terminal view has.

- v1.58 (2026-08-14): the conversation model gets its chrome — a bar, a switcher, and a way to speak without moving (D90). D89 made every conversation the same shape and reachable, but left "what else is going on" with no surface at all: `/open` could enter a conversation and never named one, so a DM filling up behind you was invisible until you thought to ask. **The conversation bar** is one row directly on the composer, in registry order — `hub  #team  #build (3)  ●@scout (2)  ○@zoe` — with `●`/`○` presence for DMs (the agent state the ctrl+b manager reads), an unread `(n)` badge, a distinct accent when a conversation said your name, and the active one accented and bold. It renders **only when there is more than one conversation**, so a session that never spawned an agent pays nothing for it, and it never carries a badge for the conversation you are in (entering one reads it). Too narrow a terminal elides to `…` around a run that always contains the active entry — deterministic, no scroll state, no animation — and below the width of one entry the active entry survives, clamped, because a chrome row that wrapped would throw off every height the frame assembler measured. **Ctrl+K opens the switcher**: every conversation in one list, ordered by **recency** rather than by the registry (a reader reaching for it is asking what just happened, not what exists) with the hub pinned on top, each row carrying presence, unread, `wants you` and an age; typing filters through the app's one fuzzy scorer, `↑/↓` selects and clamps at both ends, `Enter` switches through D89's own `switch_to` and closes, and `Esc` closes without switching — one press, peeling only the switcher, in the Menu stratum of D80's stack. **`Ctrl+X` stops the highlighted agent** through the ctrl+b manager's own stop path (same warning, same watch transition, no confirmation there or here), and the conversation stays on the list afterwards, listed idle — a stopped teammate still has a conversation worth reading back. It is `ctrl+x` and not the manager's bare `x` because this list filters as you type: a plain `x` would have to be either a letter or an irreversible kill. The switcher is **inert behind a permission dialog** (D81) — it will not open and answers no key — and the ctrl+b manager stays exactly as it was: the two answer different questions, and only the stop action is shared. **Ctrl+K's old meaning moved to alt+k**, beside `alt+d`, its sibling in the kill ring: the same kill to end of line, the same ring, the same coalescing direction. Ctrl+K no longer edits text in any state. **Line-leading routing** is the way to say one thing without going anywhere, and it exists **in the hub only**: a submit opening with a known conversation's name (`@scout have a look` / `#build ship it`, sigil required, matched exactly against the registry) delivers the rest there, does not switch, and does not start a turn — the same delivery a submit inside that conversation makes, so the two are indistinguishable at the domain. The flow keeps a dim one-line receipt, `→ @scout: have a look…`, a state line like the interrupt marker and the dialog receipts: no `❯` bubble putting the envelope in the user's mouth, no send stamp, and nothing in the model's history. A name that resolves to nothing is **not an error and not magic** — `@nobody hi` is prose and opens an ordinary turn, verbatim. Inside a conversation there is no such rule, deliberately: the buffer already *is* the target, and reading a leading name as an envelope there would silently redirect a sentence meant for the person in front of you. **The `#team` board renders and receives**: lifecycle rows now say what happened as well as what was reported (`done · fixed the parser`, where the detail alone used to be the whole row and a finished run looked like a running one), and `/team` output posts to the board instead of the hub's info tier — printed live when the board is what you are looking at, and otherwise carried by the board's unread with one info line, `→ #team`, so the command never answers with apparent silence. The board stays read-only; speaking in it is still refused as information rather than as an error. **Teammate tinting**: while a DM is active the composer's border and its `❯` glyph, and the `⠙ name is replying…` row, wear that agent's own palette colour — the same hue the avatar machinery already assigns the name, in both themes, with no new colours invented. Bash mode still wins the border, because what a surface *does* outranks who is on the other end of it, and a running hub turn no longer dims a DM's prompt: a message to a subagent is a delivery rather than a turn, so that composer is live whatever the model is doing, and dimming it promised a wait that was not happening. Not in this batch, and named rather than quietly dropped: `alt+↑/↓` and `alt+1..9` positional switching, and the eventual consolidation of the ctrl+b manager into the switcher.

- v1.57 (2026-08-14): a conversation is entered, not opened in another screen (D89). Every conversation other than the hub used to live behind a full-screen modal with its own composer, its own key map, its own message renderer and its own quick switcher — a second application reached with one key, which meant the approval dialog, the queue, the readline, the motion tokens and the transcript view all stopped at its border. **The modal is retired.** One terminal, one flow, one active conversation: the hub, a DM with a subagent, an agent channel and the `#team` board are the same shape and share one composer, one key surface, one approval path and one row builder. **Entering one** is `/open @agent` · `/open #channel` · `/open #team` · `/open hub`, with Tab completing from the conversations that actually exist (each offered with its unread count, and a conversation that named you reads `wants you`); `/open` is an instant command, so it works while a turn runs. Enter on an agent in the Ctrl+B manager opens that agent's DM the same way. A conversation bar and `ctrl+k` arrive in D90. **Switching** stashes the draft you were writing where you were writing it, restores the one waiting in the conversation you are entering, prints a `── @name ──` / `── #name ──` rule into the flow and replays that conversation's **last 30 messages** — its own, extracted by the same code that always extracted them, so a replayed DM message is byte-identical to what the retired view showed. From then on only that conversation prints. **Everything else keeps accumulating where it already lives** and counts up an unread badge; nothing is buffered on the user's behalf, and a hub turn that finishes while you are in a DM does not print into the DM — it waits at the hub, and coming back prints a `── hub ──` rule followed by exactly what it missed. **Esc is navigation before interruption**: in a conversation it returns to the hub in one press, above the interrupt in D80's stack, so a busy turn survives the press and its Esc-to-interrupt is reachable from the hub — the one place the turn is the thing on screen. Transient layers stacked over a conversation still peel first, and the status row says which it is (`esc to hub` rather than `esc to interrupt`). Ctrl+C is untouched and still interrupts from anywhere. **Sending** goes to the conversation you are in and never starts a model turn: a DM delivers to that instance under your name and appears the instant it is sent, a channel posts to the log and shows immediately, and `#team` is a record rather than a room — its refusal is an info-tier line, because nothing failed. A DM message neither queues behind a running turn nor steers it (D83 offers the hub's submissions only). Slash commands are the exception by design and act on the application from anywhere, so `/model` in a DM is still `/model`. **A DM in flight** renders in the redrawn tail, never in scrollback, because it is a state rather than a record: the message you sent, the agent's tool calls and reasoning phases as dim lines, and a `⠙ name is replying…` row that carries the instance's live token rate and spans the whole stretch a reply is owed — from the send, through tool waits, to the last delta. The moment any of it becomes record it arrives in the flow as a settled message and leaves the tail. **Decorations** the retired view carried and the flow keeps: the sender's name heads each run of messages (grouped, so a streak from one speaker carries it once), send-time stamps trail every body unchanged, runtime wake-up scaffolding still collapses to one dim line, and the D64 `[DM from user]` marker is still transport that never renders. **Accepted cost**: scrollback is written once and never rewritten, so a couple of excursions leave the same conversation on screen more than once. Every segment opens with a rule that names it, and `ctrl+o` remains the complete authority — it shows the hub session, which is what it showed before. **Lost with the modal**, and named rather than quietly dropped: the per-instance context-usage meter that only its composer footer displayed (the footer's context bar is the hub's), and the alt+↑↓ / Ctrl+K navigation the modal owned, which returns in D90 as bar and switcher over every conversation at once.

- v1.56 (2026-08-14): motion becomes a layer instead of a habit (D87). bingo's only continuous animation was the update banner's breathing, the `motion` setting reached two surfaces out of five, and the main spinner — gated by nothing — advanced a glyph every frame, roughly four times Claude Code's rate. **Seven tokens now issue from one authority** (§1.1): `pulse` (120ms/glyph, same 8-glyph star cycle), `beam` (a 6-cell glimmer crossing the running verb once per 2s), `stall` (3s of silence turns the spinner and verb warning-coloured and puts the glimmer out), `settle` (the completion row wears the accent for one 120ms window before it freezes), `breath` (the banner curve, moved unchanged), `title` (`✳ ⠂ ✳ ⠐` at 960ms on the terminal title) and `meter` (a jumped number eases to its target over 300ms — applied to the status row's `↓ N tokens`). **The gate is now singular and honest**: `motion: "off"` / `BINGO_NO_MOTION=1` rests every decoration including the spinner, and deliberately does *not* silence `stall` or `settle`, which change colour to say something rather than to move. Every token is a pure function of a frame number, so animation is testable without a clock and the demand-gated loop stays deterministic; each token keeps the loop awake for its own window only. The stall state adds no copy — the verb, the seconds and the Esc hint are unchanged, because nothing new is known at three seconds. The settle blink respects write-once by happening *before* the row freezes rather than after.

- v1.41 (2026-08-13): `/model` level-two feedback under D65 — a declared model list has no loading state and no failure state at all (it is read from settings, so no request can fail); a fetched list that fails while a cached one exists now renders **the cached list plus the reason row** (`⚠ <reason>; showing the last known list`) instead of the reason alone, applying the standing "degraded and visible" rule to a case that previously degraded to an empty menu. The page-level short-sync error row (§1 timeout tiering / §4.4 `TIMEOUT`) is unchanged and still fires. The level-two hint row gains `r refreshes` for fetched lists only.
- v1.40 (2026-08-11): tool-output feedback hardening for issues #5/#22 — Bash stdout/stderr is streamed through the configurable `bashOutputMaxChars` cap (default/maximum 48,000) and appends a `[Content truncated: …]` recovery hint directing the agent to redirect output and use Read; invalid UTF-8 is replaced with an explicit note instead of silently dropping the stream. Read range calls keep the existing 20,000-character cap and append clear range/truncation notes after the bounded content.
- v0.1 (2026-08-07): draft, covering the three error tiers (Loading/Toast/error states) and acceptance anchors.
- v1.0 (2026-08-07): merged devex's three additions — structured error protocol (4.1), TTY/non-TTY degradation (4.2), four state-machine resets (state-machine section); plus GUI-side feedback: error codes presented collapsible, and the general principle that feedback states must not depend on the environment.
- v1.1 (2026-08-07): merged main's two implementation-side additions — async focus after render (state-machine reset #4 / §5 "focus timing"), and the CLI error-code contract format `[error] code=... msg=...` (4.1); merged qa's six boundary categories — tiered timeouts with timer cancellation (Loading §"timeout"), error-code → user-action mapping (3.1), mixed state (3), action-granularity duplicate prevention (Loading §"duplicate prevention"), stale-response race (state-machine section), Toast quantification (2); testability conventions (6); under reduced-motion the loading indicator is retained, aria-live writes an empty string instead of deleting the node.
- v1.2 (2026-08-07): merged devex's msg escaping conventions (newlines normalized to spaces, primary msg truncated to 200 chars, multi-line stacks go to `detail=`, see 4.1); merged dev's current-state findings — error-code implementation path A/B/C pending main's ruling (4.3); this spec constrains only the exit contract and does not depend on the internal path.
- v1.3 (2026-08-07): merged devex's three C-path guardrails — downcast prevention (module errors implement the `ErrorCode` trait returning `&'static str`; exits only call it, never branch on types), registration is the contract (unregistered falls to `GENERIC` + debug-build warning), drift-guard unit tests (4.3); error-code naming convention `SCREAMING_SNAKE` (4.1); added the §4.4 "scenario → error code" example table for dev/qa reference.
- v1.4 (2026-08-07): merged qa/devex's three additions — the fallback code is a published stable code `GENERIC` and the unregistered-fallback path is assertable (guardrail 2); code values only grow, never modified/reused — semver rule (guardrail 3); dual-exit consistency enforced structurally: GUI/CLI share the same `map_error` function, a single code-table file, assertions as fallback (guardrail 4).
- v1.5 (2026-08-07): dev's ruling — path C exit mapping (4.3); the contract centralized in the single file `src/error.rs` (ErrorCode trait / GENERIC fallback + debug warning macro + map_error / drift-guard tests); ErrorCode trait implementation model: each module enum has an exhaustive match returning stable codes (unhandled new variants fail compilation) + a tail `_ => missing_code + GENERIC` debug warning.
- v1.6 (2026-08-07): qa clarified that "exhaustive match" and "a `_` fallback arm" contradict; dev corrected the implementation model — **drop the `_` fallback arm, compile-enforce true exhaustive match** (unhandled new variants fail compilation); `GENERIC` becomes **explicit return** (explicit behavior + debug warning), with the release-mode semantics honestly recorded as "no stable code assigned yet"; drift-guard tests change to **enumerating every variant of every module, asserting non-GENERIC** (blocks missed registration during the CI period).
- v1.7 (2026-08-07): merged the **GENERIC allowlist landing details** proposed by qa and finalized by dev (guardrail 5) — `const GENERIC_ALLOWLIST: &[&str]` in `src/error.rs` using locatable paths (e.g. `"tool::bash::Error::NonZeroExit"`); unit tests assert "any variant not in the allowlist is non-GENERIC"; entries must carry a `TODO(generic-allow): <issue>/<date> <reason>` comment; review convention: new allowlist entries must have a reason, entries without one are not allowed.
- v1.8 (2026-08-07): dev review landed — §5 annotated as the "Web DOM/ARIA stance", with the **bingo TUI mapping table** added (chat.busy→aria-busy, error-line highlight→red outline, status-area update→aria-live writes empty string, error line scrolled into view + highlighted→focus transfer, spinner rate degraded→reduced-motion, etc.; normative values unchanged); §6 adds Rust-side timing tests using `tokio::time::pause/advance` (zero new deps); 4.3 gains implementation touchpoints: `src/ui.rs` is the renderer-agnostic contract layer (the natural mount point for map_error), `UiEvent::Error(String)` has only 3 touchpoints to change into `{ code, msg }` and is the ideal entry point, and `chat.busy` is already a turn state machine precedent.
- v1.9 (2026-08-07): DX review corrections (devex) — §4.3 "landing ruling" fixes v1.5's stale wording ("`GENERIC` fallback" → "`GENERIC` explicit return + debug warning", "representative error variants per module" → "enumerate every variant of every module"), removing the self-contradiction with guardrail 5 / the v1.6 correction; v1.7 changelog attribution fixed (GENERIC allowlist proposed by qa, finalized by dev); the doc enters the reference network — `notes/research.md` "references" section gains a link, `AGENTS.md` "built-in skills sync" gains the rule "changes touching user-visible feedback states must be checked against this file".
- v1.10 (2026-08-07): dev re-review wrap-up — fixed guardrail 2 "registration is the contract" and §4.4 example table's **same-source automatic-fallback leftovers** ("unregistered → `GENERIC` fallback" / "unregistered path → lands on `GENERIC`" → "every variant explicitly returns a stable code; those not yet assigned explicitly return `GENERIC`; `_` implicit fallback is forbidden" / "explicit `GENERIC` path → lands on `GENERIC`"); the whole document is now fully self-consistent with the v1.6 model.
- v1.11 (2026-08-07): dev ruled on AC-15 timeout tiering — §1 "timeout" row and §7 anchors gain **per-operation-type granularity**: short synchronous operations (list_models/count_tokens/complete_text etc.) get the feedback layer's read 10s / write 15s, with retry as the primary action on timeout; **agent long turns don't get 10s/15s** (continuous progress feedback already exists; the transport layer 120s/60s + user interruption applies), **long-turn failures escalate to flow-level errors**; cancellation = the feedback layer drops the future at the deadline (tokio `timeout()`), the sequence-number check is only a fallback.
- v1.12 (2026-08-07): dev implementation-period backfill — §4.4 scenario table upgraded from "example" to the **complete registration-is-the-contract code table**: added `RATE_LIMITED` (429 throttling), `STORAGE_ERROR` (local storage), `TOOL_FAILED`, `HOOK_FAILED`; `AUTH_REQUIRED` semantics extended to "login expired / key missing / invalid key / 401"; `SERVER_ERROR` covers stream protocol and MCP connection failures; landing points: all 10 module error enums implement `ErrorCode` (exhaustive match with no `_` arm), `UiEvent::Error` structured (`{ code, msg }`), top-level CLI exit `[error] code=... msg=...` (non-TTY), feedback-layer timeout tiering (read 10s / write 15s). Code values only grow, never modified or reused.
- v1.13 (2026-08-07): devex post-implementation re-review backfill — guardrail 1 clarifies the downcast form: **matching by type name / string is forbidden**; `downcast_ref::<$t>()` **compile-time type references** are allowed (refactor-time errors); guardrail 4 gains the **dual-exit implementation story**: the TUI goes through `map_error`, the CLI boxed exit through `error_code_boxed` + a macro registry (registration-is-the-contract site #2, with a full-coverage test for the registry); the mapping logic stays single-sourced (ErrorCode impl) for consistency; the implementation model notes **current explicit GENERIC paths = 0** (missing_code dormant; future additions must call it).
- v1.14 (2026-08-07): qa regression-evidence addition — the implementation model hardens `missing_code`'s warning duty: any future explicit `GENERIC` return (including the **boxed-exit macro-registry miss landing on `GENERIC`** branch) must call `missing_code` to warn (loud in debug / known semantics in release); the dormancy cannot be documented alone with no code-side warning tail.
- v1.15 (2026-08-07): qa #69 contract alignment + main's ruling — §3.1 example table changes `AUTH_EXPIRED` to **`AUTH_REQUIRED`** (single-source alignment with §4.4/implementation, no new code; the login-expired/key-missing/invalid-key/401 semantics ride on msg + user action); §4.4 `TIMEOUT` row gains a **dual-presentation-level note** (short-sync = page-level; long turn = flow-level, AC-53 — the presentation level is decided by the trigger context, not inferred from the code alone).
- v1.16 (2026-08-07): #18 presentation-layer minimal implementation backfill (dev #86 + #92) — `UiEvent::Error` extended to `{ code, msg, level, context }` (level/context explicitly carried by the producer at emit time; chat.rs lands Full+LongTurn at turn level); §5 gains the TUI-side note "level is carried by the producer; the render layer only consumes, never derives; render-layer/test-side code→level mapping copies are forbidden"; §3.1 gains the "error level = typical slot, context can override" note (TIMEOUT dual-tier, PERMISSION_DENIED dual-tier takes the flow-level slot). The presentation layer is driven by `last_error` (chat.rs): Full = full-screen state (title + code + explanation + action hint, Enter retries / Esc returns / Ctrl+C exits), Page/Field = error line highlighted in the error color. **#92 short-op degradation stays visible**: §4.4 gains the "short-sync operation failure = degrade visibly, never swallow silently" story — list_models/count_tokens failures emit a Page+ShortSync error line (behavior degradation preserved: the menu stays empty/usable, budget 0), TurnStart resets the error state; production `ErrorLevel::Page`/`ErrorContext::ShortSync` move from dead_code to real emit sources.
- v1.17 (2026-08-07): todo task-area completion state tightened (ui/ux design) — the task area distinguishes **auto-opened** (TaskCreate signal, `tasks_auto`) from **manually opened** (Ctrl+T); an auto-opened panel auto-hides when all tasks are Completed (refresh_tasks closes the loop), reusing the §2 transient-row mechanism to push `✓ N/N tasks done · ctrl+t to view` (2s TTL, not persisted) for closure and a way back; a manually opened panel stays when everything completes (a state the user explicitly asked to see; no transient row); `/tasks` explicit requests are unaffected (no false "no background tasks" report).
- v1.19 (2026-08-07): non-fatal warning lifecycle tightened (main on-site report) — the `⚠` warning line above the input (MCP connection failure / image load failure etc.) changes from **persisting until /clear** to **auto-expiring after a 10s TTL** (`Chat::WARNING_TTL`, expiring entries cleaned on push + render filtering, dedup semantics kept); same spirit as §2 Toast "light notifications auto-dismiss" (difference: the warning is a static line above the input, not the Toast channel). MCP connections run in the background (not blocking turn input); failures are deferred and reported once in the next turn via `drain_unreported_failures`; new failures after `/mcp reconnect` can be reported again.
- v1.18 (2026-08-07): AskUserQuestion answer feedback block lifecycle tightened (main on-site report) — the answer-result block (`⏺ User answered the questions:`) changes from **persisting until /clear** to **transient within the turn**: cleared at TurnEnd (including zeroing the `flushed_ask_rows` cursor so the next block isn't skipped in rendering); kept during the answering process and the turn (intermediate states of multi-question flows visible, answers echoed back), gone when the turn ends — the block renders at the document tail / above the input, outside the message flow, and persistence would read as residue; same spirit as §2 transient rows' "completed feedback doesn't persist" (difference: the block is visible throughout the turn, no TTL). The answer content is already fed back to the model via tools, so no UI persistence is needed. **Superseded by v1.20**.
- v1.20 (2026-08-07): AskUserQuestion answers become **ordinary user messages in the message flow** (main on-site report: after v1.18 the block still clung above the input) — root cause: the result block rendered at the document tail (after messages, above the input), and only entered the settled/persisted state when "all preceding messages settled + no pending_ask + last message settled"; during the turn's model streaming that condition never held → the block clung above the input. Fix: delete the AskResult struct / `ask_result` field / `flushed_ask_rows` cursor / `SettledMark.ask_rows` (uproot the block's special logic); on answer submit, option confirm, or Esc decline (free_text requests) push a **User message** directly (content preserved: `User answered the questions:
  · question → answer`; declines are `User declined to answer questions`), rendered like user input (bubble), settled like it, persisted into scrollback like it, and kept with the session (no longer cleared at TurnEnd). **Ordered-settle guard**: a mid-turn answer message sits after the streaming assistant message; if the preceding messages aren't settled (streaming/tool running/image loading) this message must not settle either — otherwise persistence would jump past streaming rows and bake intermediate state into scrollback (the same invariant as "streaming content doesn't hit disk"). Difference from v1.18: answers are no longer turn-transient; they stay in the session like ordinary messages (visible on scroll-up, cleared only by `/clear`).

- v1.21 (2026-08-07): slash command interaction alignment landed (Team A · feat/slash-ux) —
  busy-time whitelist instant commands (think/model/provider/theme/status/context/tasks/help/skills run
  immediately while busy and busy stays unchanged; other slash commands queue and dispatch per command
  after TurnEnd, no longer sent to the model as plain text);
  `/think` level picker dual markers (●=in effect, fixed; ❯=browsing selection) + 1-6 direct jump +
  footer `think {level} ▸` preview state (Enter commits / Esc reverts); slash completion rows gain the
  arg_hint parameter hint;
  no-match hint row (`/zzz` → dim row, chrome-level hint not error-level, no error code);
  structured slash errors (UNKNOWN_COMMAND / BAD_ARGUMENT, `[error] code=… msg=…` single line, qa asserts on code only);
  slash output TTL grading (success 2s / error and usage ≥8s, cleared on the next input — spec in design contract §4.4);
  defer records: subcommand secondary completion, /model session-only `s`, model/thinking persistence layer (pending Q1).
- v1.25 (2026-08-09): image rendering unified on kitty Unicode placeholders (D42) — images now appear in the live viewport of both hosts the moment they load, tmux included (previously tmux fullscreen never showed images and tmux inline only after the block scrolled into scrollback); the loading state is unchanged (`#[image]` until loaded, `#[image ✗ load failed]` on failure). New one-time warning for WezTerm/Konsole (any environment): "This terminal does not support kitty Unicode placeholders (WezTerm/Konsole); images display as #[image]" — these terminals answer the kitty graphics query but cannot render U=1 placeholders, and with the C=1 direct path deleted they drop from image support (previously they displayed images outside tmux). The tmux passthrough probe warning is unchanged.
- v1.24 (2026-08-08): feedback tiers wired end to end (audit batch 3). Four output tiers: transient confirmations keep 2s; errors/usage move to the 8s error tier at every site (a dozen spoke through the success channel in plain color); a new INFO tier (`slash_info_lines`) holds explicitly requested reading — /help /status /context /config, listings, share URLs — persisting until the next input or Esc; pinned panels (`UiEvent::PinPanel/Unpin`) carry flows that must outlive any TTL: OAuth device codes stay for their full validity, the loopback flow shows the auth URL itself, and long operations (/compact, stats, MCP check/reconnect, share upload) hold a visible progress line until resolution. Startup notes (invalid provider fallback, transcript failures) reach the TUI via the info tier — the alt screen wiped stderr. Page/Field errors no longer reset a running turn's busy state, render in the fullscreen host (pinned above the prompt), and dismiss with Esc.
- v1.23 (2026-08-08): image feedback hardening after main's real-terminal reports — a failed image load renders a distinct `#[image ✗ load failed]` marker row (previously identical to the still-loading placeholder), with the warning line keeping the url; network fetches send a User-Agent and reject non-2xx bodies instead of failing later at decode; all kitty graphics commands use `q=2` so terminal error replies can no longer land in the input box as typed garbage (`ENOENT: image not found` flood), and both hosts retransmit placements after a resize purges the terminal-side image store (fullscreen previously lost images on resize until the next message).
- v1.22 (2026-08-08): fix feedback-lifecycle drift on the real host paths — the alternate-screen `run_fullscreen` now shares the Full error-state semantics with inline frames (title, stable code, explanation, Enter retries / Esc returns, input box and caret hidden) instead of bypassing `Frame::assemble`'s error branch; idle scheduling for slash error/usage lines feeds `slash_error_at` into `needs_tick`, so they clear after the 8s TTL and return to true idle even with no further input. Adds regressions for the real fullscreen frame and host/state idle TTL, covering "what the user sees" and "when feedback disappears". Public-share feedback is tightened in the same batch: local export is the default and only an explicit `--public` goes online; the "anyone can access + may contain sensitive content" warning shows before any byte is uploaded.

- v1.26 (2026-08-09): a confirmation tier the permission mode cannot silence (D46). The Team tool's changes — `start` / `stop` / `save` — and any Write/Edit aimed at `.bingo/team.json` prompt through the existing permission modal in **every** mode, including `bypassPermissions` and `acceptEdits`, and an `allow` rule cannot pre-authorize them (only `deny` outranks it); the mechanism is `Tool::confirm_reason`, sitting where the sensitive-path safety check already sat. Nothing new is drawn: the surface is the same `⏺ Allow Team` block with allow/deny. What changes is the copy contract for that tier — the description line states **the change, not the call** (`Rewrite .bingo/team.json · dev-room · 4 members (-ui +qa)`, `Pull up dev-room · 3 members (dev-ex, ui, qa) into #dev-room`), because a user approving a crew change is deciding about the crew, not about a file write. It stays one line by construction (the modal renders `request.question` as a single `Line`): rosters longer than four names collapse to `a, b, c, and N more`. Read actions (`status`/`validate`) are ordinary read-only tools and prompt for nothing.

- v1.27 (2026-08-09): the workspace view sheds its chrome (D47). The rail and sidebar are gone — Ctrl+K (now listing every conversation with its unread count) and alt+↑↓ carry navigation, Tab moves between the message list and the composer. **No surface colours**: the view paints foregrounds only, so the terminal's own background is the background in both themes; the only backgrounds left are marks (the avatar chip, the switcher's selected row) and one explicit erase (`Color::Reset` on overlay rows — ratatui patches styles, so an overlay of plain spaces would let the colours underneath show through). The header is two rows instead of three, with the team's name at the right edge. **Avatars**: where the terminal can place kitty images, each sender wears one of eight bundled portraits (4×2 cells, transmitted once per portrait, re-sent after a resize purges the image store); everywhere else the initial-on-colour chip stays, and the row count is identical either way — the fallback changes the gutter, never the layout.

- v1.28 (2026-08-09): the workspace stops quoting the runtime at the user (D49). Wake-up scaffolding written into an instance's history — a relayed channel message, a follow-up chase, the `[SYSTEM NOTIFICATION - TASK REMINDER]` block — used to render as a full quoted message under a "You" name row and avatar, as if the user had typed it. It now collapses to a single dim `▏` line (`#dev-team · main: …`, `system reminder · task tools`) that owns no name row and does not split the grouping of the real messages around it; text a person actually wrote is untouched. Same tier and same visual language as a tool attachment: this is context, not conversation.

- v1.29 (2026-08-10): the crew and its hires read as two different things (D53). `/team list` keeps the blueprint's roster and the runtime zone as the crew's alone, and adds a separate `temporary hires (N) · not in .bingo/team.json · released when their task is done` block below it — same row shape (`name · state · what for`), different heading, so a temp is never mistaken for a member. `AgentControl list` prefixes every row with `crew`/`hire` for the same reason. A released hire is announced rather than swept silently: the hub gets a task-notification line naming what went, because otherwise its next `SendMessage` to that name fails with `no subagent named …`, which reads as a bug instead of the agreed lifetime. New read command `/team norms` prints `.bingo/team-norms.md` under a `▸ team norms · <path>` header with the precedence line ("carried by every member and every hire; a direct instruction outranks it") above the file's own text; with no file it says so and points at `/team new`, which now scaffolds one beside a fresh blueprint (never overwriting an existing one).

- v1.30 (2026-08-10): empty long-turn completion no longer looks like success (#15). An upstream completion with no committed assistant content and no tool calls — including an unclosed thinking/text/tool block — is classified as malformed, retried automatically once while side-effect-free, and kept out of history; successful recovery may produce a non-fatal warning, while a second empty result enters the existing Full+LongTurn retry/back state. §7 adds the regression anchor that the empty attempt is never silently accepted.

- v1.31 (2026-08-10): a turn can no longer disappear and take the session with it (follow-on to v1.30). v1.30 stopped *producing* content-free assistant turns, but transcripts written before it still carry `content: []`, and history normalization indexed the first block of every message unguarded — resuming such a session panicked inside the spawned turn task. Tokio swallows that panic (nothing joins the handle) and the alternate screen repaints over its message within a frame, so the only symptom was a turn stuck on `✻ …ing… (esc to interrupt · Ns)` forever, with `busy` latched and gating Esc, Ctrl+C, submission and `/quit` alike — the session answered only to `kill`. Three changes: normalization reads blocks through `first()`; a lost turn is reported as the new flow-level `TURN_LOST` (retry / go back) instead of latching `busy`; and Ctrl+C force-quits once an interrupt has gone unhonoured for `INTERRUPT_GRACE` (3s), announced by the `Interrupting… press ctrl-c again to force quit` notice — a healthy interrupt is untouched, since the next turn clears the stamp. Loading history also drops messages that carry nothing (content-free messages and blank text blocks), so transcripts already poisoned are resumable rather than permanently rejected by the endpoints.

- v1.32 (2026-08-10): the transcript stops lying about order and about subagent work (D55, issue #28). **Order**: an `AskUserQuestion` answer is pushed as an ordinary user message (v1.20) but `stream_msg` still pointed at the assistant message above it, so everything the model did next rendered on top of the answer and the answer stayed pinned to the bottom until TurnEnd. The answer now ends that message and opens a fresh one (`open_continuation_message`) — the interrupted message settles and flushes immediately instead of waiting for the turn, a continuation the turn never filled is dropped at TurnEnd (tracked by `continuation_msg`), and a tool still in flight pins the stream where it is. **Folding**: `AgentControl` joins the existing collapse groups instead of producing one two-line block per call (and instead of closing whatever group was open); a look and a change are counted apart, so the summary reads `Checked 3 subagents, stopped 1 subagent` and never reports a killed run as a glance. The ⎿ row under a running group already showed the latest call — it now shows *which instance* it was aimed at, because `summarize_input`'s k=v fallback (first key, alphabetical) always landed on `action` and hid the target for `AgentControl`, `Channel` and `Team` alike. Every collapse group (not only subagent ones) now carries `· N failed` in the error colour when something inside it failed: a fold that can hide a stop must not count a refused stop as a success.

- v1.33 (2026-08-11): `/cd <dir>` follows the existing slash feedback tiers — a successful session-directory switch is a transient confirmation, while a busy turn, missing arguments, missing paths, and non-directory targets use the structured `BAD_ARGUMENT` error row and leave the current directory unchanged.

- v1.34 (2026-08-11): issue #33 narrows the entity strip and DM feedback surface. The compact strip lists running agents and channels only; running agents expose model/thinking/state, and Ctrl+G opens the full workspace directly instead of entering an inline selector. DM history and live tails render only user messages and agent prose: tool activity is deliberately absent there. DM bodies reuse the main transcript's user-bubble and assistant-markdown row builders (prefixes, wrapping, and row attributes included), while the existing DM name/avatar gutter remains unchanged; the trailing working indicator remains during a hidden tool wait so a long turn never becomes silent. DM headers expose model/thinking/state; channel headers list model/thinking when the complete names fit and otherwise use one bounded aggregate, preserving the composer on short terminals. Channel message rendering itself is unchanged.

- v1.35 (2026-08-11): issue #36 restores direct running-agent interaction beside the #33 workspace entry. With an empty composer, ↑/↓ opens and moves a bounded running-agent selector and Enter opens that agent's DM; Esc collapses it, while Ctrl+G still opens the full workspace. Ctrl+B opens a background-agent manager over the main chat: ↑/↓ select, Enter shows prompt/status/elapsed/tokens/tool count/recent tool activity, x stops the selected run, and Esc closes or returns; hub-and-spoke deliberately has no foreground action. Stop results reuse the existing warning tier, and running detail remains live through the normal tick refresh.

- v1.36 (2026-08-11): workspace DM/channel transcripts accept mouse-wheel scrolling at the same three-row step as the main fullscreen chat; `AgentControl list` adds relative last-activity feedback, refreshed on inbox receipt, turn start/end, streamed text, and tool ready/done signals.

- v1.37 (2026-08-11): live generation feedback adds a shared three-second token-rate sampler. The main footer and a running instance's DM composer show the rate only while output samples are arriving; idle and two-second stalls hide it. Five stable bands pair the number with motion density and cadence (`⠋⠙⠹`, `▓░`, `▁▃▅▇`, `█▀▄`, `≡≡≡`); `motion: "off"` keeps the value visible and freezes the frame. The same right-hand area persistently shows context usage (`▓▓░░ 58% 74240/128k`) even while idle, using the exact/projected count returned by the auto-compact token gate and the active model's context window; below 70% is neutral, 70–90% warning, and above 90% danger.

- v1.38 (2026-08-11): issue #37 adds `CONTEXT_OVERFLOW` to the stable error table. A provider-reported 400/413 overflow is recovered internally by compacting and retrying once; only failed recovery reaches the existing Full+LongTurn error surface, with the stable code available to both TUI and headless exits.

- v1.39 (2026-08-11): issue #35 removes SendMessage's sender-turn latency. The receipt stays immediate and retains its queued/delivered/answered/dropped contract; an idle receiver is claimed and shown Running before the tool returns, while a running receiver gets an inbox signal and folds everything waiting into its context between tool rounds. The batching boundary is therefore the receiver's actual inbox drain rather than the sender's turn end. The existing acknowledgement watchdog and stop/delete feedback are unchanged.
- v1.40 (2026-08-11): issue #6 adds visible manual storage cleanup. `/gc` shows its progress row for the bounded synchronous cleanup and then emits an info-tier summary with per-kind counts; failures use the structured `STORAGE_ERROR` page-level path with a disk-permission + retry action. Startup applies the same bounded policy before storage-reading command paths and warns on stderr if it cannot complete.

- v1.41 (2026-08-12): issue #39 adds long-turn recovery for transient in-stream API errors. `429`, `5xx`, overloaded, and `server_error` events restart the uncommitted model response up to 10 times; a server-provided retry delay wins, otherwise the delay uses capped exponential backoff with ±10% jitter. The first reconnect notice is suppressed and attempts 2–10 show `Reconnecting... N/10`. TUI and subagent live views clear failed-attempt output before the replacement stream; headless stdout cannot retract an already-written prefix, while persisted/result content still contains only the successful attempt. Quota, usage-not-included, invalid-prompt, and context-overflow errors remain immediate failures, and short synchronous operations retain their existing 10s/15s timeout tier without entering this retry loop.

- v1.42 (2026-08-12): review hardening of v1.41. A server-provided retry delay is now clamped at 60s so a misbehaving `retry_after` cannot park a turn for an hour behind the suppressed first notice; local backoff (500ms start, ±10% jitter, 32s cap) is unchanged. Message-based transient classification now treats a bare 5xx number as a status code only when it opens the message or follows an HTTP-status marker, so copy like "512 characters" no longer looks retryable. No copy or state-flow changes.

- v1.43 (2026-08-13): auto-compaction review (D66) moves compaction's own feedback onto the shared channel. Success, failure and the tripped circuit breaker are emitted through `UiHooks::on_warning` instead of stderr, so they surface as a TUI warning row, as headless stderr, and as the existing JSON `warning` event — previously the TUI and a JSON client (both `quiet`) saw nothing at all, and the success line wrote stderr under a TUI that owns the screen. Success reads `context compacted: N earlier messages replaced by a summary`; failure keeps its "history kept as-is" wording plus the reason. No new protocol event type and no new error code. The `/compact` slash command is unchanged and still reports through its own slash-output/slash-error rows, now with the "conversation is too short" floor reading the same `KEEP_RECENT` the compactor applies.

- v1.44 (2026-08-13): `/team start` reports a third outcome (D69). A member already up that is not mid-turn re-reads its definition and applies it in place, so start's summary is now `spawned ×N` · `refreshed ×N` · `reused ×N` — the first two words were the whole vocabulary, and an edited member came back as `reused`, which said the opposite of what had happened. The slash headline follows: a start whose only effect was re-reading definitions reads `[team] <name> up to date · definitions re-read for <names> (history kept)` instead of `already running · reusing existing instances`, and the Team tool's result gains a matching `refreshed N already running (definition re-read, history kept): …` line. No new error code, no new surface; the tier is the existing slash-output/tool-result text.

- v1.45 (2026-08-14): interrupt feedback becomes a record instead of a toast (D76). A turn stopped mid-stream used to be discarded whole — the partial reply stayed on screen while the model's history denied it existed — and the only signal was a 10s `turn interrupted` warning that expired while the split-brain stayed. The partial reply (text and signed thinking; an unfinished `tool_use` or an unsigned thinking block would 400 every later request, so both are dropped) is now recorded, followed by the model-facing marker `[Request interrupted by user]`; an interrupt during tool execution appends `[Request interrupted by user for tool use]` after the filled tool_results, and a turn with nothing accumulated records the marker alone rather than an empty assistant message. The transient warning is deleted: the marker is the signal, rendered as a single error-coloured line without the `❯` bubble or send stamp, because the harness wrote it and the user did not. Interrupted tool rows gain their own state — amber glyph, result line `Interrupted`, nothing to expand — instead of borrowing the green Done glyph, and the `!` command's row is now closed the same way (a row left Running keeps its message from ever settling). No new error code and no new tier.

- v1.46 (2026-08-14): the terminal safety net, and the compaction warning finally reaches the surface that owns the screen (D77). A panic outside the turn task used to end the session in raw mode inside the alternate screen with the cursor hidden — the panic message printed to a terminal that could no longer render it, and the shell that came back was unusable. A process-wide panic hook now hands the terminal back first (bracketed paste, mouse capture, alternate screen, raw mode, cursor — the same order and the same best-effort steps as a clean teardown) and then delegates to the previous hook, gated by a "tui active" latch claimed at setup and swapped at teardown, so exactly one of the two paths restores and neither can double-restore. Only a panic on the thread driving the host counts: a panic inside a spawned turn task is still the recoverable `TURN_LOST` state from v1.31, and that session keeps its screen. **Pre-compaction warning**: `context at {tokens} tokens; auto-compact at {threshold}` travels `UiHooks::on_warning` (the D66 channel) instead of stderr under `!quiet` — which was precisely the two hosts that never see stderr, so the TUI and a JSON client were told nothing at the last moment they could still act. It arrives as a warning row, as headless stderr, and as the JSON `warning` event; the per-turn `context: N tokens` progress line stays on stderr alone, because the TUI reads that number off the footer every turn already. **Footer bands** now count down to the auto-compact trigger instead of to the raw window: warning within 20 percentage points of it, danger within 5, measured against the same denominator the label prints. The old fixed 70/90% described no model correctly — the window and the trigger differ by that model's own output budget, so for a model reserving 384k of a 1M window the danger band opened at 90% when compaction had already fired at 55%. Superseded: the band sentence in v1.37. Bar, percentage and `used/window` text are unchanged.

- v1.47 (2026-08-14): the collapse fold stops eating what it folds (D78). A run of reads and searches renders as one line (`Read 3 files (ctrl+o to expand)`), and until now that line was where their output went: the result text was stored on a tool row only when the row stood on its own, so a folded member kept its `Read 173 lines` summary and nothing behind it — and since a row is expandable only when it has something to show, opening the group revealed summaries over empty bodies. The output of a call that had really run was unreachable for the rest of the session, with no surface left to ask for it. Every finished call now keeps its output by the same rule, folded or not (blank lines dropped, Bash's `$ cmd` echo and `[Exited with code N]` footnote stripped, bounded by the same per-result budget the model's own copy lives under), so opening a group shows each call's own lines. The collapsed row is untouched — same summary wording, same `· N failed` tally, same `⎿` input hint under a running fold — because it never read member content. Opening a group with the mouse now opens its members with it: every row of an open group is wrapped in the group's own click target, so a member row cannot be clicked open on its own, and the group's state has to carry theirs. An interrupted member is the one row that still holds nothing: it was stopped before it produced output, and its result line goes on saying `Interrupted` (v1.45) rather than borrowing a body from a run that never finished.
- v1.48 (2026-08-14): feedback leaves the viewport (D79). Every state this document defined assumed a user watching the screen, and a terminal session routinely runs for minutes while its user is in another window — so a permission prompt could block a turn indefinitely with nothing to say so, and a long turn could finish into an empty room. New §2.1 defines the attention channel: one setting (`notifications`: `auto` / `bell` / `iterm2` / `kitty` / `ghostty` / `off`), exactly three triggers (a permission prompt surfaced, a turn of 10s or more ended, a turn failed at flow level), and one-line copy that states the state and nothing else (`Waiting for permission` · `Turn complete` · `Turn failed`). `auto` reads the terminal and falls back to the bell rather than probing: a notification protocol has no query, so a wrong guess would be silence. The terminal title (`OSC 2`) carries the same three states continuously — `✳ bingo — working…`, `✳ bingo — waiting for permission`, `bingo — <directory>` — and is handed back on teardown and on the v1.46 panic path, but only by a session that took it, so `off` leaves the shell's own title alone. Inside tmux the notification OSC is wrapped in the passthrough envelope the image transport already uses, while the bell and the title deliberately go bare (tmux acts on a bell it can see and owns `OSC 2` itself). Deliberately absent: focus tracking (a notification fires whether or not the terminal is in front), per-event notifications, and any body carrying information that is not already on screen.

- v1.49 (2026-08-14): Esc becomes one ordered stack, and a permission dialog stops outliving its turn (D80). Esc used to be decided by the order the key handlers happened to be written in, with the busy interrupt first — so a dropdown, an info block or the `?` panel opened over a running turn was closed by killing the turn. It now walks one list, top-down, dismissing exactly one level per press: **ask dialog → picker menus → ctrl+b agent manager → slash dropdown → ctrl+r search → error row → info lines → `?` panel → manually opened task panel → busy interrupt → bash mode on an empty input → esc-esc clears the draft**. With nothing open, the first Esc still interrupts. Ctrl+C is unchanged and skips the stack entirely: one key always means out. The running-status row stops promising what Esc will not do — it reads `(esc to close · Ns)` while a layer is stacked over the turn and `(esc to interrupt · Ns)` otherwise. **The dialog's lifetime**: `Waiting for permission…` used to survive the turn it belonged to, and every 1-9 or Enter pressed on it answered a dropped channel — silently, forever. An interrupt now settles it, as does a turn that died on its own: the waiting side is told `Cancel` (fail closed, the same result as a user deny), anything still queued behind it goes the same way, the footer row clears, and one dim line stays where the dialog was — `(pending permission dialog cancelled with the turn)`, a state line like v1.45's interrupt marker: no `❯` bubble, no send stamp, and nothing in the model's history, because the tool call that asked went down with the turn. A background agent's question is not swept up with a foreground turn's end — only requests whose waiting side is already gone are settled there. **The arrows go back to the composer**: ↑/↓ on an empty input is prompt history (and queue pull-back while busy) always. The inline running-agent selector v1.35 put on those keys is removed — it took the two keys a terminal user reaches for first as soon as any agent was running — and its one exclusive action, opening an agent's DM, moved onto the ctrl+b manager's detail view (Enter). Ctrl+G still opens the full workspace. Supersedes the ↑/↓ selector sentence in v1.35.

- v1.50 (2026-08-14): the approval prompt says what it is approving, and a refusal can say what to do instead (D81). The dialog used to be two words over a tool name — `⏺ Allow running Bash` with `Allow` / `Deny` — so the user approved a *category* while the actual command or file change stayed off screen, and the only way to stop being asked was to leave the session and edit settings by hand. It now carries a **pre-approval preview** above the options: a Bash call shows its command lines (`$ …`), an Edit or Write shows a dry-run unified diff computed by the tool itself without touching the file, both bounded (12 diff rows / 6 command rows) with `… N more lines` and `ctrl+e` to lift the bound — expanded also spells out the exact rule the session option would install (`session rule: Bash(cargo:*)`). Three options, in CC's wording: **1. `Yes`** · **2. `Yes, and don't ask again this session`** (shift+tab confirms it from anywhere in the dialog) · **3. `No, and tell bingo what to do differently (esc)`**. Option 2 is offered **only when a session rule could really silence the gate**: the permission engine derives the narrowest rule that matches this call (Bash by the command's first word `Bash(cargo:*)`, file tools by directory `Edit(/path/to/)`, WebFetch by host `WebFetch(domain:…)`, anything else by bare tool name) and verifies it against the same matcher the gate uses, so a compound command whose parts do not share a prefix (`cd /tmp && rm -rf /`) yields nothing; prompts that outrank allow rules — a user's own `ask` rule and the bypass-immune safety check (v1.26 `confirm_reason`, sensitive paths) — yield nothing either, and the option simply is not shown rather than promising a silence the gate would not keep. The rule lives in the runtime table for the session and is **never written to disk**. Option 3 opens a feedback row under itself rather than resolving: what is typed there reaches the model appended to the same sentence it always read — `<permission_error>permission denied: {tool} ({reason}). User guidance: {text}</permission_error>` — and the tool row shows it too, so the transcript records why. An empty submit, Esc from the feedback row, and Esc from the option list are all the plain refusal, unchanged wording, no guidance. A **0.4s confirm guard** ignores Enter and digits for the first moments a permission prompt is on screen, so a dialog that appears under a keystroke already in flight approves nothing; Esc, shift+tab and ctrl+e are not delayed. Every resolved prompt leaves one dim state line where the dialog was — `> yes` · `> yes, don't ask again this session` · `> no` · `> no — <what to do instead>` — the same class as v1.45's interrupt marker and v1.49's cancellation line: no `❯` bubble, no send stamp, nothing in the model's history. `ctrl+e` is the dialog's only while a dialog is open and stays readline end-of-line elsewhere; shift+tab still cycles the permission mode when no dialog is open. AskUserQuestion is untouched — its own options, its numbered `Other` row, its `User declined to answer questions` decline, and no confirm guard — and the shared modal queue, D80's Esc stack position and `cancel_asks` semantics are unchanged. The JSON host keeps protocol v1's two-option prompt: the wire reply is a bool, and widening it is a protocol version, not a rendering change.

- v1.51 (2026-08-14): the folded output gets a place it can actually be read (D82). Every collapsed row has said `(ctrl+o to expand)` since the fold existed, and in the inline host that promise could not be kept: settled rows are printed into terminal scrollback and are the terminal's property from that moment, so ctrl+o could not rewrite them. What it did instead was **reprint** — expand everything, rewind the flush cursor and freeze the whole session into scrollback again, leaving the old collapsed copies sitting above the new expanded ones, a duplicate per press. In the fullscreen host it was a different key altogether: an in-place toggle of the *last* message's folds only. Both are gone. `ctrl+o` now opens the **transcript view**: an alternate-screen pager over the whole session — every user message, reply, thinking block, tool call with its full retained output (D78's retention is what gives it something to show), in order, rebuilt by the same row builder the two hosts draw from, so markdown, diffs, images and CJK wrapping are identical to the main screen. Nothing on the main screen moves while it is open and `q` / `Esc` / `ctrl+o` hands the previous screen back untouched, which is the whole reason it is a separate screen rather than more scrollback. **Inside**: `j`/`k` and ↑/↓ scroll a line, PgUp/PgDn and space a page (one row of overlap), `g`/`G` the ends, the wheel three rows; `ctrl+e` toggles show-all — **on by default**, because a reader who opened the transcript came for what the fold hid — and off shows the same collapsed presentation the main screen shows; `/` opens a search input in the hint bar where Enter commits, `n`/`N` step through the hits (wrapping, case-insensitive, highlighted in place) and Esc cancels the typing and puts the reading position back. The one-line hint bar states where the reader is (`transcript 42%`), the hit count, and the keys, choosing a shorter hint set on a narrow terminal rather than truncating a word. **Priority**: with a permission dialog on screen ctrl+o does nothing — the pager would bury a question that is blocking a turn, and answering it takes one key. **Known limit**: the content is a snapshot taken when the view opens; a turn running behind it keeps producing events, they queue and are drained on the way out, and the pager does not grow while you read it. The `(ctrl+o to expand)` wording under collapsed rows and in the footer is unchanged — it is still exactly what the key does, it just does it somewhere the rows can change.

- v1.52 (2026-08-14): a message typed mid-turn reaches the turn it was about (D83). Enter while busy queued the message and the queue was drained at TurnEnd — so a correction typed thirty seconds into a five-minute turn was read only after the work it was correcting had finished, and the user's choice was to wait it out or interrupt and lose the turn. Queued **plain** messages are now offered to the running turn and folded in at its next **tool barrier**: the moment the tool results are assembled and the following request has not gone out yet. The text rides in that same user message, after the tool_result blocks (which the API requires to come first), under the marker `[Message from user, sent while you were working]` — the same bracketed family as `[DM from user]` (v1.31/D64) and `[Request interrupted by user]` (v1.45) — so the model can tell the interjection from the tool output beside it. Nothing is cancelled and nothing is re-prompted: this is steering, not interruption, and Esc still means interrupt. **What does not steer**, and why: a slash command (it is dispatched on the client side and has nothing to say to a running turn), a message carrying images (mounting attachments belongs to the turn-start path), and anything queued *behind* either of those — the queue is offered as a prefix, never as a filter, because letting a later message overtake an earlier one would run them in the opposite order from the one they were typed in. A turn that produces no tool call has no barrier and is unaffected; an interrupted turn, a turn stopped by a blocking Stop hook, and a cancel landing between rounds all take nothing, because a message folded into a request that is never sent would be a message swallowed. Headless runs, the JSON host and subagents have no composer and are byte-identical to before. **On screen**: absorbed messages leave the pending queue and enter the flow where the model read them, as one dim line each under the `↪` marker — no `❯` bubble, because it landed inside a turn rather than at the prompt, but it keeps its send stamp, because unlike v1.45's and v1.49's state lines the user really did write it and it really did reach the model. The reply block splits there: what the turn said before the barrier settles above the line, what it says after appears below it, the same move an AskUserQuestion answer already makes. While a turn is running with anything still queued, the composer carries CC's wording verbatim — `Press up to edit queued messages` — in the dim row under the queued `> …` lines, which are unchanged. **The race**: the turn is the authority. `↑` pull-back asks the channel first, and an item the turn has already taken is not pulled back into the composer — the pull-back does nothing and the absorption event, already in flight, is what removes it from the queue; sending it twice was the alternative.

- v1.53 (2026-08-14): a running shell command stops being a spinner, and ctrl+b becomes its exit (D84). A foreground `Bash` call showed `⎿ Running…` and nothing else from the moment it started until the moment it exited — a two-minute build and a hung `ssh` were the same picture — and the only key that reached it was Esc, which kills it. **The tail**: while a foreground command runs, the last five lines of its merged stdout/stderr hang dim under its `⎿` row (under the fold's own `⎿` hint row when the call is folded, which is the usual case), replaced as output arrives rather than appended, preceded by `… N lines` once there is more output than the five rows show. The rows live in the redrawn tail region and never reach scrollback — a running tool keeps its message unsettled — so a finished command leaves nothing behind to unprint and the completion row takes over unchanged. Terminal semantics are applied before the rows are drawn: a `\r` progress bar is the one line it visually is (not one row per frame), escape sequences are dropped rather than written through to the terminal, and every row is clipped to the width. Traffic is bounded at ~10 events/s and never repeats rows already on screen, so a build that writes a thousand lines a second costs a handful of redraws. **Ctrl+B**: while a foreground command is in flight the key moves *that command* to the background — same process, same accumulated output, no restart — and the running-status row says so for exactly as long as it is true (`esc to interrupt · ctrl+b to run in background`). The tool call returns at once with the same `async_launched` shape `background: true` returns, plus a note that the user moved it, so the model can read the task id without inferring a decision it did not make; the command then reports through the background-task lifecycle it now belongs to and notifies on completion. With no command running, ctrl+b is the background-agent manager exactly as in v1.49/D80 — the running command wins only because it is the thing on screen the key is about. The `?` panel names both, in that order. Esc is untouched: an interrupt still kills the command, tail or no tail. Headless runs, `--print` and the JSON host produce no tail, have no key to press, and gain no protocol event; a subagent's shell output stays in its own transcript and never appears in the main view's tail.

- v1.54 (2026-08-14): the composer completes what it already knows (D85). Two gaps closed at once. **`@` mentions**: there was no way to name a file or an agent except by typing it. `@` at the start of a word — start of the input or after whitespace, so `user@example.com` stays an email address — opens a dropdown over the project's files and the running background agents, fuzzy-filtered by whatever follows the `@`, in the same place and the same style as the slash dropdown, with labelled `Files` / `Agents` sections and one footer row that states the keys and whatever the list is not showing. Files come from `git ls-files --cached --others --exclude-standard` inside a repository (so `.gitignore` is honoured and the list is what the user means by "the project") and from a bounded walk outside one (depth ≤ 6, no hidden or build directories); both are capped at 5000 entries and the footer says so rather than truncating in silence. The list is gathered **once when the dropdown opens**, not per keystroke. ↑↓ move, Tab and Enter both insert — a file as its path relative to the session directory, an agent as `@name` — followed by one space; Esc closes it, keeps what was typed, and **stays** closed until the caret leaves the token, so the next keystroke does not resurrect what Esc just dismissed. A permission dialog keeps priority: nothing opens behind it, because a surface that competes for Enter must not appear under a question that is blocking a turn. **Argument completion**: past a command's name the same dropdown offers that command's *values* — `/model` the declared model catalog (then this session's fetched list, then a fresh disk cache; never a network fetch, which a per-keystroke dropdown must not do), `/theme` and `/think` their level tables, `/resume` the stored sessions, `/provider` its `login`/`logout` subcommands alongside the provider names and, after `login`, only the names login actually accepts. Every source is the data the command itself validates against, so a candidate offered is a value accepted; commands with free-form arguments (`/cd`, `/rename`, `/team message …`) offer nothing at all, and no longer fall back to re-offering the command name the user has already finished typing. The rows are the same rows in the same place for the same keys — the single visual difference is that an argument is not a command and so carries no `/`. Tab completes the argument in place, leaving the command and any earlier arguments alone, and adds a space so the next argument's list opens straight away; **Enter is still what sends the line**, and the name phase's "complete and execute" shortcut is explicitly not applied here — it would have dispatched `/deepseek-chat` as a command. Esc in the argument phase closes the dropdown and keeps the half-typed command, unlike the name phase where a bare `/` query goes with it. `EscLayer::MentionDropdown` joins D80's stack adjacent to `SlashDropdown`; the two are mutually exclusive by construction, since one funnel picks exactly one surface per edit. The `?` panel gains the `@` line, and `tab` now names all four things it completes.

- v1.55 (2026-08-14): the composer becomes a readline, and a long prompt gets a real editor (D86). Four gaps closed on one surface. **`$EDITOR`**: `ctrl+g` — and readline's `ctrl+x ctrl+e` chord, which is the same door — hands the current draft to `$VISUAL`, then `$EDITOR`, in a temp file, and puts the saved content back with one trailing newline trimmed, as a single undo step so `ctrl+_` returns to what was typed before. With neither variable set the key is not a dead press: the info tier says `set $EDITOR to edit the prompt in your editor`. A **non-zero exit keeps the draft** and says `editor exited without saving · the prompt is unchanged` — an abandoned editor is the one outcome where replacing the prompt would be unrecoverable — and an editor that cannot be run at all reports the reason rather than nothing. A successful edit is silent, because the new text on screen is the feedback. Composing while a turn runs is allowed and normal; a permission dialog keeps priority and the key is inert behind one, exactly as `ctrl+o` is. The terminal is handed over through the same claim protocol the clean teardown uses, so a panic while the editor holds the screen restores what a clean return would have. **Shift+Enter**: at startup bingo now asks the terminal whether it speaks the kitty keyboard protocol and, if so, pushes `DISAMBIGUATE_ESCAPE_CODES` — the one flag that makes `shift+enter` arrive as its own key instead of an indistinguishable `\r`, so it inserts a newline while plain Enter still sends. Terminals without support are byte-identical to before: nothing is emitted, and `\`+Enter / `ctrl+j` remain the newline every terminal can type. **Pastes stop misfiring**: a paste is not typing, so during a bracketed paste and during a detected key burst the completion surfaces *close* rather than recompute — a pasted `@` or `/` is a character, not a question, and the `@` dropdown opening mid-paste used to claim the very Enter the rest of the paste needed as a newline, turning one paste into a half-sent message. The empty-input meanings of `!` and `?` are suppressed the same way, and the funnel's per-keystroke file walk no longer runs once per pasted character. Enter inside a burst was already a literal newline and still is. Known limit, unchanged in kind: the burst heuristic cannot see a paste's first four characters — that is what bracketed paste exists to fix, and it remains the primary path. **Readline**: `ctrl+p`/`ctrl+n` are `↑`/`↓` exactly, routed through the same function, so prompt history, multi-line caret movement and the busy-turn queue pull-back all behave identically. `alt+b`/`alt+f` move one word and `alt+d`/`alt+backspace` kill one, using alphanumeric boundaries — `/`, `-`, `_` and `.` are stops, so a path is walked a segment at a time and a mistyped segment is reachable without retyping the whole path — while `ctrl+w` keeps the whole whitespace token, exactly as a shell does. The kills now feed a **kill ring** bounded at 10 instead of a single slot that each kill overwrote: consecutive kills in the same direction join into one entry in text order (so `ctrl+w ctrl+w` yanks both words back the way they were typed), `ctrl+y` inserts the newest, and `alt+y` immediately after a yank rotates the ring in place, wrapping. `alt+y` anywhere else does nothing at all rather than inserting something unasked. `ctrl+k` keeps its meaning and joins the ring. **The `?` panel** gains `ctrl+p/n`, `alt+d · alt+bksp` and the ring's `alt+y`, and `ctrl+g` now reads `edit the prompt in $EDITOR (or ctrl+x ctrl+e)` — the workspace it used to open is reached from the ctrl+b manager.
