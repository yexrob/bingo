# The terminal surface — design language

> The record M11 and every later TUI change starts from. Decisions are dated at the end; a change to this file is a change of taste, made on purpose. The 2026-08-30 comparison (tape / bench / editorial) led to the tape; the same evening the user chose the other road — a full-screen application that owns every cell — and asked for life, not restraint. This is that.

## 1. Anchor

bingo in a terminal is a counterpart you work beside for hours, and it should feel **alive**: it breathes while it thinks, its work arrives with a pulse, what it wants from you comes forward and the rest recedes. Alive is not busy — every motion says something true about state (thinking, arriving, waiting, done), nothing moves for decoration, and stillness is the default when nothing is happening. Three rules hold the energy: **hierarchy first** (the eye is led, never searched), **content over furniture** (chrome is thin and dim; colour and motion are spent on what matters), and **full control** (the surface owns the whole screen, so nothing is left to the terminal's mercy — scrolling, selection, layout and every transition are ours to make good).

The **grammar is Claude Code's**, taken whole because a person who knows it should feel at home: `⏺` for what the model says and does, `⎿` for what came back, `>` for what you said, a rounded box around the input, `✻` that sparkles through a verb while it works (`✻ Simmering… (esc to interrupt · 8s · ↓ 1.2k tokens)`), `… +12 lines (ctrl+o to expand)`, a bordered card asking `Do you want to…?` with numbered answers, a mode line `⏵⏵ accept edits on (shift+tab to cycle)`, a welcome box. On top of that grammar, what Claude Code does not do: warm truecolor, an input box that glows while the model works, bullets that pulse while live and flip when done, a comet tail on streaming text, translucent diff backgrounds, child sessions as rows you can step into, a rail, and a full screen we own. And one thing Claude Code does not do that we keep not doing: a header — nothing sits above the transcript; the one line of furniture is under the input box. Other references, one trait each: `btop` — live meters that never flicker; Charm's `gum` — motion with wit and restraint. Density is middle-high: 80 columns must be good, 120 earns a rail.

## 2. Three glances

| glance | sees | how |
|---|---|---|
| first | what it **wants from you** (a card, `needs you`); else the **latest answer** | a card has the only bright border on screen and dims what is behind it; `needs you` pulses; the answer is the brightest text after a white `⏺`, its own block |
| second | what it is **doing / did**: thinking, tool rows, output tails, live signals | `⏺ Tool(args)` rows whose bullet is orange and pulsing while live, green when done, red when failed; results under `⎿` in dim |
| third | furniture: mode, notices, where you are, model | one status line under the input box, dim; nothing above the transcript; the input box's border is dim until the model works |

## 3. Layout: the frame

The surface owns the alternate screen for the whole run. Regions, top to bottom — nothing above the transcript — with a rail past 120 columns:

```text
                                                       ┌ rail ─────────────┐
  > what is in this workspace?                         │ Todos             │
                                                       │ ☐ write the plan  │
  ⏺ I'll read the manifest first.                      │                   │
                                                       │ cargo test        │
  ⏺ Read(Cargo.toml)                                   │ ▬▬▬▬▬▬▬▬░░ 24/33  │
    ⎿  Read 3 lines                                    └───────────────────┘

  ⏺ reviewer(review the manifest)                                        ← a child session is a row; ⏎ steps in
    ⎿  Done (4 tools · 8.1k tokens · 40s)

  ⏺ The workspace is one package, demo 0.1.0. …

  > write me a note and run the tests

  ✻ Thought for 2s

  ⏺ Write(note.txt)                                                       ← bullet orange, pulsing
    ⎿  Wrote 27 bytes to note.txt

  ✻ Tinkering… (esc to interrupt · 4s · ↓ 0.4k tokens)                   ← activity row, sparkling
  ╭──────────────────────────────────────────────────────────────────╮
  │ > ▌                                                              │    ← input box, glowing while it works
  ╰──────────────────────────────────────────────────────────────────╯
  ⏵⏵ accept edits on (shift+tab to cycle)   1 needs you · context 84%   gpt-5.4  ← status line: mode · notices · place
```

- **Transcript**: a virtualised list of blocks (one per item), scrolled by us — smooth, by line, with the mouse wheel and `pgup/pgdn`; `ctrl+f` searches it; `v` or a mouse drag selects and `y`/`ctrl+c` copies through OSC 52; a long block folds to its tail and opens in place on `⏎` when focused. The frame around it is a hairline in the raised tint, not a bright box.
- **Sessions**: a child or peer is a row in the transcript where it began — `⏺ reviewer(review the manifest)` with `⎿  Running… 3 tools · 1.2k tokens` while it works, `⎿  Done (4 tools · 8.1k tokens · 40s)` after, `⎿  Needs you` pulsing — and `⏎` on that row steps into its transcript; the status line's right slot then reads `in reviewer`. A room's messages are `> reviewer: …` lines. **One list, two doors** (user-directed, 2026-09-02): `↓` on an empty composer and `ctrl+g` open the same list of sessions, a dropdown above the input box like the `/` menu — the Teams list below. It appears only on the gesture, so the one-line-of-furniture rule holds; there is still no tab strip: a session that is doing nothing is not on screen until you ask to walk them.
- **Rail** (≥ 120 columns): pinned panels and live signals (ADR-0013), each a small card, from the top row down; below 120 columns the same cards draw in the transcript under the running rows; without them the rail is not drawn.
- **Cards** are the dialog form: a permission, a question, a sign-in, the switcher, the rewind picker. A card is a bordered box under the `⎿` of the row that asked — Claude Code's `Do you want to…?` — with the only bright border on screen; what is behind it dims, it reveals top-down over three frames, and `esc` closes it in reverse. Focus moves into a card and back out — never anywhere else.
- **Sheets** take the whole frame for a moment — help, the pager for a long output or diff, an image, the full panel list — and slide up from the composer over four frames; `esc` slides them down.
- **Status line**: the one line of furniture, under the input box, three slots. Left, the mode: `⏵⏵ accept edits on (shift+tab to cycle)`. Middle, notices — only what is true now: `1 needs you (ctrl+g)` pulsing, `2 running`, `context 84%` from 70 % of the trigger warming toward `bad`, the latest notice for 4 s; `? for shortcuts` while the box is empty and nothing else is true. Right, place: `in reviewer · gpt-5.4` (`gpt-5.4` alone at the root). It is vim's line — status and message in one row.
- **Notices** are toasts without a corner: a notice fades into the status line's middle slot over two frames, holds 4 s, fades to dim and goes; the next waits its turn.
- **Nothing jumps.** The input box never moves; the transcript grows upward from a fixed baseline; a card's reveal never shifts what is behind it. Resize re-lays the frame in one draw. The **activity band is reserved**: its two rows (air + the verb row) are held even while idle, so a turn starting or ending never reflows the transcript — the verb appears and leaves in place.
- **Leaving** prints the last screenful of the transcript, plain, into the shell's normal screen so the conversation is still on the terminal after `exit` — the one thing the alternate screen would otherwise take.

### Teams

A room is not a region of the frame. It is a session, so it is already every part of §3 at once: a row in the transcript where it was opened, a view `⏎` steps into, a chip on the quick cycle, a card in the rail. What is on screen for one today, after M34:

```text
  ⏺ reviewer in #design: the plan is thin on tests     ← in a member's own transcript: who, where, what
  ⏺ scout: Two nits, else fine.                        ← a direct message came from nobody but its sender

  ⏺ OpenRoom(design)                                   ← the seats the call took, folded at five rows
    ⎿  └─ #design
          ├─ helper
          ├─ scout
          ├─ watcher [ 120s ]
          └─ parent [ live ]

  ⏺ SendMessage(#design)
    ⎿  to    #design
       from  parent
       read  by every member, as it lands

  ⏺ ListAgents
    ⎿  ├─ helper [ busy ]
       └─ scout [ idle ]
```

In the room's own view the name stands alone — `⏺ reviewer: the plan is thin on tests` — because there is only one conversation there to name; the composer reads `#design > post to the room` and the status line's right slot `in #design`.

**2026-09-03 (user-directed)**: a room reads as a conversation does. Every user item in a room's journal is a post and draws as one — the author's name in `text` bold, then the *whole* of the message in `text`, every line of it wrapped as prose under the bullet. Nothing of a post hangs dim under a `⎿` and nothing folds away: that is the shape a subsystem reporting in wears (§4's marked line), and under it the body of a message reads as collapsed metadata rather than as what somebody wrote. A post nobody signed came from the session the room hangs under — a person at that composer, or the model that holds it — and is drawn under the roster's own word for that seat, `parent`, exactly as every member reads it. There is no `>` bar in a room: what you type there is a post like any other.

And the other way round: a **member's** transcript, the holder's included, draws none of the room's machinery (ADR-0034). The wake that opens its turn (`origin.surface == "room"`) and the reading its turn's head folded in (`contributor:rooms`) are dropped at render time, as is a post an older journal copied before the fan-out stopped copying. The room's own view is one keystroke away and is where all of it is read. It is those two origins and nothing wider: a peer's message names a conversation of its own and still draws, and every other contributor still speaks. `ctrl+t` on the room draws the roster the room publishes, and `⏎` on that row pins it into the rail:

```text
  ❯ bingo.rooms · members                                  owed
    ├─ reviewer                                          room     owed
    └─ scout [ live ]                                    ─────────────────
                                                         #design  reviewer
```

The ears drawn there are the ones the roster declared, and only the ones it had to ask for: a seat on the default ear wears no badge (ADR-0034 §6). A seat that retuned its own with `Listen` says so under its own `bingo.rooms · ear:<name>` row, and reading the two together is left to a person.

**2026-09-02**: the clock left the card. It was a third column, `14:02`, one wider than the rail and folded to `14…`, and a signal republished only when a debt opens or closes cannot say an age anyway. The card names the room and who owes; the moment each was asked rides beside the table as `debts[].at` (ADR-0022 §4), and the age is the list's to say from it, at the moment it is drawn.

**The list** (built M36 from sketch C turned upright, re-cut to one column in M37) is where a person sees who is where. `↓` on an empty composer and `ctrl+g` open the same thing — one column, grouped under labels, the way a chat sidebar is:

```text
Agents
❯ ⏺ project   what is in this workspace?
  ~ reviewer  running · in #design · 3 unread · live · owes an answer · 22m
  ⏺ watcher   idle · in #design · listening · 600s
  ⏺ scout     done · 4 tools · 8.1k tokens
Rooms
  #design  2 seats · 1 owed
  #ops     3 seats
```

It is **flat**: a room's members are sessions, so they sit among the sessions and never nest under the room — a member is a thing you go *to*, and the list is how you get there. A room's own row says how big it is and what it is owed; a member's says which room it sits in, how much of it stands unread, what it hears there and what it owes. All of it is composed at render time from what the rooms plugin already publishes — the room's `members` extension, each seat's `ear:<name>` register and its `cursor:<name>` mark, the `owed` signal on the room's parent — joined surface-side, because which rows sit beside which is a decision no plugin gets to make (ADR-0013 §4). The first three of those live in the *room's* journal, the mark included (ADR-0034 §2), which is what lets a member be read without touching the member. The unread count is the posts of that journal standing after the one the mark points at; a seat at the head, one the room has no mark for, and one whose mark names a post the journal no longer holds all say nothing rather than a number they guessed.

A row reads in the order a narrow line gives it up: what the session is doing, where it sits, what stands unread there, what it hears, what it owes, whether it wants you, and last of all what it has spent. The tail is what the clip takes, because a count of tools is a thing to glance at and a debt is a thing to act on. A session that has not run a turn says **what it was asked** instead of a status — the whole of what there is to know about it yet. A **live** seat wears `~` for its dot and keeps the dot's own colour, so the glyph adds a fact rather than spending one, and `NO_COLOR` loses neither. It is the live ear that is marked, because listening is what a seat does unless its roster asked otherwise (ADR-0034 §6) and a glyph on every row says nothing about any of them; for the same reason the words say `live` and `listening · 600s`, and the default patient ear says nothing at all. A room wears no dot at all: its `#` is its own sigil, and it sits in the column the dots do.

`↑`/`↓` walk the whole column and step over the labels, which are furniture and nowhere to land; the view follows the cursor as the old strip's walk did, so walking *is* switching. `⏎` keeps where the walk landed, `esc` gives back the session it was opened from, and a click on a row does what the cursor there does — a click on a label does nothing, because there is nothing there. One `window::around` over the whole list keeps the row the keyboard is on on the screen, with a `…` at an end it cut; a label past that end is simply not drawn, and one at the window's own edge stays. The labels are dim and sit at the margin the rows are indented from — that column is what tells a dim label from a dim row. With only one kind of session in the tree there are no labels at all: a name for the only thing on screen separates nothing.

What it still does not do: show a room's roster without being asked for (sketch A, a rail card, refused — the roster lives on the room's session, so drawing it beside a *member's* transcript would mean publishing it twice or teaching the rail to read a sibling's state).

**2026-09-02**: a row does now say how long a debt has stood — `owes an answer · 22m`, from the `debts[].at` the card carries, counted against the frame's own clock at draw time. A card an older process left has only its clock time, and that row still reads `owes an answer since 14:02`, because there is no date under `14:02` to subtract from.

## 4. Tokens

Truecolor is the native look, chosen after the terminal's background is read (OSC 10/11) and derived for light and dark alike; the eight ANSI colours are the fallback and every rule below holds in both. The body background is never painted — the terminal's stays — but cards, sheets and the rail sit on a **raised tint** one step from it, which is what gives the frame depth.

| token | ANSI | truecolor (dark) | where, and only where |
|---|---|---|---|
| `text` | default | `#ece7df` | answers, what you type, option labels — warm off-white |
| `dim` | DIM | `#8a847a` | results under `⎿`, thinking, hints, the status line, everything behind a card |
| `raised` | none | `#211d17` (bg + one step) | the bar behind a `>` line, rail cards, the surface of a sheet; never text |
| `presence` | yellow | `#d97757`, glow `#f2a07c` | bingo's own colour: the `✻` sparkle, a live `⏺`, the glowing input border, the welcome mark, progress fills — the one warm colour, and the only one that moves |
| `good` / `bad` | green / red | `#8fcf8a` / `#e0655a` | a finished `⏺` and a failed one; a failed turn's line; diff tints |
| `mode` | blue | `#8fb4de` | the `⏵⏵` on the status line and links — the one cool colour |
| `bold` | BOLD | — | tool names in `⏺ Read(…)`, card titles, markdown headings; the model's own `⏺` is bold white |

Gradients exist in two places only: a progress bar's fill (`presence` → its glow) and the comet tail of streaming text (§6). Diff lines sit on translucent `good`/`bad` tints. Colour never carries a fact alone: every state has a glyph, so `NO_COLOR` and a monochrome terminal lose nothing.

### Glyphs and words

| element | form |
|---|---|
| presence | `✻` on the activity row and in the welcome box, `presence`; the activity row's sparkles `✻ ✢ ✶ ✽` and breathes while a turn runs; when idle none of it is on screen |
| your line | `> what is in this workspace?` on a `raised` bar the width of the transcript; in a room it is a post like any other — `⏺ parent: then let us ship it`, no bar |
| a post | in the room's own view only: `⏺ reviewer: the plan is thin on tests`, the name bold and the whole message in `text` under the bullet, wrapped as prose, nothing folded. In a member's view a room's rows are not drawn at all |
| the model speaks | `⏺` bold white, then the text; a blank line before and after |
| tool row | `⏺ Read(Cargo.toml)` — the name bold, the argument plain; the bullet `presence` and pulsing while live, `good` when done, `bad` when failed |
| skill row | `❖ Skill(guide) the wire format` — a tool row with the skill's own mark in the bullet's place, one cell wide so nothing under it shifts, wearing the bullet's colours by the same states; the words the skill was given sit *outside* the parentheses, because they are what it reads and not what it is. The model's `Skill` call and a person's own `/guide` are one run and draw this one row. `BINGO_ASCII=1` gives the mark the bullet's `*` — no seventh character, since the row says `Skill(guide)` in words |
| result | `  ⎿  Read 3 lines` in dim; three tail rows while running; then `… +12 lines (ctrl+o to expand)` |
| receipt | joins the result: `⎿  Wrote 27 bytes to note.txt`, `⎿  denied — <feedback>` |
| thinking | `✻ Thinking…` dim italic while it lasts, with **exactly two dim `⎿` rows of what is being thought under it** — the newest of what has arrived, scrolling up as the deltas do, no cut mark and no key (a tool's tail keeps three; a thought keeps two, because a tool's output is read and a thought is read past); after, `✻ Thought for 2s` (`<1s` under a second) **alone — the row closes and nothing hangs under it**. What was thought is one click or one `ctrl+o` away and stays where it happened: the first press peeks at its **first** two rows under `… +N lines (ctrl+o to expand)` — nothing is moving, so the peek reads from the top — the next opens the whole of it in place, and the next takes the sheet. A thought that came back empty (redacted, or a provider that summarised nothing) is the row alone with no states at all: no tail, no fold, no key, no sheet |
| activity row | `✻ Simmering… (esc to interrupt · 8s · ↓ 1.2k tokens)`: the sparkle cycles, the verb is one of bingo's own (Simmering, Noodling, Tinkering, Rummaging, Mulling, Weaving, Sketching, Percolating — one per turn, at random), the clock ticks each second. Once `esc` has been pressed the verb is `Stopping` and the hint is gone — the key it named has been pressed — while the sparkle and the clock carry on: `✻ Stopping… (8s · ↓ 1.2k tokens)`, until `TurnCompleted` takes the row away |
| card | a bordered box (`╭ ╮ ╰ ╯` in `presence`) under the asking row's `⎿`: title bold, a diff or command preview on its tints, `Do you want to create note.txt?`, `❯ 1. Yes` / `2. Yes, allow Edit(src/) during this session` (the scope is the kernel's answer spec; no `(shift+tab)` — here shift+tab cycles the mode and does not answer) / `3. No, and tell bingo what to do differently (esc)`; no hint row under the options — every hint lives in the thing it acts on |
| input box | `╭─╮ │ > ▌ │ ╰─╯` the width of the transcript, one to ten rows; the border `dim`, `presence` and glowing while the model works; a dim placeholder until the first keystroke |
| the strip | **landed 2026-09-04 (M48)**: on a terminal that draws pictures, the pictures the line still names ride as three rows of thumbnails — at most four, each fitted into 3 rows by 12 columns with a column between, `+N` dim after the last when more were cut. `[image 2]` stays in the words: it is the record and the anchor in the sentence, and deleting it takes its thumbnail with it. **Moved 2026-09-04, at the user's word after seeing it**: the strip stands *on* the box's top border, in rows of its own indented to the prompt's column, so the box is exactly the height it was whatever is pasted — a box that grows on a paste read as the input changing shape. The rows come out of the transcript, never the draft; on a screen too short for the whole strip, none of it is drawn and the prompt stays. Same day: the token is **one thing** to the editor — backspace, delete, `←` and `→` at its edge take or step over all of `[image 2]`, never its bracket alone. Nowhere else: a terminal without graphics has the tokens, which already say what is attached |
| child row | `⏺ reviewer(review the manifest)` — the row *is* the call that spawned it (not `⏺ SpawnAgent(…)`), the name bold, the brief plain, the bullet by state as a tool's; `⎿  Starting…` before its first turn, `⎿  Running… 3 tools · 1.2k tokens` live, `⎿  Done (4 tools · 8.1k tokens · 40s)`, `⎿  Needs you` (an open question, not merely unread) pulsing; a room's row is its name alone; `⏎` steps in |
| the list | `↓` on an empty composer or `ctrl+g` (`ctrl+t` stays the panel sheet, Claude Code's todo key): a dropdown above the input box like the `/` menu, one column grouped under dim `Agents` / `Rooms` labels at the margin — the sessions that answer a model, then the rooms, a room's members among the sessions and never under the room. A row is `❯ ~ reviewer  running · in #design · 3 unread · live · owes an answer · 22m · 3 tools · 1.2k tokens`: the coloured `⏺` for the glance and the words for `NO_COLOR`, `~` in the dot's place for a seat on the live ear, the name in `text` where the cursor is and `dim` elsewhere, `needs you` and `owes an answer` in `attention`. A session that has not run says what it was asked instead of a status; a room's row is `#design  2 seats · 1 owed`, its `#` in the column the dots are in. No title and no hint row. `↑↓` walk the column, stepping over the labels, and switch the view as they go; `⏎` keeps where the walk landed, `esc` gives back where it started, a click does what the cursor there does and a click on a label does nothing. One window under the cursor covers the whole list, `…` at each end that was cut, and a row too long for the width is cut from the tail |
| status line | left `⏵⏵ accept edits on (shift+tab to cycle)` in `mode` (`default` shows nothing but the hint); middle, notices only while true — `1 needs you (ctrl+g)` pulsing `presence`, `2 running`, `context 84%` (`dim` from 70 %, `bad` from 90 % with ` · /compact`), the latest notice for 4 s, else `? for shortcuts` while the box is empty; right `in reviewer · gpt-5.4`, dim |
| welcome | a bordered box on a fresh session: `✻ Welcome to bingo!`, `/help for help · /login codex to use a subscription`, `cwd: ~/…`; scrolls away like anything else |
| todos | `☐ ☒` rows in a rail card (`bingo.tasks` through ADR-0013) |
| paths | relative inside the cwd, `~` for home, middle-elided `…` beyond 48 cells |

Spacing: `⏺` at column 0, its text at 2; `⎿` at 2, its text at 5; a blank line between blocks; cards and boxes pad by one cell. Measure for prose `min(width, 100)`.

## 5. Content kinds

Each kind has a degrade so `--print` and an IM channel never lose information:

| kind | drawn as | degrade |
|---|---|---|
| markdown | headings bold, lists `•`, quotes `│`, tables ruled, links underlined with the url dim | the text |
| code | fenced, syntax-highlighted in the palette's classes, line numbers past 8 lines, opens in a sheet | the text |
| diff | unified, coloured by column, word-level emphasis, `ctrl+e`/`⏎` to open | the unified text |
| table / key-value | hairline rules, right-aligned numbers, `–` for a missing cell | rows joined by ` · ` |
| progress | gradient fill `████████░░ 80 % · label`; a moving sheen when unbounded | `label 80 %` |
| badge | `[ text ]` in the tone's colour | `[text]` |
| tree | `├─ └─` with glyphs and badges, folds on `←` | indented lines |
| image | **kitty landed 2026-09-04 (M46, M48)**: the terminal is asked at start-up and a picture it can draw is drawn, whatever format it arrived in (`bingo-pictures` decodes; kitty takes PNG), scaled first to the pixels its cells hold. Drawn only on **kitty ≥ 0.28.0, Ghostty ≥ 1.0.0, iTerm2 ≥ 3.5.6 and Rio ≥ 0.5.27**, named by XTVERSION — the list exists because the protocol cannot be asked: `a=q` answers `OK` with no feature bit for the `U=1` placeholder cell, and WezTerm, Konsole, Warp and xterm.js all answer `OK` and draw tofu (two of them a stray picture at the cursor besides). They get the chip, and so does a terminal that will not name itself (research of 2026-09-04, M48 Verified). **Through tmux, landed 2026-09-04 (M49)**: the probe and every APC chunk travel in tmux's `DCS tmux; … ST` passthrough, so it is the terminal *behind* tmux that answers and draws, while the placeholder cells stay ordinary text tmux scrolls and repaints. It wants `set -g allow-passthrough on`, tmux ≥ 3.4 (which rewrites the cells' combining characters), an outer terminal on the list above, and bingo started in the focused pane — tmux delivers the outer terminal's answers to whichever pane is active. tmux answers DA1, XTVERSION and `CSI 16 t` itself, so only XTVERSION is asked of it bare and everything else goes wrapped; when tmux answers and nothing behind it does, one `Info` notice at the run's opening says which setting. GNU screen has no passthrough of this shape and zellij's was not researched: both keep the chip. iTerm2's own protocol, sixel, half-block cells and the full-size sheet are still owed. Everywhere else `[image: <media type>]` | `[image: name]` |
| meter | `▁▂▃▅▇` sparkline; the context meter in the `/status` sheet | `42 %` |
| view (plugin) | ADR-0013's vocabulary, any nesting, in a block, a rail card or a signal | `View::text()` |
| custom | the fold, as text: a word this surface has not learned draws nothing of its own (ADR-0038) | the fold |

## 6. Motion

A terminal moves in three ways — a glyph changes, a row appears or leaves, brightness shifts — and at 30 frames a second that is enough for motion with intent. Principles: **every motion reports a state change; stillness is the default; nothing moves for decoration; a person may switch all of it off.**

| moment | cue | rhythm |
|---|---|---|
| presence | the activity row's `✻` sparkles `✻ ✢ ✶ ✽` and breathes between 65 % and 100 % while a turn runs; the input box border glows in step; neither exists when idle | 150 ms per glyph, 1.6 s breath |
| thinking | `✻ Thinking…` dim italic over the two dim `⎿` tail rows of what has been thought so far, scrolling up as the deltas arrive; then **closes** to `✻ Thought for 2s` alone as the next block starts — the two rows go, and the transcript is left with the one row that says a thought happened. The tail wears no comet: the glow is for words being said, and thinking is where `dim` lives (§4) | per delta |
| streaming | text after a `⏺` grows in place with a **comet tail**: the last eight cells fade from `presence`'s glow to `text` | per frame, tail 180 ms |
| tool running | the row's `⏺` pulses `presence` ↔ glow; three dim `⎿` tail rows that scroll up as they arrive | 1.2 s pulse |
| tool done | the `⏺` turns `good` (or `bad`) with one bold frame; the tail folds into `⎿ … +N lines (ctrl+o to expand)` | 33 ms flash |
| block arriving | *withdrawn 2026-09-02 (§10)*: a block arrives in place, taking its own room and nothing more | — |
| activity row | appears only after 300 ms of a turn: `✻ Tinkering… (esc to interrupt · 4s · ↓ 0.4k tokens)`; one verb per turn | 300 ms delay, 1 s clock |
| card opening | the world dims; the card reveals top-down; rows are dim through the kernel's 400 ms guard and brighten as it lifts, with the bell | 3 frames + guard |
| card closing | the reverse; the world brightens | 3 frames |
| sheet | slides up from the composer; `esc` slides it down | 4 frames |
| notice | fades into the status line's middle slot; holds 4 s; fades to dim and goes | 2 frames in, 2 out |
| needs you | the `1 needs you` notice, the child row's `⎿  Needs you` and its switcher row pulse `presence` | 1 Hz |
| scroll | by line with ease-out; the wheel, `pgup/pgdn`, `ctrl+f` hits | 100 ms |
| session switch | the transcript crossfades through dim; the status line's right slot renames | 2 frames |
| context | the `context 84%` notice appears at 70 % of the trigger and warms from `dim` to `bad` across the last 20 %; absent below | per turn |
| idle | no redraw at all | 0 Hz |
| reduced motion | `BINGO_MOTION=off`: spinners freeze to `•`, no breath, no tail, no reveals, no pulse; `NO_COLOR` strips colour | — |

Budget: an animation tick of 33 ms while anything animates, none otherwise; a full draw under 4 ms at 120×40; a keystroke echoes on the next frame. Smoothness is the absence of flicker, tearing, jumps and dropped keys — the motion above sits on top of that, never instead of it.

## 7. Ergonomics

- **Hands on the keys, the mouse welcome.** Every card answers to one key (`1-9`, `y/a/n`, `⏎`, `esc`); every sheet closes with `esc`; `?` shows the whole table; `ctrl+o` opens the latest fold one rung further — a result, a thought, an action's receipt, whichever is newest or focused — until it is whole and the next press takes the sheet; `ctrl+g` — or `↓` on an empty box — opens the one list of sessions; `shift+tab` cycles the mode. The mouse scrolls, clicks a child's row, a card row or a row of the list, opens a fold, drags a selection; it is never required.
- **A key means one direction; a click goes round.** `ctrl+o` only ever opens further (shut → peek → whole → sheet) and `esc` comes back, because a key pressed twice must not undo itself. A **click on a block is one gesture on one row**: it advances the fold one step and from the whole comes back to where the block started. That start is the only thing the kinds differ by, so **a thought that is over cycles three states** — shut, its first two rows, all of it — **and everything else cycles two**: a result, an action and a quiet notice start at their five-row cut and have no shut, because only a thought has a state a person wants to skip past. It is the same fold either way — one map, written from the keyboard or from the pointer — so a quiet notice, whose cut promises no key, still opens to a click.
- **One focus, always visible.** `❯` marks what the keyboard talks to: the composer, a card row, a sheet, a focused block. Focus moves only by opening and closing, never by ambient events; a child that asks pulses until you go to it.
- **No hidden state, no idle furniture.** Nothing lives above the transcript. What is true now is on the status line: the mode; `N needs you`, `N running` and the context percentage for as long as they are true; where you are and the model. A queued line shows dim above the input box; `/status` opens the sheet with the rest.
- **Esc is one ordered stack**: sheet → card (cancel/deny) → dropdown → interrupt the turn; `ctrl+c` clears the composer, twice exits, and says so. **One `esc` ends a turn** — there is no second, harder press to learn: the kernel drops every call in flight, a shell command's process group with it. The activity row says `✻ Stopping…` from the frame the key was pressed, because the answer to a key never waits for the kernel to come back.
- **Readable widths**: prose wraps at `min(width, 100)`; tables and code never wrap — they fold to the width and open in a sheet.
- **Predictable geometry**: the input box never moves; the transcript baseline is fixed; cards and sheets are layers, not reflows.
- **Latency**: a keystroke echoes within one frame; the loop never awaits the kernel.
- **Accessibility**: colour never the only signal; `NO_COLOR`, `BINGO_MOTION=off`, `BINGO_ASCII=1` (`>` `*` `+` `x` `-` `|`) honoured; every card readable at 80×24.

## 8. Extension by plugins (ADR-0013)

A plugin never sees the TUI. It describes what to show as a `View` and chooses a lane by how long it should last:

| lane | call | lifetime | drawn |
|---|---|---|---|
| block | `ToolOutput.display = Some(view)` | with the item, in the transcript | under the tool row, folded like any output |
| panel | `host.extend(session, plugin, kind, view.into())` | journaled; back after `--continue` | a rail card, or the panel sheet |
| live | `host.signal(session, plugin, kind, view.into())` | until replaced or `Null`; gone on resume | a rail card that updates in place; under the running rows below 120 columns |

Interaction: a `View::Actions` row (`[ 1 Approve ] [ 2 Next hunk ]`) fires `Input::Action{name, args}` into the plugin's command; a question that must stop the turn is `ToolHost::ask` as always. A live `Progress` gets the gradient and the sheen for free; a `Table` in a rail card gets the hairlines; a `Badge` with tone `attention` pulses like everything that wants you. The TUI renders each node exactly once, tested once; a plugin's UI is a value asserted with `assert_eq!`.

What the surface decides, and a plugin never asks about: a panel is pinned into the rail with `⏎` on its row in the `ctrl+t` sheet, and the pin is remembered per session; a signal is a rail card the moment it arrives and needs no pinning; `tab` walks the cards and `❯` marks the one the keyboard is talking to; a key on that card fires the button it names — the plugin's `key` hint, else the button's position — and the button wears `…` until the session's stream answers. One plugin gets eight rows in the rail before the rest folds to `… +N lines`; below 120 columns the same cards draw under the running rows instead.

A word the vocabulary does not have is `View::Custom{kind, data, fold}` (ADR-0038): the plugin names the element, ships the data it owns and writes the fold, and this surface draws the fold — the same text it draws for a `kind` off the wire that is newer than the binary reading it. No kind is learned richly here yet; when one is, it is a module inside this crate and the sdk never hears of it.

The worked example is `crates/bingo-demo-ui` — read it whole; this is its middle, and `cargo test --doc -p bingo-demo-ui` compiles it:

```rust
use bingo_demo_ui::Board;
use bingo_sdk::{ContentPart, HostHandle, KernelError, SessionId, ToolOutput, View};

/// The block lane: a person reads the board, the model reads the text.
fn block() -> ToolOutput {
    ToolOutput {
        parts: vec![ContentPart::text("3 rows")],
        is_error: false,
        display: Some(Board::default().view()),
    }
}

/// The panel lane: journaled, and back after `--continue`.
async fn panel(host: &HostHandle, session: &SessionId) -> Result<(), KernelError> {
    let view = serde_json::to_value(Board::default().view()).unwrap_or_default();
    host.extend(session, "bingo.demo.ui", "board", view).await
}

/// The live lane: never journaled, gone on a resume, `Null` removes it.
async fn live(host: &HostHandle, session: &SessionId, step: u64) -> Result<(), KernelError> {
    let bar = View::Progress { value: step, total: Some(15), label: Some("cargo test".into()) };
    let payload = serde_json::to_value(bar).unwrap_or_default();
    host.signal(session, "bingo.demo.ui", "progress", payload).await
}
```

## 9. Measures

What a change is checked against, in this order: `TestBackend` snapshots for every region and layer in §3 and every node in §5, at 80×24 and 120×40; `assert_row_styled` proving colour lands only where §4 says; a frame-by-frame test with an injected clock for every row of §6 (the reveal at frames 1, 2, 3; the tail at 0, 75 and 150 ms); the tmux drives (`scripts/tui-smoke.sh` plus card, sheet, toast, signal, action, mouse); a 5 000-block transcript scrolling at 30 fps with a draw under 4 ms; an idle frame count of 0 over 2 s.

## 10. Decisions

- **2026-08-30** — Direction: the tape (A) was proposed and then set aside for a **full-screen application** at the user's call: full control over layout and motion is worth more than the terminal's native scrollback, which the frame replaces (virtualised, smooth, searchable, selectable, printed back on exit). The bench's band becomes the header's tabs; the editorial measure (≤ 100) stays for prose. The anchor is **alive**, with every motion reporting a state. Extension is UI as data (ADR-0013), unchanged by the road taken.
- **2026-08-30, later** — Grammar: **Claude Code's, taken whole** (`⏺` `⎿` `>`, the rounded input box, the sparkle and its verbs, the bordered `Do you want to…?` card, `ctrl+o to expand`, `⏵⏵ accept edits on`), at the user's call, made sexier rather than different: warm truecolor, the glowing box, pulsing bullets, the comet tail, rising blocks, diff tints. Two earlier rules are reversed on purpose: the input box keeps its border (it is the grammar's signature and the glow needs it), and the warm colour is bingo's *presence*, not a reservation for wanting — wanting is the one bright border plus the pulse. `✓ ✗ ⊘ ○ ⠹` retire in favour of a coloured `⏺`.
- **2026-08-30, night** — **No header.** The session tabs and the context bar came out at the user's call: a header is a row of furniture that is always on, and Claude Code never spends one. What it carried goes where a terminal keeps such things — a child session is a **row in the transcript** where it began (`⏺ reviewer(…)` / `⎿  Running…`, `⏎` to step in) and `ctrl+g` is a switcher dropdown; **context is a notice** (`context 84%`, from 70 %, warming to `bad`), not a meter; the model and where you are sit at the right of the **status line**, one row under the input box — vim's status and message line in one — the only furniture on screen. The rail's toggle goes with the header: below 120 columns its cards draw inline.
- **2026-08-31** — **M11b landed** (worker B, merged `ecd87c9`), with the choices the doc had not made: the look is chosen once from `NO_COLOR` / `COLORTERM` / `BINGO_ASCII` and fixed to the ANSI table under test, so no snapshot depends on the terminal that ran it; `NO_COLOR` keeps DIM and BOLD (weights, not hues); a token may be spent only in the files a test names (`theme::tests::every_token_is_spent_where_the_design_says` — a new call site is argued in that table first); `Warn` wears `presence`, `Error` `bad`, `bypassPermissions` `bad`; the permission question is `Do you want to ` + the kernel's summary lowered, its paths shortened; the ASCII table adds `...` for `…` and `-` for `•`, and keeps `·` (Latin-1); diff hunk headers are `dim`, added/removed rows sit on tints only 24 bits can draw and the `+`/`-` column carries the fact elsewhere; the welcome box spans the transcript's full width (it agrees with the input box, not the prose measure) and is drawn only for a session this surface opened; the room prompt is `#design > ` with the placeholder `post to the room`; `Pending` and `Interrupted` bullets are `dim`; a receipt names its tool only when the row above did not.
- **2026-08-31, later** — **M11a landed and the two were joined** (worker A merged `3257d59`, integration `206e38b`). A's choices: the middle slot of the status line sits against the left margin when there is no mode, else centred; `v` starts a selection only while the transcript is held; a committed search owns the keyboard until `esc`; search is case-insensitive; a card **covers** the tail of the transcript rather than pushing it up (§3: layers, not reflows), and so do the `/` dropdown and a command's block; help and the panel do not capture the keyboard, the picker does; the old context badge and sparkline are gone — the context notice and `/status` are what remain; `esc` closes a layer over its frames and it stays until the last one. Integration choices: a card is anchored only under a row of the transcript on screen (a child's item ids are its own); a card taller than its room keeps its title and its newest rows — the preview gives way, never the answers; a card pads by one cell like every box; the switcher is a dropdown above the input box like the `/` menu, with no border and no dimming; the backdrop dims through `dim` (fg on 24 bits, DIM on eight); the current search hit is `presence` + bold, other hits bold on the tint, a selection `presence` on the tint — every mark visible on eight colours; `ctrl+o` opens the latest result whole (or the focused block's) and again folds it; a tab in a result runs to the next stop of eight.
- **2026-08-31, later still** — **M11d landed** (worker D, merged `d2a0693`), with its choices: the panel sheet now captures the keyboard (pinning needs `↑/↓ ⏎`; M11a's "the panel does not capture" is reversed on purpose); a rail card is not a bordered box — a bold title on the `raised` tint, one blank row between cards, no frame; a card is titled by its `Panel`'s own title, else by the kind it was published under; the live segment below 120 columns carries no tint; `Stack` puts nothing between its children (air is a `Text{""}`); a `Tree` draws whole (no fold cursor for a value handed afresh each frame); `Code` wears markdown's fence and a line-number gutter past eight rows; `KeyValue` wears no rule, dim keys, numbers right-aligned; an unbounded `Progress` is a three-cell lit run and its count; a table's rule spans the columns and `–` marks a missing cell; a fired button reads `[ 1 Approve… ]` until the stream moves; `tab` cycles rail cards and back round to none; a digit fires only while the composer is empty and a card has focus.
- **2026-08-31, at last light** — **M11c landed and the whole of M11's motion is joined** (worker C, merged `dd6bb3a`). C's choices: one wall clock for every free-running cue (a breath, sparkle or pulse takes the clock's own turn of its period — nothing holds an origin); the breath is a triangle, eased at both ends; a ramp collapses to its two ends on eight colours, changing over halfway; **esc closes a sheet before a card** (dismissing help must never deny a permission — the order §7 always said); a blank row above the activity row; the verb is seeded from the turn id (a turn never changes its mind, a test can name it); the token count is in thousands and absent until the turn has spoken; a guarded card is dim in its rows, bright in its border; the rise is room beneath the block, never an overlap — a resize is not an arrival and what a person arrived to never rises; **the oldest notice is the one being said** and the next waits; stillness is drawn, not skipped (`BINGO_MOTION=off` holds every cue at its resting frame); the desktop hears about the root's turn only (OSC 9 for iTerm2/Terminal.app, 777 elsewhere, tmux passthrough); the kernel's pace is not the screen's — frames coalesce to one draw per 33 ms and the loop draws once more on the way out. Two loop bugs its tests caught: an arriving delta used to cancel the animation sleep forever, and the loop measured with one clock while sleeping on another. Joined with M11d at the merge, plus two fixes the join forced: **the answer to a key is never a notice** — `press ctrl+c again to exit` is derived from the armed window on the status line's middle slot, ahead of every queued notice (a queued answer arrived after its own exit window had lapsed); and an armed window counts as animating, so its hint leaves the line on its own frame when it lapses.
- **2026-08-31, evening** — **M11e landed and M11 is whole** (worker E, merged `d7d5c59`). E's choices: a fence in an answer goes through `views::code` and a GFM table through `views::table` — a fence and a plugin's block are one rendering, gutter, fold and all; **three inks and no rainbow** (`keyword`→`mode`, `comment`→`dim`, the rest `text`), and operators stay plain — punctuation is not vocabulary, and colouring `=` and `&&` would put the cool colour on every other cell; a fence whose language is `diff` or `patch` **is a diff**: it goes to the one unified-diff renderer and wears its tints, not the inks; a table's alignment comes from its cells, not GFM's delimiter row — which column reads as numbers is a fact about the values; **`ctrl+o` only ever opens further** (fold → expanded → pager) and `esc` folds — M11a's "again to fold" reversed on purpose, one key never means two directions; the pager's keys are hinted on its own title row, not a hint row (§4: the hint lives in the thing it acts on); the rewind card is a dropdown above the input box like the switcher, but **with** a bold title — unlike the switcher's, its rows do not say what they are; `esc esc` needs no clock — any other key is what says the two presses were not one gesture; a completed `@` mention keeps its `@`, so the line itself says which of its words are paths and the attachments are derived from it rather than remembered beside it; the `@` walk is sorted by path — the filesystem's order is nobody's; `BINGO_THEME` is an env var beside `BINGO_MOTION`/`BINGO_ASCII`, not a settings key — the look is settled before the kernel is up; the light palette is §4's eight at the other end of their own lightness, with `raised` one step *down* from the ground; the background is probed only where the answer would change something (24 bits on, `NO_COLOR` off, no look named). Refused: **images** — `image` + `ratatui-image` measured +33 crates against the ~25 the plan allowed (the sixel quantiser is most of it); `ItemBody::Asset` keeps §5's `[label]` degrade, and an `@`-mentioned image still becomes an attachment. Recorded: no `/rewind` command exists yet, so the picker is offered only when a `rewind` spec is in the session's catalogue — silent today, lit the day a store command lands with no change to the surface.
- **2026-08-31, after M15** — **The activity band is reserved** (user-reported: "流式开始和结束的时候不稳定"). Frame-by-frame capture showed the whole bottom-anchored transcript bouncing two rows at each end of every stream: the verb row and its air arriving reflowed everything above, and the 300 ms delay made fast turns bounce for nothing. The verb and the easing were innocent. Fix: the band's two rows are held even while idle (`demand.activity.max(2)`), so the verb appears and leaves **in place**; idle screens wear the two rows as air above the box. Every screen snapshot moved up by two; four row-math tests were re-aimed. §3's "nothing jumps" now includes the band. Also decided, user-directed: the **quick cycle** — `↓` on an empty composer turns the status line into a chip strip (agents then rooms), `←`/`→` live-switch, `↑`/`esc`/typing returns the line; `ctrl+g` stays as the full tree card. Implementation queued behind M16's TUI-touching worker. **Landed** (worker C, merged `b17bcc9`): `cycle.rs` is pure — the marked chip *is* `tree.view()`, the only new fact is `cycling: bool`; the sketch's `●/○` gave way to the roster's `⏺` vocabulary (a room wears no dot; its `#` is its name); the mark is a `❯` cursor plus text-vs-dim weight, so `NO_COLOR` loses nothing; walking wraps; a second `↓` holds it open rather than closing; `↓` with one session keeps its old job silently; the strip's `esc` answers before the escape stack (the `ctrl+f` precedent) so dismissal never interrupts a turn; a mouse click switches the view and re-marks without dismissing — the keys own dismissal. Overflow scrolls the strip under the chip you are on, `…` at each cut end. Proof of §3: a snapshot asserts every row but the status line's is byte-identical with the strip open. Carried: the 80-column help sheet, already one line over, now drops `/exit` too — the sheet wants a scroll or bottom-anchor decision; the strip has no PTY scene (the smoke fixture has one session, where `↓` is rightly inert).
- **2026-09-01** — **The loop is biased toward the person, and slow draws own up** (`53c3c24`, user-felt latency). `tokio::select!` picked randomly among ready branches, so under a frame storm whose draws ran past a tick a pending key lost the coin flip a few times, each loss another whole draw; now `biased` puts keys first (never continuously ready, starves nobody), replies second, frames ahead of the tick (or an animation could shut the stream out while draws run long). And since tracing is invisible on the alternate screen, a draw past 100 ms says so once per run on the status line — `drawing took 132ms; a debug build or a slow terminal does this` — so felt lag stops being a mystery. **Quiet notices landed** (worker K, merged `d677c5d`, user: "大黑框难看，改成 tool call 那种绿点行"): a `User` item from a subsystem surface — the closed set `agent`/`bash`/`room`/`schedule`, written down once in `transcript.rs` with the lean-loud rule in its comment — reads as a tool row does (`⏺` in the status colour, the notice's own first line as the summary, the rest under `⎿`, dim, folded at five rows), because that is what it is: something that happened, not something said to you. Channels stay loud — a Feishu message is a person. A room's own journal view becomes a chat of marked lines for the same rule (three stacked person-bars read worse); the person's own words keep the bar everywhere. A folded notice promises no key: `ctrl+o` reaches a result, so the cut says `+N lines` and nothing else — the pager for notices is carried, as is `screens.rs` sitting 31 lines under the size fail.
- **2026-09-01, small hours** — **Two dishonest clocks: a cause and its mask** (worker M, merged `d43ad4f` + `43c5cf2`; found chasing the smoke's stalled pager sheet). The cause: `animating` read every cue at the wall's *now*, but the frame on the screen was drawn at `painted`; a draw that outlasts an animation's remaining frames ends it unseen, parking a sheet at reveal frame 3 of 4 until the next keystroke. Fixed by reading every clock in `animating` at `painted` — "does the screen owe a frame?" is a question about the screen — which also makes `exit_armed`'s remembered extra frame structural. The mask: `blocks.rs`'s `Motion::moved` latched forever after any flash, redrawing the idle surface at the tick rate for the rest of the run — violating "an idle surface draws zero frames" and hiding the stall on any machine fast enough to flash. Fixed by `drawn(now)`: `moved` is re-derived from the same sample the cue is drawn from, so a block can never rest while its held frame still flashes, and rest *is* the frame just drawn. Proven as cause-and-mask by A/B: blocks-fixed plus the old `animating` reproduces the field pane to the row. Two truths recorded with it: a completion only flashes where a draw actually *saw* the rendering it replaces (why the waste was intermittent), and the slow-draw notice also masks stalls by keeping `animating` true — it stays, as a truth-teller, not a scheduler.
- **2026-09-02** — **The rise is withdrawn; a notch adds to where the transcript is going; a terminal event waits for the frame** (M34 slice A; user-reported: the transcript "refreshes line by line"). The rise put its two blank rows at the *end* of the transcript, and the viewport hangs from the foot — so what a person saw was not a block falling into place but the whole screen jumping up two rows and walking back down over three frames, once per assistant message, tool row and tool result. §3's **nothing jumps** outranks §6's cue, and the cue reported the wrong thing anyway: it moved the world, not the block. Deleted whole — the constants, the rows, the segment and the arrival bookkeeping — so §6's "block arriving" row is withdrawn and a block now takes its own room and nothing more. Two more of the same feeling, traced the same day. A wheel notch used to restart the 100 ms ease from the *interpolated* position, so a trackpad burst — notches far faster than 30 Hz — paid off about a line an event and dropped the rest; a notch now adds to the ease's **target** and eases there from wherever the transcript has reached, so ten notches are thirty lines whether they take twenty milliseconds or twenty seconds (`Scroll::target`, and the ease is `clock::ease_out` now rather than a second spelling of it). And a terminal event used to force a full paint of its own while the kernel's frames were coalesced to one draw per 33 ms: both pass the same gate now, and the frame boundary is the one wake that always draws — an event arms a draw within one tick, never later and never a second one inside it, which is §6's "a keystroke echoes on the next frame" with the waste taken out.
- **2026-09-02** — **A thought is readable where it happened, and a fold answers the pointer** (M34 worker B). `✻ Thought for 2s` used to be the whole of a thought on screen: the text was drawn nowhere, and the only way in was a click on the block followed by `⏎` — a path nothing on screen said. It now hangs under the row like any result (§4's thinking row), dim under a `⎿`, folded at five rows with the same `ctrl+o to expand`, and `ctrl+o`'s `latest()` reaches it. Choices made with it: the text lands **when the thinking is over**, not live — while it streams the row says the same thing whatever arrives, which is what lets `blocks::size` stop revising the block per delta; streaming it would put a second moving region under the one §6 already spends on the sparkle, for a summary most providers send in one piece. `Thought for 0s` becomes `<1s` (a thought that happened did not take no time) and the no-op `.max(0)` goes with it. An **empty thought** — Anthropic's `redacted_thinking`, an OpenAI turn the provider summarised nothing of — is the row alone: no fold, no key, no sheet, because a dead affordance is worse than a silent row. `ctrl+o` also reaches an **action's** result, whose `ctrl+o to expand` had been a promise nothing kept. **A click toggles a fold** (§7's new rule): one gesture on one row, opening what is folded and folding it again, for a result, a thought, an action and a quiet notice alike — the notice's cut still promises no key, because it has none, and the pointer is what opens it. It fills the same `expanded` set `ctrl+o` fills, so a block is open in one way only. Recorded with it: `/think <level>` on a session whose model resolved `reasoning: false` now names the model and `models."<provider>/<model>".reasoning = true`; the level is stored either way, so `/model` to a model that reasons is all it takes — and the config view (`thinking: null`) had been telling that truth all along while the ack said `thinking: high`.
- **2026-09-02** — **Every list a cursor walks windows itself** (user-reported: "when a picker has many options it does not scroll; the selected row goes out of view and you cannot tell what is selected"). The `/` menu and the rewind card drew the first eight rows; the switcher, the resume picker, the panel sheet and a card's answers drew every row and let the layer clip. Past that many rows the selection was not on the screen at all — §7's "`❯` marks what the keyboard talks to" was quietly false. `window.rs` is the one brick: the run of a list that keeps its cursor in view in the room it has, and a `…` line at each end it cut. The marks are **inside** the row budget — the run plus its marks come to exactly the room asked for — so a window is invisible until it is needed, and every snapshot written before this is byte-identical, which is the proof. Where there is no room for both a mark and the cursor's own row, the mark goes. Two placements, each with its own name rather than a flag: `around` centres the run on the cursor for a list of rows, `onward` starts at it for the `ctrl+t` panel, whose rows carry their views under them. Sideways stays `cycle.rs`'s own: chips have widths, so that one grows a chip at a time, while rows are all one high and this is arithmetic. `MENU_ROWS` and rewind's `ROWS` are now the size of the window rather than a truncation, and the rewind card can be walked to every turn there is. A card is the same list — a question with a dozen options keeps the one the cursor is on — and `layers::fitted` is untouched above it: "the title and the newest rows" is what a permission wants, so the preview is still what gives way first.
- **2026-09-02** — **A room looks like a room** (M34 worker E). Four things a person could not see: a room post read exactly like a direct message, because `headline()` drew the origin's principal and never its `conversation` — it now says `⏺ reviewer in #design: …` wherever the view is not the room itself, the name bold and the room dim, and the name alone in the room's own transcript. `SendMessage`, `OpenRoom` and `ListAgents` answered text and no `display`, so a person read the model's sentence under `⎿`; each now draws the block lane's own value — a `KeyValue` receipt, the seats as a tree, the roster with a state badge and the teammates under `Beside you`. The `members` extension is published as a payload that is *both* a membership and a `View::Tree`, so `ctrl+t` and a pinned card draw a roster instead of `members: ["reviewer","scout"]`: the vocabulary was not made the sole representation because a `TreeNode` has no slot for a patience (an ear would have to be parsed back out of a badge) and because `{"members":[…]}` is a shape already in people's journals — one writer mints both halves from one `&[Seat]`, which is what keeps it one fact. And the `owed` signal ADR-0022 has published since M19 was drawn for the first time, which showed that its three columns are one wider than the rail: the clock time folds to `14…`, recorded rather than cured, since every cure is a taste call this doc has not made. **Cured 2026-09-02**: the clock left the card — two columns fit the rail whole, the moment each question was asked rides beside the table as `debts[].at` in the same one-payload shape the roster uses, and the age is the session list's to say from it. Carried: a **members pane** is still nobody's — §3 "Teams" sketches the three shapes it could take (rail card, sheet, quick-cycle chips) with what each costs, and the choice is M35's.
- **2026-09-02** — **One `esc` ends the turn, and the row says so** (M34 D; user-felt: `esc` on a running command read as a dropped key). The trace: `esc` reached the kernel and cancelled the turn's token, but the executor *awaited* `Interrupt::Block` tools to completion, and every MCP and plugin tool inherited that fail-closed default — so a call that never looked at its token held the turn for as long as it liked. Nothing on the stream said an interrupt had been received, so the frames between `esc` and `TurnCompleted` were byte-identical to a turn that ended on its own. Two things were considered and one was refused: a **second, harder `esc`** (`esc` asks, `esc esc` cuts) was designed and then set aside at the user's call — Claude Code and Codex both end on the first press, a key that sometimes works is worse than one that always does, and a stage a person has to learn is a stage the design failed to avoid. What landed instead: **a tool has no say in what an interrupt does** — `ToolTraits::interrupt` and the `Interrupt` enum are gone, the executor races every call against the one token, and dropping the future is the end of it (`bingo-tool-bash`'s `Group` takes the process tree, a plugin bridge says `tool/cancel` from its drop and abandons the call). The surface's half: **the activity row answers the key on the key's own frame** — `✻ Stopping…`, sparkle and clock kept, hint dropped — from a surface-local flag holding the turn id it was pressed on, not from anything the kernel sends back. The kernel's mailbox is FIFO and may be mid-await; a row that waited for it would be the dropped key all over again. Keyed by turn id rather than a clock, so it can never speak for the turn after this one, and `TurnCompleted` takes the row away. No new event, no new port method, no ack to parse: the one thing a surface must know it already draws.
- **2026-09-02, later** — **A thought streams as it is thought** (user-directed; M34-B's hold reversed). M34-B made a finished thought readable and deliberately held the text back while it was being had: the row said `✻ Thinking…` whatever the deltas carried, which is what let `blocks::size` skip `Reasoning` and stop revising the block per delta. The user's call is that a person watches a thought arrive, as Claude Code shows one. So the running row now wears **the same three dim `⎿` tail rows a running tool does** — `transcript::tail` is the one helper, so a thought and a command move alike and neither has a second spelling of "the last rows of what is arriving" — and `blocks::size` takes its `Reasoning { text }` arm back, because the rows drawn now change per delta and a block that is not revised would show yesterday's tail. What lands when the thinking is over is unchanged: `✻ Thought for 2s`, the whole of it folded at five rows, `ctrl+o` and a click. Two things decided with it. **No comet on the tail**: §6's comet is `presence`'s glow on words being *said*, and §4 puts thinking in `dim` — a second warm light beside the sparkle would spend the one moving colour on the one thing a person is meant to read past, and a summary most providers send in one piece would strobe rather than stream. And **a thought folds only once it is one**: `input::folds` had answered for any thought with text in it, so while one streamed it stole `ctrl+o` from the result above it and opened nothing; it now asks `completed_at` — the same fact the row is drawn from — as a call folds only once it has come back. The screens a thought is read through move to `screens/thinking.rs` (the noun gets its module, and `screens.rs` gets back the room §10 recorded it was short of); the four scenes that moved keep their eight snapshots byte-identical, which is the proof they only moved.
- **2026-09-02** — **One list, two doors** (M36; the user's own choice, from §3's three sketches). Two lists of the same thing were on screen: the `↓` strip of chips along the status line and the `ctrl+g` dropdown of rows. Sketch **C** was chosen and **turned upright** — a strip is one row, so a large room scrolls under the cursor and a debt gets no more than a dot, and the same content read down the screen has room to say what it means. The strip is **deleted**, not kept beside it (`cycle.rs`, `Ui.cycling`, `tree::switcher_lines` are gone): two renderers of one list is the second representation this repo does not keep, and a key that sometimes opens one and sometimes the other is a stage a person has to learn. Two flat single-column sketches were refused on the way; what landed is **two columns** — sessions on the left, rooms on the right, `↑↓` along a column and `←→` across. A room's members sit **among the sessions** and never nested under the room: a member is a session, and the list is how you reach one, so nesting would have made a place to go look like a thing to expand. Walking switches the view, as the strip's walk did; `⏎` keeps where it landed, `esc` gives back the session it was opened from — remembered on `Switcher` as a fact about the gesture, the way `esc_armed` is, rather than read back out of the tree. Each column draws through `window::around` (§10, earlier today), which is the bug the pickers were fixed for and would have come straight back here. Choices made with it: a row says what a session is **doing**, and one that has not run a turn says **what it was asked** instead, because that is the whole of what there is to know about it and `starting…` says nothing a person did not just do; the row reads in the order a narrow column gives it up — doing, where it sits, what it hears, what it owes, whether it wants you, and last of all tools and tokens, so the clip takes the glanceable tail and never the actionable head (the permission card's "the preview gives way, never the answers", again); a listening seat wears `~` in the dot's place and keeps the dot's own colour, so the glyph adds a fact rather than spending one; the headings are drawn only where there are two columns, because a name for the only thing on screen separates nothing. The seat facts are composed **surface-side** from what `bingo.rooms` already publishes — the room's `members` extension, each seat's `ear:<name>` register, the `owed` signal on the room's parent — read as data, with a shape the surface does not recognise leaving the fact out rather than guessing; no plugin changed. Recorded rather than cured: the `owed` signal carries the **clock time** a question was asked and no date to subtract from, so a row says `owes an answer since 14:02` and not `22m` — the age would have to be invented from a `%H:%M` string, which pins a day and a timezone. **Cured the same day**: the signal now carries `debts[].at` beside its table, the row says `owes an answer · 22m` from it, and a card without debts — an older journal's — keeps the `since 14:02` arm for the reason above. Also recorded: the list is a dropdown of the *transcript's* width, so a rail card takes 24 columns from it and a row is cut harder at 120 with a rail than at 80 without one. Carried: `input.rs` crossed the 1000-line fail on the way and the **pointer** came out into `pointer.rs` — a key is a gesture with a meaning, a click is a gesture with a place. **Reversed 2026-09-02, after the user used it** (M37 brick 7): the two columns are **one column grouped under labels** — a dim `Agents` and the sessions that answer a model, a dim `Rooms` and the rooms, Slack's sidebar. The user tried the two columns and chose otherwise, and the reasons hold up: the rooms column was mostly air (a room's row is short, and the two-column split gave it half the width whatever it asked for), while the sessions column — the long rows, the ones carrying a debt and an age — paid for that air by clipping; a `←`/`→` that crosses is a second axis a person has to learn for a list that has only ever been read down; and each column kept a window of its own, so the other column showed its head whether or not that was the part worth showing. Now `↑`/`↓` walk the whole of it, the labels are stepped over and are not rows a click answers, and one window covers the list — a label past a cut end is simply not drawn. The labels sit at the left margin, dim, where nothing but the `❯` starts, because a dim label at the rows' own indent reads as another row. Landed with it: **a member's row says `3 unread`** beside the room it sits in, from ADR-0034's cursor — the room's head less the `seq` the seat's own `cursor:<room>` mark stopped at, read off the member's journal as data, and left out entirely where there is no mark or the mark is at the head.
- **2026-09-02, later still** — **A thought is stable: two rows while it streams, shut when it ends** (user-directed; the shape M34-B and the entry above were both feeling for). The complaint is one word: *stable*. A thought had been growing under the row while it arrived — three rows, the tool tail's constant — and then leaving five rows of somebody else's working parked between the question and the answer for the rest of the session. Both numbers were borrowed from a result, and a result is not what a thought is: a tool's output is **read**, a person is looking for the line that says what happened; a thought is **read past**. So a thought now streams **two** rows of its own constant, `THOUGHT_ROWS`, which says that in the one place it is written down; and when the thinking is over the row **closes** — `✻ Thought for 2s` alone, the text still where it happened, one click away. The transcript a person scrolls back through is the shape of the conversation again, not a transcript of the model's notes. Done once, for every fold. `ui.expanded: BTreeSet<ItemId>` is gone; `ui.folds: BTreeMap<ItemId, Fold>` with `Fold { Shut, Peek, Open }` replaces it, and the new module `fold.rs` owns the noun. The map holds only what a person set: a block that is not in it wears the **start** its kind has, written once in `fold::start` — a running thought at its peek (the two rows moving under `✻ Thinking…` are the whole of what says it is thinking), a finished thought `Shut`, and a result, a notice and an action at `Peek`, which is the five-row cut they always had. `fold::fold_of(folds, item)` answers everything: `transcript::item_lines` already asked "opened" once at the top and that call is now this one, `blocks::revision` carries the `Fold` where it carried a `bool`, and `transcript::kept` matches the three states where it matched two. The nice consequence, and the reason there is no second table anywhere: **a click advances the fold one step and from open comes back to the start**. That one rule gives a thought that is over three states and everything else two, because the start is the only thing they differ by — so every non-thinking snapshot is byte-identical, which is the proof that a result's click still toggles the two states it always toggled. `ctrl+o` keeps its one direction: `fold::deeper` climbs shut → peek → whole and answers `None` for the sheet, so a thought simply has one rung more than a result because it starts one rung lower; the pager's `esc` *forgets* the entry rather than writing a state, so folding back and the kind's start are the same fact and cannot drift apart. Two calls made in passing. The peek shows the **first** two rows, not the last: a tail follows something moving, and a person opening a finished thought is reading it from the beginning. And the peek keeps its `… +N lines (ctrl+o to expand)` while the streaming tail has none — the count under a live thought would change on every delta, and a tail is not a promise that the rest is reachable, whereas a cut of a finished text is exactly that promise and every other cut in the surface makes it.
- **2026-09-02, night** — **One skill, one row** (user-directed: unify `/skill` and a model-invoked skill as `Skill(xxx)` with an icon). The same thing happening had two shapes on screen: the model's `⏺ Skill(guide)` was a tool row, and a person's `/guide` was a quiet notice whose headline was the line they typed, `⏺ /guide the wire format`. A person reading back could not see that those were one run. Both are now `❖ Skill(guide) the wire format` (§4's skill row), and there is one place either is drawn — `transcript::skill_row` — over one place the question is asked, the new `skill.rs`: *is this item a skill run, and which one*. The mark is the skill's own glyph in the **bullet's place**, one cell wide, in the colour the bullet would have worn: what kind of row it is and what state it is in are two facts and each keeps its own carrier, so a skill's mark still turns `good` when its body comes back, `NO_COLOR` loses nothing, and `BINGO_ASCII=1` spends **no seventh character** — the mark degrades to the bullet's `*` and the row says `Skill(guide)` in words, which is where the fact really lives (`ascii_skill`). Choices made with it. **The catalogue decides, not the outcome**: a `/name` is a skill because `CommandSpec::family` says `skill`, never because it answered with a `Prompt` — every prompt-answering command is a skill today, but that is a fact about what has been written and not about what a prompt *means*, so a `/compact` that grew a prompt tomorrow would not silently become one. It follows that before the catalogue lands nothing is a skill and the row is the typed line, which is why `blocks::revision` carries the skill bit: the catalogue arrives after the first frames are drawn, and a block held from before it has to be drawn again. **A call that names nothing is the tool it is** — no name is invented for the screen. **The typed line stays in the item**, where a rewind reads it back; it is simply no longer the headline. The `"command"` surface string is written once now (`commands::SURFACE`), because the quiet set and the skill both ask about it. The skill's screens take their own module, `screens/skills.rs`, with every snapshot name unchanged — the proof they only moved — and `screens.rs` gets back the room. **Recorded rather than cured**: the two doors draw the same row but not the same *cut*. A call's body promises `… +N lines (ctrl+o to expand)`; a typed skill's says `… +N lines` and promises nothing, because `input::folds` deliberately does not reach a quiet notice (2026-09-01) and `pager::lines` has no arm for a `User` item — promising the key there would be the dead affordance M34-B ruled worse than a silent row. A click opens both. The cure is to teach `ctrl+o`, `pager::lines` and `pager::title` that a typed skill is a run, and it is nobody's yet. Carried in passing: the catalogue would have been `blocks::sync`'s eighth argument, so `sync` now takes the `Rows` it used to assemble and `Rows::of` is the one place that assembly is written — the surface knows what a frame is drawn against, and the cache is only asked to keep it.

- **2026-09-03** — **The ear reversed, and the mark moved into the room** (M37's other half: the list drawn against what `bingo.rooms` publishes after ADR-0034, not against what it published when the list was written). Two facts moved under the list while it was being drawn. **The default ear is patient** (ADR-0034 §6): a bare name on a roster is the chaser's 300 s, and a live seat is asked for as `name:0`. The surface's `Ear` had `Live` for its derived default, so every seat a roster passed over was drawn as the very thing the reversal exists to prevent. `Ear` now carries a written `Default` of `Listening { patience_s: None }` — and because a roster lists only the seats whose ear is *not* the default, that variant is exactly "the ear this roster did not have to ask for": a patience the surface has no number for and must not invent one for. So a row says nothing about it. **`~` moved to the live seat.** The sigil marked a listening seat, which after the reversal is nearly every row, and a glyph worn by every row is a glyph spent on nothing; it now marks the exception — which, after the storm of 2026-09-02, is the seat worth the glance. The words follow it: `live`, `listening · 600s`, and silence for the default. Two carriers for one fact is deliberate here, as it was before: the glyph is for the sweep down the column and the word is what survives a screen reader. **Unread reads the room's own register.** The mark left the member's session for the room's journal — `bingo.rooms · cursor:<member>`, `{"post": "<item-id>"}` — because a room is a `Log` session that answers nobody, so a reader can open one without being its last client and closing an idle seat under the wake just sent to it (ADR-0034 §2). The count stopped being arithmetic on `seq` and became what it always meant: the user items of the room's journal standing after the one the mark names. It buys a third silence — no mark, a payload of a shape this surface does not know, and now a mark naming a post the journal no longer holds, which the subtraction could not have noticed. **Recorded rather than cured**: a seat's own post counts against it until it next reads, because the surface counts posts and does not sort them by author. `bingo-rooms` leaves a seat's own posts out of what it hands that seat to *read* but still moves the cursor past them, so the two agree the moment a turn runs; between a post and that turn the row says one more than the plugin would. Sorting by author here would mean the surface deciding what `origin.principal` means for a post nobody signed, which is the plugin's rule to keep. Carried in passing: the fixtures had gone on saying things the plugin no longer publishes — `OpenRoom`'s tree gathered listeners under a `listening` node where `ear::nodes` is now flat and badged (`watcher [ 120s ]`, `parent [ live ]`), and a roster fixture asked for a patience of 300 that is now the default and therefore never published at all, so it asks for 600.

- **2026-09-03, later** — **The `@` dropdown offers people, and a room reads like one** (three user-directed changes to the two places a room touches the screen). **`@` now offers names before paths.** It had only ever ranked the files under the session's own directory, while the thing an `@` most often means is a person: `@name` at the head of a line is what the redirect hook delivers to a child of this session, and `@name` inside a post is what opens a mention debt against a seat. Both sets are derived from the tree at the keystroke that asks — the children of the session on screen for a model's view, the room's own `members` roster less `parent` for a room's, read as data the way the seat facts are (no plugin imported, ADR-0001). The names are ranked on the same `nucleo` matcher the paths use and come **first**, because there is a handful of them and thousands of paths and ranked together the name would never survive the eight rows. The two runs are told apart by dim `Agents` / `Files` labels at the margin, inserted at render time and only while both runs are present — the roster's grammar, and the same window keeps the row the cursor is on. A rostered name nobody holds yet is offered: the debt it opens is chased when somebody does. A room *under* the session is not offered even though a line addressed to one would arrive: a room is somewhere you post, and its own view is one keystroke away. **A member's transcript draws none of a room's machinery** — the promise ADR-0034 already made, kept at the surface. M37 stopped the fan-out copying posts, but two things still land in a member's journal and were still drawn: the wake nudge (`origin.surface == "room"`, no principal) and the reading the turn's head folded in (`contributor:rooms`). Both are dropped at render time, as is a post an older journal copied. It is those two origins and nothing wider: a peer's message names a conversation of its own and still draws, and every other contributor still speaks. **And the room's own view reads as a conversation**: every user item there is a post — the author's name bold, then the *whole* message in `text`, wrapped as prose under the bullet, nothing dim, nothing under a `⎿`, nothing folded. The marked-notice shape it had been wearing is the shape of machinery reporting in, and under it the body of a message read as collapsed metadata. A post nobody signed is the holder's and is drawn under the roster's own word for that seat, `parent` — so what a person types into a room is a post like anybody else's, and the `>` bar has no place there. Carried: `transcript.rs` crossed the 1000-line fail, and the `User` lane came out into `transcript/said.rs` — one noun, one module.
